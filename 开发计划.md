# EvidenceCraft：规范报告自动生成工作流 — 开发计划

> 目标：用大模型生成**符合规范、具备专业性**的报告。以股票研报作为第一个试跑场景（数据公开、模板典型），
> 验证流水线后平移到地学报告等数据敏感场景。
>
> 本项目**不使用** LangChain/LangGraph 等编排框架，不使用自由探索式 agent 工作流（此前用 cozz 效果一般，
> 根因是：规范报告需要的是"每段有据可查、每个数字精确、结构 100% 合规模板"的工程纪律，
> 而自由 agent 恰恰把结构决策交给了模型）。核心原则一句话：
>
> **数据用代码取，结构用 Schema 定，叙述交给 LLM，每个环节加校验。**

---

## 一、参考方案调研结论

调研过的主流开源方案及其对本项目的启示：

| 项目 | 思路 | 借鉴点 | 不照搬的原因 |
|---|---|---|---|
| [Stanford STORM](https://github.com/stanford-oval/storm)（[论文](https://arxiv.org/abs/2402.14207)） | 多视角提问→检索→大纲先行→成文 | 大纲先行的写作方式 | 面向开放网络、维基百科体 |
| [GPT-Researcher](https://github.com/assafelovic/gpt-researcher) | 规划/执行双 agent，逐节检索写作 | 分节生成的组织方式 | 数据不可控，无数字对账 |
| [LangChain open_deep_research](https://github.com/langchain-ai/open_deep_research)（[博客](https://www.langchain.com/blog/open-deep-research)） | supervisor 拆分子题，并行研究，**子任务上下文隔离** | 上下文隔离防串味，是长报告不跑偏的关键 | 检索驱动，非模板驱动 |
| [字节 DeerFlow](https://developer.volcengine.com/articles/7508666684236890138) | deep research + 人工干预计划 | 中文生态、代码质量 | 仍是通用 deep research |
| [WriteHERE](https://github.com/0voice/awesome-ai-tools) | 异质递归规划，4 万字+ 长文 | 递归规划思路 | 偏通用写作 |

**共同局限**：都是面向开放网络检索的通用 deep research。而规范报告（研报/地学报告）是
**面向固定数据源的模板化报告**——这正是通用方案效果一般的根本原因，也是自研的理由。

### 试跑对标样本

东方财富上的公司点评报告：[山高环能(000803)《Q2油脂盈利弹性加速兑现，定增审核通过打开未来成长空间》](https://data.eastmoney.com/report/info/AP202609071829090723.html)。
其结构即我们要复刻的"规范"：

1. **标题即结论**（核心判断写进标题）+ 股票代码 + 评级
2. **核心观点**：3~5 条加粗小标题，每条一段论述，全部用当期财报数据支撑
3. **盈利预测与估值**：未来 2~3 年营收/净利/PE 预测表
4. **风险提示**：固定套路的合规段落
5. **附表**：财务预测表 + 分析师声明 + 评级说明

该类报告创新空间小、模板约束强、数字全部来自固定数据源——适合当试金石。

---

## 二、总体架构

七阶段确定性流水线，每个阶段落盘为 JSON/Markdown 中间产物（天然断点，重跑自动跳过已完成阶段）：

```
① 报告规格 (schema)
   报告类型 → JSON Schema：章节树 + 每节数据槽位 + 字数/口径/术语约束
        ↓
② 数据层 (纯代码，LLM 不参与取数)
   固定数据源 adapter → facts.json（每条事实带 value/unit/source/as_of）
        ↓
③ 大纲实例化
   LLM 唯一职责：从事实包选材，生成章节级 writing brief（不写正文）
        ↓
④ 分节生成 (map-reduce，参照 open_deep_research 的上下文隔离)
   每节 = 独立 LLM 会话，只喂：该节 brief + 相关事实切片 + 该节模板与范文
   → 产出结构化 JSON（论点列表 + 引用的事实 ID）
        ↓
⑤ 数字一致性对账 (纯代码)
   正文数字与 facts.json 逐一对账；表格由代码从数据直接渲染，LLM 不碰数字
        ↓
⑥ 校验层
   规则校验（必备章节/术语/口径）+ LLM-as-judge 对标范文评分 → 不合格退回④
        ↓
⑦ 渲染
   jinja2 → HTML（前端预览） / python-docx → Word 终稿
   声明、评级说明等合规段落由模板自动补齐
```

**LLM 的职责被压缩到"把给定事实写成合格叙述"**，幻觉空间被 Schema、事实包、对账层三重夹住。

---

## 三、技术栈

| 用途 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.11+（本机 3.13） | 数据处理与文档渲染生态全在 Python |
| 模型接入 | openai SDK（OpenAI 兼容客户端） | 直连 GLM/DeepSeek/Qwen，换模型只改 base_url |
| Schema | pydantic v2 | 报告规格与 LLM 结构化输出共用一套模型 |
| 数据获取 | AkShare + 东财 JSON 接口直调（已验证✅） | 免费、无 token、中文数据全 |
| Prompt 模板 | jinja2 | prompt 与报告规格分离 |
| 终稿渲染 | python-docx（后续可选 LaTeX/PDF） | 研报交付格式 |
| 前端 | FastAPI(SSE) + 单个静态 HTML | 薄前端，见第六节 |
| 不用 | LangChain/LangGraph/Streamlit | 确定性管道不需要编排框架；Streamlit 做不了分阶段流式与产物下钻 |

依赖刻意保持最小，全部胶水代码自写，每一行可读可改。

---

## 四、目录结构（规划）

```
evidence-craft/
├── 开发计划.md              # 本文件
├── requirements.txt
├── config/
│   ├── settings.yaml        # 模型 base_url/key、数据源开关、缓存 TTL
│   └── report_types/        # 报告规格（①阶段产物，人工+LLM 共同维护）
│       └── company_review.yaml   # 公司点评：章节树、槽位、字数、范文摘录
├── datalayer/               # ② 数据层
│   ├── sources/
│   │   ├── quote.py         # 行情/估值（东财 push2，已验证）
│   │   ├── financial.py     # 财务三表（东财 datacenter / AkShare）
│   │   ├── announcement.py  # 公告全文（巨潮资讯=权威源 + 东财=聚合源，交叉校验）
│   │   ├── consensus.py     # 盈利预测一致预期（东财 reportapi）
│   │   ├── peer.py          # 同行对比（行业板块）
│   │   └── news.py          # 行业新闻（联网搜索 API，占比<10%）
│   ├── assemble.py          # 组装 facts.json：id/value/unit/yoy/source/as_of
│   └── cache.py             # 按新鲜度分级缓存：行情分钟级/财务日级/公告事件触发
├── pipeline/                # ③④⑤⑥ 流水线
│   ├── outline.py           # ③ 大纲与 writing brief
│   ├── sections.py          # ④ 分节生成（每节独立会话、结构化输出）
│   ├── reconcile.py         # ⑤ 数字对账（正文数字 ↔ facts.json）
│   ├── validate.py          # ⑥ 规则校验 + LLM-as-judge（对标范文评分）
│   └── runner.py            # 阶段编排：顺序执行、落盘、断点续跑（自写百行胶水）
├── render/                  # ⑦ 渲染
│   ├── html_report.py       # HTML 预览版（前端"最终报告"栏的内容源）
│   └── docx_report.py       # Word 终稿（python-docx + 模板）
├── webapp/                  # 薄前端
│   ├── app.py               # FastAPI：POST /api/run、GET /api/events(SSE)、产物静态服务
│   └── static/index.html    # 三栏界面（无构建工具，原生 JS）
├── run_pipeline.py          # CLI 入口：python run_pipeline.py --stock 000803
└── artifacts/               # 所有阶段产物落盘处（gitignore）
    └── 000803_20260907/
        ├── facts.json
        ├── outline.json
        ├── sections/*.json
        ├── reconcile_report.json
        ├── judge_report.json
        └── final.html / final.docx
```

---

## 五、数据层设计（"最新最全"怎么保证）

**核心认知：研报数据不是"搜"出来的，是"采"出来的。** 卖方分析师靠的是 Wind/Choice 终端而非搜索引擎，
我们复刻的是"终端"：每类数据都有固定接口，"最新最全"是接口选型与缓存策略问题。

### 数据清单与来源映射

| 研报需要的数据 | 数据源 | 新鲜度 |
|---|---|---|
| 行情、估值（PE/PB/市值） | 东财 push2 接口 | 实时 |
| 财务三表、业绩指标 | 东财 datacenter / AkShare | 季报披露后即可取，缓存日级 |
| 公告全文（定增、业绩预告等催化剂） | 巨潮资讯（**权威源**）+ 东财（聚合源） | 事件触发即拉 |
| 盈利预测一致预期 | 东财 reportapi（券商研报汇总） | 日级 |
| 同行对比 | 东财行业板块接口 | 日级 |
| 行业新闻/政策 | 联网搜索 API | 仅作补充，占比<10% |

### 关键工程约束

1. **LLM 完全不参与取数。** 唯一参与处：公告长文本的要点提取（提取结果必须回贴原文出处供人核对）。
2. **交叉校验**：聚合源（东财）与权威源（巨潮/交易所）各取一份关键数据，不一致即报警。
   研报财务数字错一位是事故，此层值得花功夫。
3. **每条 fact 可溯源**：`{"id": "rev_2026q2", "value": 5.83, "unit": "亿元", "yoy": "+12.4%",
   "source": "eastmoney/datacenter", "as_of": "2026-08-25"}` —— 正文引用数字必须带 fact id，
   对账层据此校验，前端据此高亮数据出处。
4. **adapter 模式面向平移设计**：地学报告将来只需新增监测站/钻孔库/遥感产品等 adapter，架构不动。

---

## 六、前端设计（薄）

**定位：观察窗口，不是必需品。** 流水线本身是纯 Python 函数，CLI 与 Web 共用同一入口。

```
浏览器                      FastAPI 后端
  │  POST /api/run ────────▶ 启动流水线
  │  GET  /api/events (SSE) ◀─ 阶段完成/LLM每写一段就推一条事件
  ▼
三栏界面：
├─ 左：阶段进度条 ①→⑦，实时显示当前在写哪一节、流式输出文字
├─ 中：中间产物查看器（点任意阶段看 facts.json/大纲/稿件；
│     校验失败标红，点开看哪个数字对不上账）
└─ 右：最终报告预览（渲染后的 HTML，即 docx 的内容源）
```

- "思考过程"用**阶段事件**表达而非暴露思维链：用户真正要看的是"大纲为什么这么定、
  这节引用了哪些事实、校验为什么打回"，全在落盘 JSON 里。
- 后端只做两件事：跑流水线、推事件；产物天然落盘，前端无需自己的存储。
- 不用 React/Vue 构建链、不用 Streamlit/Gradio。

---

## 七、开发阶段与里程碑

### M1 — 数据层跑通（先做，风险最高）✅ 2026-09-07 完成
- [x] `datalayer/sources/` 六个 adapter：quote / financial / announcement / consensus / peer / news
- [x] `assemble.py` 产出山高环能(000803)完整 facts.json（34 条标量事实 + 公告全文 + 一致预期 + 同行）
- [x] 交叉校验跑通（业绩报表 vs F10 主要指标，PASS）——注：巨潮接口当前 502，
      代码留有接入位（`assemble.check_financial` 的 authoritative_source 字段），恢复后追加
- [x] 分级缓存（`datalayer/cache.py`，TTL 按源配置）
- **验收**：`python run_pipeline.py --data-only --stock 000803` 产出 facts.json，✅ 通过
  （现价 7.38/PE 26.43/PB 2.28 与 push2 探测一致；2026Q2 营收 7.3538 亿、归母净利 0.651 亿
  +60.47% 与 datacenter 原始值一致；公告全文中文解码正常）
- **追加（同日）**：治理/法律公告覆盖（诉讼/担保/人事关键词，tags 分类）+ 个股资讯流
  （getListInfo，30 条标题）；主营构成（分产品/地区，含毛利率）+ 财务三表明细（4 期关键科目）
  —— 上轮评估的三个数据缺口已补齐两个，标量事实 37 条，交叉校验 PASS

### M2 — 流水线主干（生成第一版报告）✅ 2026-09-07 完成
- [x] `company_review.yaml` 报告规格：章节树 + 槽位 + 范文摘录（对标国信证券研报全文，
      PDF 直链有防盗链，正文经网页提取后人工拆解进规格）
- [x] ③大纲（`pipeline/outline.py`：标题+观点小标题+事实选材）→ ④分节生成
      （`pipeline/sections.py`：每节独立会话+结构化 JSON 输出）→ ⑤对账
      （`pipeline/reconcile.py`：79 个合法数字索引、容差匹配、公告原文出处白名单）
      → ⑦HTML 渲染（`render/html_report.py`：券商版式+溯源附注+免责声明）打通
- [x] `pipeline/llm.py`：OpenAI 兼容封装，处理推理模型特性（思维链耗尽 max_tokens
      导致空回复→自动翻倍预算重试；固定前缀+JSON mode 便于 context cache 命中）
- [x] push2 主站限频 → 行情/同行 adapter 增加镜像主机降级链（push2delay）
- **验收**：`python run_pipeline.py --full --stock 000803` 端到端产出 final.html ✅
  标题《净利同比+60.47%，定增获批打开成长空间》，四条观点小标题齐备；
  **正文 32 个数字 100% 通过对账**（31 个命中事实索引 + 1 个公告批复文号）；
  结构对标样例研报：结论式标题/核心观点/盈利预测表（一致预期均值+现价推算 PE，
  代码渲染）/风险提示/评级（规则推算"增持"）

#### M2.5 对照真报告的差距修补 ✅ 2026-09-07 完成
用户拿真研报逐段 diff 发现两处差距，修复：
- [x] 风险提示改"镜像法"：逐条审视核心观点，其依赖假设的反面即风险
      （产能/价格/汇率），公告特有风险（诉讼/担保）保留为补充。
      修复后输出与真研报高度对齐：`毛利率修复不及预期；产能释放进度不及预期；
      工业级混合油价格需求波动；境外收入汇率波动；诉讼及担保`
- [x] 行业素材缺口（真研报的 UCO 价格段）：①长公告（募集说明书等）行业章节
      摘要 digest（`announcement.industry_digest`，关键词段落抽取）——行业段落
      从"降级写区域"恢复为写出 UCO→生物柴油→欧洲需求产业链逻辑；
      ②行业新闻扫描 adapter（`news.fetch_industry`，全市场要闻流按行业关键词
      过滤，关键词配置在报告规格 yaml）；海关出口价格数据（③）按用户决策暂不做
- [x] 对账层增强：引用归一化（剥 []、标题式引用记 ignored 噪声不算 fail）、
      境外收入合计等合成事实入索引、行业新闻数字白名单
- **遗留**：行业段落无价格数字（1147.1 美元/吨级别），需 ③ 海关数据 adapter；
      ignored_citations 格式噪音留给 M3 judge 扣分
- [x] 报告头图：上证指数日 K（蜡烛+MA5/10/20/30+成交量/额副图），本地 matplotlib
      绘制（`render/kline_chart.py`），数据东财 push2his 主用、腾讯 ifzq 跨厂商兜底
      （主站限频时自动切换，数据两源一致性已抽验），图随 facts 缓存 1 分钟

### M3 — 校验闭环（质量上限所在）✅ 2026-09-07 完成
- [x] 规则校验器（`pipeline/validate.py`）：结构（标题形态/观点数/小标题与正文字数）、
      风险提示条数与格式、禁用词（第一人称/感叹号/夸大措辞）、评级合法域、
      **PE 口径互查**（PE-TTM > 静态PE 的口径疑点检测——源于溯源表 113.58 vs 26.43 的发现）
- [x] LLM-as-judge（`pipeline/judge.py`）：以国信真研报节选为对标
      （`config/reference/company_review_guosen_000803.md`），五维评分
      （结构/专业性/数据支撑/合规性/可读性，各 1~10）+ 证据化评语 + 点名 issues；
      结合对账与规则校验结果综合判定
- [x] 退回重写（`pipeline/revise.py`）：judge fail → 仅重写点名节（原稿+意见回喂）→
      重新对账/校验/评审，至多 2 轮，超限落盘转人工
- [x] runner 集成修订循环；`tests/test_reconcile.py` 验收用例
- **验收**：✅ 篡改数字用例（8.71/33.15/1.2 三个假数）全部被对账层拦下，正确数字零误报；
  ✅ 端到端跑通——首轮 judge 38/50 直接 PASS（评语专业且给证据，如"'当前估值具备吸引力'
  带有主观推销色彩"）；此前一轮曾触发两轮真实修订（74字过短/归因缺成本数据/夸大表述），
  修订循环机制验证有效
- 过程修复：模型引用输出整数导致对账崩溃（类型防御）、judge 用序号代替 slot_id
  （提示词约束+位置兜底映射）、"3家券商"量词误报（正则排除）

### M4 / M5 — 已并入《多部门泛化方案.md》新里程碑

2026-09-08 起路线图升级为多部门平台化：原 M4（前端）、M5（泛化评估）与新增的
模板工厂、数据源插件化合并重排为 **M6（Spec v2 + 模板工厂）→ M7（数据源插件化 +
部门配置）→ M8（前端工作台 + docx 终稿）→ M9（试点接入与回归）**，
详见根目录《多部门泛化方案.md》（含开发节奏决策：后端先行、前端集中 M8）。

### M6 — Spec v2 泛化 + 模板工厂（进行中）

前半段（Spec v2 + 流水线消费侧改造）✅ 2026-09-08 完成
- [x] `template_factory/schema.py`：Spec v2 pydantic Schema——章节带 kind
      （views/table/risk/text/figures）、范文下放槽位/章节级、槽位兜底声明化
      （`source:selector:limit`）、表格 renderer 分发、禁用词/受控词表/校验参数/
      派生规则/judge 范文/免责声明全部配置化；`load_spec()` 加载即校验
- [x] `company_review.yaml` 迁移 v2；流水线六个模块（outline/sections/reconcile/
      validate/judge/revise）全部改为 spec 参数贯穿，章节按 kind 查找，角色
      （writer_role）注入 prompt；渲染层章节驱动版式，评级逻辑拆出
      （`pipeline/rating.py`）；`run_pipeline.py` 增加 `--spec` 与 progress 回调
      （M8 SSE 直接转发）
- [x] 回归验收 ✅ 三股票全流程：000803 judge 37/50 PASS（基线 38，同水位）、
      600519 40/50 PASS、600754 38/50 PASS；对账零未匹配数字（仅小标题修订
      漂移类告警）；M3 篡改数字用例依旧全拦
- 回归发现的 v1 遗留缺陷（本轮修复）：
  ① 公告数字白名单对小数失效——比对取整数部分（43.96 截成 43 永远配不上），
     改为完整数值/整数双路比对；
  ② 负值事实无法对账——正文"下滑1.95%"与事实 -1.95 符号不匹配，改为按绝对值
     比对（幅度归对账，方向归 judge）；
  ③ 模型把裸公告号（AN2026...）当引用导致白名单失效——新增 `norm_citation`
     规范化（剥[]、补 ann. 前缀）；
  ④ 模型对事实数字做算术（合计/倍数）绕过对账——行文规则显式禁计算 + judge
     强制 FAIL 指令同步；
  ⑤ 字数硬伤仅在 judge 评语转述、无自己的修订指令，修订在字数区间两端震荡——
     validate FAIL 项由 judge 报告代码级合成"带方向+明确区间"的修订指令
- 后半段（模板工厂）✅ 2026-09-08 完成（真实验收待地学样例到达）
- [x] `template_factory/parsers.py`：docx/pdf(文字版)/md/txt → 统一块结构
      （标题/段落/表格/图注；硬换行并段、公文编号标题识别）——过程中修复
      数字开头正文句误判标题、md 硬换行拆段的两个解析缺陷
- [x] `template_factory/extract.py`：六步提取（骨架→章节归类→槽位细化→
      表格/图件→规则风格→多样例交叉合并），LLM 输出全部落 Spec v2 Schema
      校验；产出草案 yaml + extraction_report.md（置信度/分歧点/待人工裁决）
      + extractions.json（中间草案）
- [x] `template_factory/replay.py`：样例回放——样例数字抽成临时事实
      （reliability=manual）→ 迷你大纲 → 复用④分节与⑤对账；判定三级
      （未对账数字/缺失引用/字数越界才 FAIL），`--rounds 2` 多轮取最好抗方差
- [x] `API.md`：M6 全部模块签名与数据形状契约冻结（M8 前端照此对接）
- [x] **自测（已知答案模式）**：以国信真研报节选为样例跑提取器——4 章节归类
      全部正确（text/views/text/risk）；3 个槽位语义与手写 spec 一一对应
      （第 4 个 catalyst 缺失系范文节选未收录该段，非提取器缺陷）；槽位级
      逐字范文 3/3；risk 形态（条数/结尾词/分隔符）正确；与手写 v2 spec 的
      自动比对报告见 `company_review_draft.extraction_report.md`
- [x] **回放验证**：用提取出的草案 + 样例数据回放——三段论述全部数字 100%
      对账、字数区间内，PASS_WITH_WARN；期间回放对账曾拦下一次偶发幻觉数字
      （0.07），数字纪律防线在模板验收场景同样生效
- **M6 整体验收（待用户）**：真实地学样例 2~3 篇 → 提取 → 人工确认/修改
  点 ≤10 处 → replay 回放通过 → 定稿入库

### M7 — 数据源插件化 + 部门配置 ✅ 2026-09-08 完成

- [x] `datalayer/adapters/`：SourceAdapter 统一接口（`fetch(params) -> AdapterResult`：
      facts/collections/meta/ctx/warnings）+ 自动注册表；`rows_to_facts` 声明式
      行映射（local_file 与 database 共用）
- [x] 8 个东财源收编为 web 类首批 adapter（sources/ 模块原样保留）；事实组装逻辑
      从 assemble 内联迁入各自 adapter；`registry.run_department` 按部门 profile
      绑定拉数（`$` 引用解析：运行参数/词表/ctx，dict 递归——修复嵌套
      query_params 不解析缺陷）
- [x] `config/departments/stock_demo/profile.yaml`：绑定/词表（industry_keywords
      自 spec 迁入）/特性（kline_chart）/交叉校验/judge 范文部门级配置；
      `run_pipeline.py` 增加 `--department/--project/--period`
- [x] 四类 adapter：local_file（xlsx 声明式映射 + inbox 目录取最新 + 内容 md5
      来源指纹）、database（SQLite 查询模板 + 命名参数；真实库型换驱动复用映射）、
      rag（外部检索客户端 http_json 形态 + mock 片段；`reliability=retrieved`；
      **抽取即对账**：片段抽数的数字必须在原文中找到，否则丢弃）、web 公开出口
      （USGS 地震 API——跨厂商 web 模式验证：修复 FDSN format=json/limit 两处
      参数错误后打通）
- [x] 流水线多部门兼容：outline 双路径（股票分板块素材 / 通用事实清单，顺带修复
      title_style 未进大纲 prompt 的 v1 潜伏缺陷）；table/risk/kline 章节可选化；
      judge 评审内容与 target 按实际章节动态生成；**judge 新增【模板格式要求】块**
      （按 spec 评审，不套用其他文体惯例——地学报告被误要求"风险"结尾的根因修复）；
      rating/reconcile/sections 对缺省 collection 键容错
- [x] 回归中发现并修复的 v1 缺陷：①风险段装饰性引用炸对账——生成侧
      `_citations_resolvable` 过滤不可解析引用；②judge 看不到评级徽章——rating
      入评审输入；③judge 看不到表格——table 章节"表格+说明"一体入评审输入；
      ④judge 分数解析对非数值容错；⑤validate min_shaped=0 被 `or 3` 吞掉的
      falsy-zero bug；⑥计数量词白名单扩展（件/份/个/批次/组）
- [x] **geology_demo 合成部门端到端**：虚构矿区 GM-1（SQLite 钻探库 6 孔 +
      xlsx 化验台账 25 样品 + 规范 RAG mock 片段 3 段），spec（views/table/risk
      三章 + generic_rows 渲染器 + 槽位范文数值与台账一致）+ profile（三类
      adapter 绑定）；全流程 judge **39/50 PASS**（首轮 32 FAIL → 修订一轮过线，
      修订循环在第二个部门上完整工作）；`tests/test_bindings.py` 五项断言全过
      （SQLite/xlsx 事实、指纹变化、RAG 抽数、假数字丢弃）
- **M7 验收（对照方案）**：✅ 三股票最终回归 judge **38/42/45 全 PASS**（高于
  M6 基线 37/40/38——judge 补齐评级/表格/模板要求三项输入后全线提升）；
  ✅ local_file/database 出事实、指纹正确、对账通过；✅ rag 抽数管线
  reliability 标注 + 假数字拦截；✅ 开放出口清单首项（USGS）验证；
  ✅ 第二部门端到端（防单例过拟合）

### 明确不做（v1 范围外）
- 自由 web 检索式研究（deep research 类功能）
- PDF/LaTeX 渲染、图表自动绘制（v2）
- 多用户/Web 鉴权
- 模型训练/微调

---

## 八、主要风险与对策

| 风险 | 对策 |
|---|---|
| 免费接口改版失效（AkShare 常见） | adapter 隔离变动面；直调东财接口兜底；缓存降低重拉频率 |
| 聚合源数据错误 | 与巨潮/交易所权威源交叉校验，不一致报警 |
| LLM 叙述空泛、不像专业研报 | 范文摘录进 prompt（few-shot）+ judge 对标评分闭环 |
| 盈利预测表瞎编 | v1 预测表由"一致预期数据 + 规则公式"生成，LLM 不碰数字；v2 再考虑模型辅助测算 |
| 长报告前后口径不一致 | ④阶段上下文隔离，各节只见自己的 brief 与事实切片，⑤全局对账兜底 |
| 模型输出不守结构化格式 | pydantic + response_format 强约束，解析失败直接重试 |

---

## 九、下一步

按 M1 开工：先写 `datalayer/sources/quote.py` + `financial.py`，产出 000803 的 facts.json 雏形。
每阶段完成在本文档勾选对应条目。

---

## 十、M8 — 前端工作台·正式版（2026-09-08 完成）

- [x] **平台壳**：侧边导航 + 顶部部门上下文切换（localStorage 持久化）+ toast +
      SSE 监听封装；Vue 3 + Naive UI + ECharts 全部 CDN 本地化（static/vendor/，
      零 node 工具链）；FastAPI 薄层 `server/`（业务逻辑全在已冻结模块上）
- [x] **系统设置**：模型配置（base_url/model/api_key 掩码只写不回显/reasoning_effort）
      读写 + **热生效**（settings.reload + llm.reset）+ 连通测试；ruamel 往返保注释
- [x] **/run 生成监控**：三栏（SSE 阶段进度实时流 / 产物下钻：对账未匹配标红 +
      规则校验可视化 + judge 五维雷达（ECharts）/ final.html 内嵌预览 + docx 下载）；
      参数表单按 profile `params_schema` 动态生成；运行历史表（judge 分/判定/回看）
- [x] **docx 渲染**（render/docx_report.py，与 html 同输入）：黑白规范版式、真实
      表格（行禁跨页拆分 + 续页重复表头）、题注规范（表上/图下）、标题 keep-with-next、
      尾注并段防孤页；流水线渲染阶段直接产出 final.docx；
      **judge 视觉验收 4 页全 PASS**（首版 4 处排版缺陷全部修复）
- [x] **/studio 模板工作台**：上传样例（多选）→ SSE 推六步提取 → 草案章节卡片
      （置信度标记 + 待人工裁决清单 + 槽位范文内联展示）+ YAML/JSON 视图切换；
      手动编辑与**对话 patch 同一条路**（LLM 产结构化 ops → patchlib 应用 →
      SpecV2 校验 → **diff 预览 → 确认才落盘**，每次保存前快照）；回放验证（多轮
      取最好）/ 真实数据试跑（新模板 × 部门绑定数据）均 SSE 推进度；定稿入库
      （同名旧版自动进 versions/）
- [x] **模板库**：全部模板清单（草稿/正式）+ YAML 查看 + 版本时间线 + 任意两版
      unified diff + 一键回滚（回滚前自动留痕）
- [x] **部门管理**：profile 编辑器（绑定表：adapter 下拉 + params JSON + 删增 +
      **单绑定测试**——$ctx 链累积执行到目标绑定，返回事实数/样例/warnings）+
      词表/特性/交叉校验编辑 + 新增部门向导
- [x] **数据源**：adapter 注册表总览（13 个）/ databases + rag 连接配置（写
      settings.yaml，SQLite 只读连通测试 / RAG mock·live 探测）；playwright
      受控采集灰色开关（默认关闭，M9+）
- [x] **首页 Dashboard**：快捷入口 / 部门数·模板数统计 / 模型健康卡 / 部门卡片 /
      最近生成表
- **M8 期间发现并修复**：①extract 槽位 id 被 _snake 清洗成 unnamed（GLM 返回中文
  id）→ 去重 + slot_N 兜底（id 是大纲/回放主键）；②patchlib set 到不存在的中间
  节点 KeyError → 自动建 dict，del 越界/非法路径统一报错防注入；③前端 loadJob
  未回写 jobId 导致操作按钮永久 disabled；④docx 四处排版（孤行标题×2、附表跨页
  断裂不重复表头、孤行尾注）+ 图题歧义（K线为上证指数参照，题注已注明）
- **M8 验收（对照方案）**：✅ 浏览器完成"上传样例 → 看草案 → 对话改一处 → 回放/
  试跑 → 定稿 → /run 用模板生成 → 下载 docx"全流程（000803 在 /run 页两次完整
  跑通，final.docx 可下载）；✅ 页面改模型配置热生效 + 连通测试通过；✅ 单绑定
  测试（SQLite 12 条 / xlsx 48 条）通过；✅ 模板库 diff/回滚可用。真实验收遗留：
  地学真实样例到达后在工作台复跑一次提取定稿。

---

## 十一、M8-R1 重构：单用户"报告类型"模型 + 现代化界面（2026-09-09 完成）

按用户反馈重构：单机单用户定位，**"部门"概念移除**，界面实体改为
**报告类型 = 报告结构（report.yaml）+ 数据来源（sources.yaml）+ 生成参数**；
导航收敛为 首页 / 报告生成 / 报告类型管理 / 系统设置。

- [x] **数据迁移**：config/departments/{stock_demo,geology_demo}/profile.yaml 与
      两个 spec 合并为 config/report_types/{company_review,geology_demo_review}/
      （report.yaml + sources.yaml + versions/）；registry.load_sources /
      run_data_layer / test_binding 按新路径重写（旧名保留兼容别名）；
      run_pipeline 改 `--type`（--department 兼容别名）；config/demo_types/ 存
      演示副本供"载入演示"一键体验
- [x] **报告类型管理页**（合并原 模板工作台/模板库/部门管理）：列表（状态标签 +
      复制/删除）+ 详情六 tabs——结构（卡片表单化增删章节/槽位、排序、字段
      tooltip 字典、高级模式源码）/ 对话修改（patch diff 确认）/ 样例与提取
      （结构树 + **划选文字设为槽位范文**）/ 数据来源（**按适配器出表单字段** +
      可用变量提示 + 单绑定测试含解析后实际参数）/ 验证（**回放历史记录与对比**：
      判定/字数合规/未对账数，改一版看变化）+ 试跑 / 版本（时间线/diff/回滚，
      保留 20 份自动清理）
- [x] **状态机**：草稿 → 已验证（最近回放 PASS/PASS_WITH_WARN 自动流转）→
      已发布（可用于生成）；每状态唯一主按钮引导
- [x] **报告生成页**（原生成监控）：选已发布类型 → params_schema 动态表单 →
      **数据预检**（只跑数据层秒级暴露缺参/无数据）→ 生成（大进度视图 +
      **每步耗时与 LLM 调用次数** + **取消**，阶段边界干净退出并写 _cancelled
      标记）→ 报告预览 / 运行详情折叠（对账红点/规则校验/judge 雷达）→
      docx 下载 + **再来一次**；失败提示人话 + 复制详情（bus._humanize +
      error_detail）；报告历史按类型筛选 + 详情抽屉 + 删除
- [x] **系统设置**：模型配置（热生效+连通测试）+ **流水线参数**（judge_threshold
      默认 36 / revise_rounds 默认 2，settings.yaml pipeline 段，judge 与流水线
      消费）+ 全局连接（数据库/RAG）+ 适配器总览
- [x] **视觉与交互**：蓝色主题（#2080f0）+ 线性 SVG 图标（**全站去 emoji**）+
      页头一句话说明 + 空态引导（含"载入演示报告类型"）+ 危险操作确认 +
      禁用原因 + 标签页标题跟随 + final.html 打印样式（浏览器打印出干净 PDF）
- **重构期间修复的真实缺陷**：①api.js 非破坏性初始化（覆盖 EC 命名空间导致
  图标/字典丢失）；②hash 路由不解析 query（?id= 打不开详情）；③Shell 漏返回
  routeQuery（渲染期报错整页白屏）；④replay 槽位范文中的样例数字不在事实索引
  被误判编造（范文数字补充入账——此前 6 次回放全 FAIL 的根因）；⑤consensus_pe
  渲染器缺一致预期数据时 KeyError 天书 → 人话错误 + 修复指引；⑥run_pipeline
  事实为 0 不中止（遗留观察项）
- **验证**：浏览器全流程（新建→上传提取→卡片编辑+对话改一处→回放→配数据来源
  em_quote/em_consensus 并单绑定测试通过→发布→报告生成页选中→数据预检→生成
  000803→预览/docx→再来一次→取消→删除）实测通过；tests/test_bindings.py、
  tests/test_reconcile.py 全过；DeepSeek 切换路径 judge **41 PASS**（同时验证
  热切换模型生效）；GLM-5.3-flash 同模板 judge 34~39（压线，已知模型短板）。
  **未 commit/push，等用户验收。**

### 11.6 数据来源页人机工效改进（2026-09-09）

针对用户反馈"绑定参数不知道什么跟什么"，四项改进（不改 sources.yaml 格式与取数逻辑）：

1. **适配器自描述**：`SourceAdapter` 基类新增 `summary`/`param_schema`/`ctx_keys`
   声明，12 个内置适配器全部补齐；`GET /api/sources/adapters` 增量返回三字段
   （API.md 已同步）。前端按声明渲染参数表单（必填＊/占位示例/中文标签），
   下拉框直接显示中文说明；键值对编辑保留为兜底（历史/自定义参数不丢失）。
2. **`$` 引用可见可插入**：每个参数值下方按上下文给出可点击变量签
   （`$生成参数`/`$vocabulary.键`/`$ctx.上游产出`，按绑定位置精确计算可引用集），
   点击插入；空值或单一 `$` 引用时替换。
3. **`$ctx` 顺序校验**：引用了下方绑定才产出的 `$ctx.x` 当场红字警告（含应移动
   到哪条之前）；引用不存在的产出同样警告。卡片头部显示该适配器产出哪些
   `$ctx` 供下方引用。
4. **术语与布局**：`need` 改"数据项名称"；"测试用参数"移入绑定区标题行（只服务
   测试按钮）；"judge 对标范文"挪至"验证"tab（附保存按钮）；tab 顶部加一句用途
   说明；空状态给三步引导 + "添加第一条绑定"。

**顺手修复既有 bug**：`types.js` setup return 漏暴露 `watchTab`（自 R8 以来每次
切换标签页 naive 内部 `he(undefined)` 抛 "e is not a function"，污染
document.title 为"渲染错误"，且版本 tab 懒加载失效）。一行修复，浏览器实测
三个 tab 切换零报错。同批附带此前未提交项：settings.yaml 可选 `stock` 键
（settings.py 改 `cfg.get`）。

验证：浏览器实测（表单渲染/chips 插入/顺序警告/单绑定测试 OK 6 条事实/空状态
引导/临时类型删除）；`tests/test_bindings.py` 全过；现有三个报告类型不受影响。
**未 commit，等用户验收。**

### 11.7 全链路打通《国内高纯石英研究报告》（M9 前夜，夜间目标模式）

目标：验证"投入范文 → 提取结构 → 生成对应报告"对**单矿种矿产研究报告**
（图/表/数字齐全、内容翔实、时效性强）的泛化能力。范文：《我国高纯石英资源
勘查开发现状及思考》（中国矿业 2025）。全部改动**未 commit**，留工作区待验收。

**新增能力**：
1. **W1 本地资料库**：`datalayer/corpus.py`（PDF→切片→fragments.json，
   fitz 优先/pypdf 兜底、句子边界拼装、短片段并前、年份线索）；rag 适配器
   mock 模式升级 2-gram 关键词检索 + 片段年份透传 as_of。高纯石英资料建库
   809 片段（32 PDF 成功，7 个扫描版无文本层）。
2. **W2 联网搜索**：`datalayer/adapters/web_search.py`——api 预留→百度→搜狗
   →Bing RSS→DDG 级联（实测百度/搜狗相关性远好于 Bing RSS）；playwright 连
   本机 Edge 兜底抓正文；抽取回验原文；事实卡带最终 URL+发布日期；结果缓存。
   base.py 增 get_text；settings 增可选 web_search 段。
3. **W3 通用图表**：`render/charts.py`（bar/line/pie，中文字体同 kline 方案）；
   schema 增 ChartTemplate + Section.charts + sections_of()；图与正文绑定同一
   事实/表格，图文同数。
4. **W4 流水线泛化**：run_pipeline 按 spec 章节顺序执行（text 逐章成文
   `gen_text_section`、多表格逐节说明 `gen_table_note`、figures 渲染期出图）；
   outline 通用路径章节选材规划（section_plans）；reconcile/validate/judge/
   revise 全线扩展 texts/notes；reconcile 前缀补 rag./web.；validate 增 text
   字数与 freshness 校验；正文生成后剥离引用标注。
5. **W5 类型打通**：extract 提取范文草案（views 无槽位自动降级 text）→人工
   修订定稿 `config/report_types/hp_quartz_review/`（15 章节：4 观点+5 综述
   +3 表+4 图+风险；13 绑定：rag×5+web×3+xlsx×5）；`tools/build_hpquartz_
   tables.py` 片段库提表（逐格数字回验）5 张；judge 对标范文入库。
6. **studio 向导加固**：/api/types/{id}/extract 在已有 report.yaml 时拒绝
   （409）——提取直接落 report.yaml，防覆盖已精修 spec；服务器侧向导链路
   实测通过（临时类型提取→spec 校验→删除）。

**关键修复（均在夜间实战暴露）**：
- collections 覆盖：多 xlsx 绑定 tables 相互覆盖（单表类型从未暴露）→registry
  汇总处 dict 按键合并/list 扩展
- 对账正则回溯："174号"回溯成"17"、"4月10日"的"10日"、"4N8"的"4"、"173种"
  的"17"均误报未匹配→前瞻类补 \d/日/号/种/类/项/处 + 剥正文引用标注
- 年份正则捕获组（"20年"）；extract views 降级；企业表重复行合并

**验收结果（第 5 轮）**：`artifacts/国内高纯石英_20260911_032344/`——
judge **36 分 PASS**（结构7/专业7/数据7/合规8/可读8），对账零未知数字，
validate PASS；报告含 **4 图**（分省储量柱状/消费结构饼图/分等级产量柱状/
国内产量柱状，图注带文献来源）+ **3 表**（分省/消费/企业）+ 数据溯源附录；
policy_dynamics 引用 2025-04-10 新矿种公告（31 条 web 事实、22 条 2025 年）。
test_bindings 全过；geology_demo_review 全流程回归跑通；服务器 8765 已重启。

## 11.8 工作流重构 Phase A：views 可选化 + 严格贴范文（2026-09-11）

用户确认的新工作流方向：①类型生成 = 投范文→提取结构→**用户确认结构**→
生成报告类型；②报告生成 = 一段话意图 + 指定本地文件夹 → 系统自动整合
本地/数据库/联网数据为事实依据 → 走原流水线（Phase B 意图规划器、Phase C
Studio UI 待做）。核心原则：**报告结构跟范文走**，"核心观点"只是研报的
一种章节，不是必需品。

**A 阶段改动（已完成）**：
1. **views 降级可选**：outline（任务文本动态拼装，无 views 只规划标题+选材）、
   judge（评审对象动态生成，views/table/risk 均可缺省）、revise（None 安全 +
   修掉 table_sec.id/risk_sec.id 求值顺序崩溃）、validate（risk/forecast 规则
   缺章守卫）。dryrun/replay 仍要求 views（职责就是验观点章）。
2. **渲染贴范文**：Section 新增 heading（"0　引言"式标题覆盖）/subheading
   （"1.1　市场供需"子标题）/inline（内嵌表格位，无章节标题）/view_numbering
   （观点槽位渲染 3.1~3.5 子节编号）；charts 可配在任意章节（正文后内嵌图，
   范文"图随文走"形态）；html h3 子标题样式；docx 图号全文连续。
3. **hp_quartz_review 重构**：15 章研报形态 → 范文五棵章树 12 节——摘要/
   关键词/0 引言/1 市场供需及资源保障（1.1+1.2）/2 勘查开发现状（2.1+2.2）/
   3 找矿勘查面临主要问题（views 5 槽位 3.1~3.5，fewshot 为范文原文）/
   4 思考与建议；3 表内嵌（消费/分省/企业），4 图内嵌；无风险章、无核心观点章。
4. **数据口径对齐范文**：消费结构表换为范文图 1 口径（张海啟 2022《矿产保护
   与利用》：光伏 52.81/半导体 20.59/电光源 12.22/光纤 5.56/光学 4.63/其他
   4.19，此前误用张海啟另一篇论文口径被判内部矛盾）；石英股份产量对齐 web
   事实 15553 t；分省储量表按储量降序；企业表去掉带数字型号（IOTA 4/8、
   3500t 孤立值——表格单元格数字会进正文并被对账）。
5. **对账/健壮性**：_CITE_RE 支持全角括号引用剥除（（rag.05.15）（web.02.05、
   web.02.06）（grades.SiO2≥99.995%.purity）等模型变体，特征=括号内无空白
   含点号，总长 ≤80）；标准号 DZ/T 0467—2024 等不计为量值；引用可解析性
   兼容 xlsx 三段式 id（t in index 即认）；chat_json 非 dict 输出（模型偶发
   返回数组）统一纠错重试。
6. **judge 字数感知校准**：GLM 目测中文字数系统性偏高约 25%，问题章字数带
   180~520 收窄至 160~400（真实 400 内 judge 才不会误判超限）。

**验证**：judge 36 PASS（E 轮，F 轮为剥引用残留的干净重跑）；无 views 合成
spec 冒烟（reconcile/validate/revise 全绿）；geology_demo_review 全流程回归
（views+risk 老路径）；test_bindings / test_reconcile 全过。

## 11.9 工作流重构 Phase B：意图驱动数据规划器（2026-09-11）

新工作流②落地（CLI 优先）：`python run_pipeline.py --full --type X --project 主题
--intent "一段话" [--folder 本地资料目录]`——不再预配死绑定，数据跟着意图走。

**实现**：
1. **datalayer/planner.py**：`make_plan(spec, sources, intent, folder)` = LLM
   规划（意图+结构概览+静态绑定清单 → focus/rag 查询≤8/web 查询≤6/沿用表格）
   + **规则兜底**（LLM 失败沿用模板静态查询，plan 标注 mode=fallback 与 error，
   系统永远可跑）；`_clamp_plan` 收敛条数/取值；多主体意图由 LLM 按主体拆查询。
   `plan_to_bindings` 计划 → sources.yaml 同 schema 动态绑定（rag/web 接管查询、
   表格按 tables_kept 筛选沿用）。
2. **语料现场建库**：`ensure_corpus(folder)` 按目录内容指纹（PDF 相对路径+大小
   +mtime 的 sha256）缓存至 data/corpus/_intent/<fp>/，未变化直接复用；换库时
   全部 rag 查询指向新片段库（不与模板库混用）。
3. **registry.run_data_layer** 增 `extra_bindings`（追加）与 `bindings_override`
   （整体替换——意图模式用，防静态+动态双份查询双份事实）。
4. **run_pipeline**：--intent/--folder 参数；plan.json 随运行落 artifacts
   （规划方式、全部查询、语料指纹——可审计）；意图焦点写 facts meta.intent。
5. **outline**：meta.intent 进入大纲提示词【本次报告意图】，标题与各章选材
   优先支撑意图。

**验证（GLM 余额 429 期间完成的离线链）**：单 PDF 小目录建库 93 片段（0.3s）
→ 二次调用指纹缓存命中（rebuilt=False）→ LLM 429 自动落 fallback 计划
（5 rag/3 web/5 表格，corpus 指向新库）→ 13 个动态绑定生成正确 →
run_data_layer(bindings_override) 执行：xlsx 22 条事实+5 表格集合正常产出，
rag/web 因 429 记 warning 容错不阻塞。完整 e2e（LLM 抽取+分节写作）待余额
恢复后验证；web_search 的页面事实抽取与 rag 抽取同属 LLM 环节（抽取即对账
设计），余额不可用时整个数据层只剩 xlsx 类绑定可用。

## 11.10 工作流重构 Phase C：Studio UI（2026-09-11）

新工作流①②的界面收尾（前端 ?v=R10→R11）：

1. **结构确认步骤**（类型向导「样例与提取」页签）：extract 改为落
   report.draft.yaml 草稿（不再直接定稿）；页面新增"结构确认"卡片——
   章节清单（kind 标签/可编辑标题/上移下移/删除/槽位数摘要）→「确认结构，
   生成报告类型」→ SpecV2 校验后定稿 report.yaml → 引导继续在「结构」
   页签精修与「数据来源」配绑定。配套接口：GET structure-draft /
   POST structure-confirm / DELETE structure-draft；type_detail 增
   has_draft/draft_spec；定稿后重复确认 409、已定稿类型再提取 409。
2. **意图输入 + 资料文件夹**（生成页）：写作意图 textarea + 本地资料目录
   输入框，透传 /api/runs/start 与 /api/runs/preview（--intent/--folder）；
   预检响应带采集计划摘要（AI 规划/规则兜底、rag/web 条数、语料片段数与
   新建/复用、焦点句），留空时行为与原版一致。
3. **模型切换**：settings.yaml 切至 DeepSeek（base_url https://api.deepseek.com，
   model deepseek-flash；GLM 配置注释保留可回滚）；llm.py 客户端加 api_key
   非空/ASCII 校验（占位中文会在 HTTP 头编码处炸出难懂的
   UnicodeEncodeError）。等待用户填入 DeepSeek key 后跑意图模式全链路 e2e。

**验证（无 LLM）**：结构确认流接口级全通（草稿→确认定稿 12 章→重复确认
409→定稿后再提取 409→type_detail 状态）；浏览器实测——生成页新表单渲染、
结构确认卡片渲染、改标题→确认→定稿（指纹出现+toast）；意图模式预检
（LLM 不可用自动规则兜底：22 条 xlsx 事实+计划摘要）；非意图预检回归
（geology 60 条事实，plan=None）。清理全部测试残留。

## 11.11 意图模式 DeepSeek 全链路验证与收尾结论（2026-09-11）

**跑通验证（3 轮真实运行 + 1 次重评审）**：
- 第 1 轮（judge 34）：暴露规划器剔除图表数据源（grades/domestic_table）
  → 已修（spec 图表引用的表格强制沿用）；元话语泄漏与品位罗列 → 已加规则。
- 第 2 轮：修复取值层级写错（table_id 在 params 内层）→ 主动杀掉重修，
  用真实绑定结构重测通过。
- 第 3 轮（judge 35，4 图 3 表齐全）：暴露表格单元格数字对账孤儿（意图规划
  换查询后原同值联网事实不在）→ 已修 reconcile（本表单元格数字视为有出处，
  真实数据回验未知归零、篡改数字回归仍绿）；尺度数值跨主体复用与统计口径
  漂移 → 已加两条写作规则。
- 重评审（对第 3 轮稿件、修复后对账）：仍 35，但构成变了——data_support
  5→6，专业性 7→6。丢分项全部是模型推理/文风方差（论证跳跃、口径混写、
  计算型表述复发），非系统缺陷。

**结论**：意图模式工程验收完成（链路全通、机制修复全部落地）；judge 35 与
36 门槛的差距为 DeepSeek 文风方差，全链路重跑属掷硬币，停止跑轮。
遗留优化项：①数据层抽取并行化（当前串行 60+ 次 LLM 调用，全程 25~35 分钟，
DeepSeek 比 GLM 慢一倍）；②针对 DeepSeek 文风收紧写作模板（长句/数字密度）；
③judge 阈值按模型基线校准（36 分线是 GLM 时代标定）。

## 11.12 速度优化：数据层与调用链提速（2026-09-11）

模型横评后用户切换多次供应商（GLM 余额耗尽 → DeepSeek 官方 35 分/25~35min
→ 百炼专属 qwen3.8-flash 单次 24~37s 且偶发 >120s 超时炸跑 → 硅基流动
DeepSeek-V4-Flash 3.2s/次）。速度成瓶颈后实施四项工程优化：

1. **rag 批量抽取**（rag.py）：每绑定 top_k 个片段合并为一次 LLM 调用
   （facts 带 frag 序号），数据层调用数 40+ → 6~8；批量失败自动退回逐片段
   老路径；"抽取即对账"逐条回验所属片段原文不变。
2. **并行化**：registry._resolve_all 重构——连续无 $ctx 依赖的绑定并发执行
   （≤6 一批），ctx 依赖链回落串行（geology 样品链不受影响）；web_search
   逐页抓取+抽取并发（≤4）；llm._stats 加锁线程安全。
3. **超时加固**（llm.py）：客户端超时 120→180s；APITimeoutError /
   RateLimitError / APIConnectionError 指数退避重试（治百炼偶发超时炸跑）。
4. **结果复用**：run_pipeline `--reuse-data <旧运行目录>` 跳过规划与数据层
   （copy facts/plan/crosscheck）；rag 抽取结果按 片段库+查询+模型 缓存
   （data/cache/rag_extract/，片段库指纹内嵌于 key）。

**实测**：硅基流动 V4-Flash 单次生成 3.2s（百炼 Qwen 24~37s 的 1/8）；
数据层（含 6 rag 批量 + 3 web 并发 + xlsx）约 8~10 分钟（同 key 并发排队
+ 反爬兜底页 Edge 逐个起浏览器是剩余大头）；**--reuse-data 全流程（写作+
评审+渲染）10~12 分钟**，迭代轮达标。

**质量横评（judge，同一范文结构）**：GLM-5.3-flash 官方 36（12min，余额
耗尽）；DeepSeek 官方 deepseek-flash 35；硅基流动 V4-Flash 31（轻量档，
文风偏短）。硅基流动同 key 下有 deepseek-ai/DeepSeek-V4-Pro 可作质量档。
遗留：反爬兜底页的 Edge 逐个启动可加白名单/限额；judge 阈值按模型校准。
