# 优化进度（F1~F4 功能增强）

> 本轮目标：F3 生成页选模型档 → F1 数据库动态规划 → F2 溯源可点击 → F4 图表扩展。
> 全程不 commit/push；进度实时更新，每项附验证证据。

## F3 生成页选模型档 ✅（代码完成，全链路证据见 F1 联跑）

**改动**
- `config/settings.yaml`：新增可选段 `model_tiers`（fast=硅基流动 V4-Flash 继承 model 段；quality=DeepSeek 官方 deepseek-flash）与 `tier_roles`（extract=fast，write=quality）。两段都删掉即回到单模型行为。
- `datalayer/settings.py`：加载 `model_tiers` / `tier_roles`。
- `pipeline/llm.py`：`chat_json(tier=...)`；`client()` 按 tier 缓存多客户端；`tier_for(role)` 角色映射（--model-tier 全局覆盖优先）；`stats()` 增加 `tiers` 分档计数（含各自模型名与秒数）。
- 调用点分工：rag/web 抽取与规划器走 `tier_for("extract")`；outline/sections/judge/revise 走 `tier_for("write")`。
- `run_pipeline.py`：`--model-tier` 参数（写入 meta.json 的 `model_tier`）；SSE 进度 payload 增加 `llm_tiers`。
- `server/routes_runs.py`：`start` 透传 `--model-tier`；`preview` body 支持 `model_tier`（仅本次预检生效，try/finally 还原）。
- `static/js/views/generate.js`：生成页"模型档位"下拉（默认/快/质量），start/preview 透传；运行进度显示分档调用次数。
- `static/index.html`：缓存参数 R11 → R12。

**验证证据**
- 分档路由冒烟（三档各一次最小 JSON 调用）：
  - `tier_for(extract)=fast`，`tier_for(write)=quality`
  - stats 输出：`{'default': {calls: 1, model: 'deepseek-ai/DeepSeek-V4-Flash'}, 'fast': {calls: 1, model: 'deepseek-ai/DeepSeek-V4-Flash'}, 'quality': {calls: 1, model: 'deepseek-flash'}}`
  - quality 档确认走 `https://api.deepseek.com`（deepseek-flash）
- 全链路（真实流水线里两档各自调用次数）见下方 F1 联跑小节。

## F1 数据库动态规划 ✅（代码+单测+联跑数据层证据）

**改动**
- `datalayer/adapters/database.py`：新增 `sanitize_sql(query, limit=200)`——SQL 安全硬约束代码强制：剥 `--`/`/* */` 注释后仅允许单条 SELECT；引号外出现分号（多语句）拒绝；ATTACH/PRAGMA 等关键字拒绝；无 LIMIT 自动补 `LIMIT 200`。SQLiteAdapter 执行前统一过此函数（静态/动态查询双保险），参数值一律字符串化绑定。
- `datalayer/planner.py`：
  - `_db_schemas()`：PRAGMA 只读 settings.databases 各 SQLite 库的表名+列名（不取业务数据）；
  - 规划 prompt 扩展【可用数据库】【可用运行参数】，输出 schema 增加 `db[]`（need/db_ref/query/query_params/id_column/name_template/value_columns/as_of_column?/table_columns?/table_id?），与 SQLiteAdapter params 契约一致；
  - `_clamp_plan` 对每条 db 计划过 sanitize_sql，违规拒绝并记 `plan["warnings"]`；静态 db 绑定不可剔除（强制进 tables_kept）；
  - `plan_to_bindings` 把 db 计划转 sqlite_query 动态绑定（query_params 值字符串化，可写 `$project` 引用运行参数）；plan.json 落查询全文可审计。
- `run_pipeline.py`：采集计划摘要增加 db 条数，规划告警打印。
- `server/routes_runs.py` preview：plan 摘要增加 `n_db`/`warnings`。

**验证证据（单测，tests/test_bindings.py ⑥⑦⑧ 全绿）**
- ⑥ sanitize_sql：DROP/多语句/DELETE/PRAGMA/注释夹带 ATTACH/INSERT 全部拒绝；无 LIMIT 自动补 LIMIT 200；已有 LIMIT 不重复；注释剥除。
- ⑦ 规划器清洗：合法查询入库（自动 LIMIT、参数字符串化）；`SELECT ...; DROP TABLE` 拒绝并记 warning；未知 db_ref 拒绝；plan_to_bindings 产出 sqlite_query 绑定。
- ⑧ adapter 双保险：DROP 直达 SQLiteAdapter 也被拒绝。

**验证证据（联跑 run2：`GM-1_20260911_221132`，意图"GM-1 钻探见矿与品位情况综述…"）**
- plan.json（mode=llm）：`rag 3 条 / web 3 条 / db 1 条 / 沿用表格 2 个`；db 查询
  `SELECT 孔号, 进尺, 见矿段数, 采样日期 FROM drill_summary WHERE 项目 = $project AND 期次 = $period LIMIT 200`
  （SQLite $ 参数风格 + query_params `{"project": "$project", "period": "$period"}`，registry 解析后按键绑定；LIMIT 自动补齐；含 table_columns 全表），查询全文落 plan.json 可审计；
- 数据层取回动态 db 事实 12 条（db_drilling_summary.ZK180x.footage/ore_intersections），总事实 103 条；
- **对账首轮 PASS（未匹配数字 0）**——run1 的 2 个 `0.0` 未匹配系 `_NUM_RE` 截断缺陷（见"联跑附带修复"），修复后消除；终稿 WARN 仅为 heading_drift（修订改写小标题与大纲漂移，设计内告警信号），无任何对不上账的数字；
- SQL 注入防护：单测 ⑦⑧ 覆盖（规划层拒绝 + adapter 层拒绝 + registry 记 warning 路径）。

**run1（`GM-1_20260911_215921`，修复前）**：plan.json 同样产出合法 db 查询（`:project` 命名参数风格）并取回 12 条 db 事实——两种参数风格均验证可用。

## 联跑（F1+F3+F4 同一轮全流程，run2 `GM-1_20260911_221132`）

- 命令：`python run_pipeline.py --full --type geology_demo_review --project GM-1 --period 2026H1 --intent "GM-1 钻探见矿与品位情况综述，重点说明钻探工程控制程度与化验品位成果"`（exit 0，约 6 分钟）；
- **F3 分档统计日志行**（新增的完成摘要）：
  `LLM 分档调用: fast档 13 次/113.3s（deepseek-ai/DeepSeek-V4-Flash）、quality档 19 次/244.9s（deepseek-flash）`
  ——抽取/规划走 fast 档（硅基流动）、大纲/分节/judge/修订走 quality 档（DeepSeek 官方），两档各自调用次数与耗时可见；
- **F4 直方图实跑**：`chart_chart_au_hist.png` 渲染成功且内嵌进 final.html（img naturalWidth>0，浏览器实测加载）；
- **F2 落地**：final.html 含 103 个附录锚点、12 个来源链接、1 行章节来源（本 run 模型可解析引用集中在 1 节）；
- judge 35 FAIL（structure 4 分：deepseek-flash 小标题超长倾向，与既有结论一致——judge≥36 线为 GLM 时代标定，现役供应商未达标属模型文风方差；对账/数据支撑/合规维度 8~9 分健康）。

## F2 溯源可点击 ✅（代码+重渲染静态核验；浏览器点击实测见收尾）

**改动**
- `render/html_report.py`（仅渲染层，不进提示词/对账/字数）：
  - 溯源附录每行加锚点 `id="src-{fact_id}"`；
  - 新增 `_sources_html`：text/views/table/risk 章节末尾追加小字行 `<p class="sec-src">本章数据来源：[编号链接] 名称=值（来源，截至 …）</p>`，编号链接指向附录锚点，至多 12 条，无引用章节不显示；
  - notes 参数改为全量 dict（表格说明的 cited_fact_ids 可用）。
- `run_pipeline.py`：html 渲染传全量 notes（docx 保持 notes_bodies 现状，不做跳转）。
- CSS：`.sec-src` 11px 灰字、锚点暗金色。

**验证证据**：意图版成品 `国内高纯石英意图版_20260911_203227` 重渲染
- 附录锚点 188 个、章节来源行 8 行、来源链接 46 个、悬空链接 0；
- 来源行样例：`本章数据来源：rag.01.00 2025年我国4N级及以上高纯石英需求预测=38.31万t（RAG:全球高纯石英资源开发利用现状及供需分析_张海啟 p.5，截至 2025年）；…`
- 浏览器实测（8765 服务器 + 内嵌浏览器）：页面加载后统计 188 锚点/46 链接/8 来源行一致；点击来源链接 `consum.电光源.share`，页面从 0 滚动到 22367px，附录对应行精确停在视口顶部（截图留证）。

## 联跑附带修复（回归中发现的历史缺陷）

1. **对账数字截断**（pipeline/reconcile.py `_NUM_RE`）：小数后紧跟单位字母（"0.97g/t"、"4.3m"）会被负前瞻回溯截断成 0/4——首轮联跑的两个 `0.0` 未匹配数字即源于此。修复：小数分支仅禁跟数字、允许字母；整数分支维持原禁集（编号碎片防护不变）。11 组用例（含 hp_quartz 场景的 52.81%/38.31万t/SiO2≥99.995%）逐一验证，test_reconcile/test_bindings/test_charts 全绿。
2. **table 章节不嵌图**（render/html_report.py）：table 分支漏掉 `_charts_html`（docx 本就全章节嵌图），geology 直方图挂在表格节时 HTML 缺图——已补。

## 回归（tier_roles 注释后 = 默认单模型行为）

- `tier_for('extract'/'write')` 均 None，默认档 = model 段（V4-Flash @ siliconflow）；
- `company_review --data-only`：exit 0，37 条事实，交叉校验 PASS；
- `geology_demo_review --data-only`：exit 0，65 条事实；
- tests/test_bindings.py（①~⑧）、test_reconcile.py、test_charts.py 全绿。

## 汇总

**交付**：F3 模型分档 → F1 数据库动态规划 → F2 溯源可点击 → F4 图表扩展，四项全部完成并各有验收证据；附带修复两个历史缺陷（对账数字截断、table 章节不嵌图）。

**最终配置态**：settings.yaml 保留 `model_tiers`（fast=V4-Flash 继承 model 段；quality=DeepSeek 官方 deepseek-flash），`tier_roles` 已注释——默认行为与改动前完全一致；需要分档时取消注释，或生成页/CLI 按次指定 `--model-tier`。

**耗时**：约 2.5 小时（含两轮 geology 意图全流程联跑各约 6~22 分钟、一轮 company/geology 数据层回归、三轮浏览器实测）。

**改动清单**：
- 后端：datalayer/settings.py、datalayer/planner.py、datalayer/adapters/database.py、pipeline/llm.py、pipeline/reconcile.py、run_pipeline.py、server/routes_runs.py、render/html_report.py、render/charts.py、template_factory/schema.py
- 前端：static/js/views/generate.js、static/index.html（R12）
- 配置/模板：config/settings.yaml、config/report_types/geology_demo_review/report.yaml（内嵌 Au 品位直方图）
- 测试/文档：tests/test_charts.py（新增）、tests/test_bindings.py（⑥⑦⑧）、API.md、优化进度.md

**验证产物**（artifacts/，gitignored）：
- `GM-1_20260911_221132`（run2：F1 对账 PASS + F3 分档日志 + F4 直方图内嵌）、`GM-1_20260911_215921`（run1）
- `artifacts/_f1_run2.log`（分档统计行）、`_reg_company.log`、`_reg_geology.log`（默认行为回归）
- 意图版重渲染 `国内高纯石英意图版_20260911_203227/final.html`（F2：188 锚点/46 链接，浏览器点击滚动实测）

**遗留问题**：
1. judge≥36 未在任何现役供应商达成（run2 35 分，structure 维度受 deepseek-flash 小标题超长拖累）——既有结论：阈值按模型校准或等 GLM 余额恢复，本轮未动；
2. server 同进程并发场景（preview 与生成同时跑）的 tier 覆盖为进程级单值，已用 try/finally + run 启动时清理收敛，极端交叉仍可能串档（单机单用户可接受）；
3. 反爬兜底页（百度文库等）依旧抓取失败记 warning——既有已知项，未在本次范围。

## F4 图表种类扩展 ✅（代码+单测；geology 实跑见联跑）

**改动**
- `template_factory/schema.py`：`ChartTemplate.type` 增加 `"scatter" | "hist"`；新增 `bins` 字段（hist 分箱数，缺省 10）；docstring 注明 scatter/hist 仅支持 table: 源。
- `render/charts.py`：`_draw_scatter`（x 数值列 + y 多序列，x/y 解析失败的点剔除，无可绘点报错）、`_draw_hist`（y 单数值列，matplotlib 分箱计数，有效值 <3 拒绝）；中文字体/配色/图注机制复用；`_save` 统一出图。
- `tests/test_charts.py`（新增）：合成表格（rows_to_table 形状）直方/散点各渲染 PNG 成功 + 两类边界拒绝用例。
- `config/report_types/geology_demo_review/report.yaml`：assay_table 章节内嵌 `chart_au_hist` 直方图（Au 品位分布，bins=8）。

**验证证据（tests/test_charts.py 全绿）**
- ① 直方图 PNG 20KB（25 个品位值/bins=8）；② 散点 PNG 39KB（x=深度数值列，y 双序列）；③ hist 多数值列拒绝；④ scatter x 列无数值拒绝。

## 补记：GLM-5.3 全流程横评（2026-09-11 深夜）

- settings.yaml 切到硅基流动 `zai-org/GLM-5.3` + `reasoning_effort: low`（实测该参数被接受，思考字数 394→28）；
- 全流程 10 分钟（53 次 LLM 调用 / 398s），facts 118 条，4 图 3 表 + 11 行章节来源 + 118 个溯源锚点；
- **judge 37 分（各供应商最高）**，五维 7/7/7/8/8 全部 >4；verdict 仍 FAIL 的唯一原因：metallogeny 节"400～600℃ 成岩温度"为模型背景知识、语料 809 片段中不存在，对账层按"数字必须有出处"拦截（设计行为，非 bug）；
- 产物：`artifacts/国内高纯石英_20260911_232116/`（final.html / final.docx）。
