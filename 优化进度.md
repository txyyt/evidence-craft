# 优化进度（F1~F4 功能增强）

> 本轮目标：F3 生成页选模型档 → F1 数据库动态规划 → F2 溯源可点击 → F4 图表扩展。
> 全程不 commit/push；进度实时更新，每项附验证证据。

## F3 生成页选模型档 ✅（代码完成，全链路证据见 F1 联跑）

**改动**
- `config/settings.yaml`：新增可选段 `model_tiers`（fast=硅基流动 V4-Flash 继承 model 段；quality=DeepSeek 官方 deepseek-flash）与 `tier_roles`（extract=fast，write=quality）。两段都删掉即回到单模型行为。
- `datalayer/settings.py`：加载 `model_tiers` / `tier_roles`。
- `pipeline/llm.py`：`chat_json(tier=...)`；`client()` 按 tier 缓存多客户端；`tier_for(role)` 角色映射（--model-tier 全局覆盖优先）；`stats()` 增加 `tiers` 分档计数（含各自模型名与秒数）。
- 调用点分工：rag/web 抽取与规划器走 `tier_for("extract")`；outline/sections/judge/revise 走 `tier_for("write")`。
- `run_pipeline.py`：`--model-tier` 参数（写入 meta.json 的 `model_tier`）；SSE 进度 payload 增加 `llm_tiers`。
- `server/routes_runs.py`：`start` 透传 `--model-tier`；`preview` body 支持 `model_tier`（仅本次预检生效，try/finally 还原）。
- `static/js/views/generate.js`：生成页"模型档位"下拉（默认/快/质量），start/preview 透传；运行进度显示分档调用次数。
- `static/index.html`：缓存参数 R11 → R12。

**验证证据**
- 分档路由冒烟（三档各一次最小 JSON 调用）：
  - `tier_for(extract)=fast`，`tier_for(write)=quality`
  - stats 输出：`{'default': {calls: 1, model: 'deepseek-ai/DeepSeek-V4-Flash'}, 'fast': {calls: 1, model: 'deepseek-ai/DeepSeek-V4-Flash'}, 'quality': {calls: 1, model: 'deepseek-flash'}}`
  - quality 档确认走 `https://api.deepseek.com`（deepseek-flash）
- 全链路（真实流水线里两档各自调用次数）见下方 F1 联跑小节。

## F1 数据库动态规划 ✅（代码+单测+联跑数据层证据）

**改动**
- `datalayer/adapters/database.py`：新增 `sanitize_sql(query, limit=200)`——SQL 安全硬约束代码强制：剥 `--`/`/* */` 注释后仅允许单条 SELECT；引号外出现分号（多语句）拒绝；ATTACH/PRAGMA 等关键字拒绝；无 LIMIT 自动补 `LIMIT 200`。SQLiteAdapter 执行前统一过此函数（静态/动态查询双保险），参数值一律字符串化绑定。
- `datalayer/planner.py`：
  - `_db_schemas()`：PRAGMA 只读 settings.databases 各 SQLite 库的表名+列名（不取业务数据）；
  - 规划 prompt 扩展【可用数据库】【可用运行参数】，输出 schema 增加 `db[]`（need/db_ref/query/query_params/id_column/name_template/value_columns/as_of_column?/table_columns?/table_id?），与 SQLiteAdapter params 契约一致；
  - `_clamp_plan` 对每条 db 计划过 sanitize_sql，违规拒绝并记 `plan["warnings"]`；静态 db 绑定不可剔除（强制进 tables_kept）；
  - `plan_to_bindings` 把 db 计划转 sqlite_query 动态绑定（query_params 值字符串化，可写 `$project` 引用运行参数）；plan.json 落查询全文可审计。
- `run_pipeline.py`：采集计划摘要增加 db 条数，规划告警打印。
- `server/routes_runs.py` preview：plan 摘要增加 `n_db`/`warnings`。

**验证证据（单测，tests/test_bindings.py ⑥⑦⑧ 全绿）**
- ⑥ sanitize_sql：DROP/多语句/DELETE/PRAGMA/注释夹带 ATTACH/INSERT 全部拒绝；无 LIMIT 自动补 LIMIT 200；已有 LIMIT 不重复；注释剥除。
- ⑦ 规划器清洗：合法查询入库（自动 LIMIT、参数字符串化）；`SELECT ...; DROP TABLE` 拒绝并记 warning；未知 db_ref 拒绝；plan_to_bindings 产出 sqlite_query 绑定。
- ⑧ adapter 双保险：DROP 直达 SQLiteAdapter 也被拒绝。

**验证证据（联跑 run2：`GM-1_20260911_221132`，意图"GM-1 钻探见矿与品位情况综述…"）**
- plan.json（mode=llm）：`rag 3 条 / web 3 条 / db 1 条 / 沿用表格 2 个`；db 查询
  `SELECT 孔号, 进尺, 见矿段数, 采样日期 FROM drill_summary WHERE 项目 = $project AND 期次 = $period LIMIT 200`
  （SQLite $ 参数风格 + query_params `{"project": "$project", "period": "$period"}`，registry 解析后按键绑定；LIMIT 自动补齐；含 table_columns 全表），查询全文落 plan.json 可审计；
- 数据层取回动态 db 事实 12 条（db_drilling_summary.ZK180x.footage/ore_intersections），总事实 103 条；
- **对账首轮 PASS（未匹配数字 0）**——run1 的 2 个 `0.0` 未匹配系 `_NUM_RE` 截断缺陷（见"联跑附带修复"），修复后消除；终稿 WARN 仅为 heading_drift（修订改写小标题与大纲漂移，设计内告警信号），无任何对不上账的数字；
- SQL 注入防护：单测 ⑦⑧ 覆盖（规划层拒绝 + adapter 层拒绝 + registry 记 warning 路径）。

**run1（`GM-1_20260911_215921`，修复前）**：plan.json 同样产出合法 db 查询（`:project` 命名参数风格）并取回 12 条 db 事实——两种参数风格均验证可用。

## 联跑（F1+F3+F4 同一轮全流程，run2 `GM-1_20260911_221132`）

- 命令：`python run_pipeline.py --full --type geology_demo_review --project GM-1 --period 2026H1 --intent "GM-1 钻探见矿与品位情况综述，重点说明钻探工程控制程度与化验品位成果"`（exit 0，约 6 分钟）；
- **F3 分档统计日志行**（新增的完成摘要）：
  `LLM 分档调用: fast档 13 次/113.3s（deepseek-ai/DeepSeek-V4-Flash）、quality档 19 次/244.9s（deepseek-flash）`
  ——抽取/规划走 fast 档（硅基流动）、大纲/分节/judge/修订走 quality 档（DeepSeek 官方），两档各自调用次数与耗时可见；
- **F4 直方图实跑**：`chart_chart_au_hist.png` 渲染成功且内嵌进 final.html（img naturalWidth>0，浏览器实测加载）；
- **F2 落地**：final.html 含 103 个附录锚点、12 个来源链接、1 行章节来源（本 run 模型可解析引用集中在 1 节）；
- judge 35 FAIL（structure 4 分：deepseek-flash 小标题超长倾向，与既有结论一致——judge≥36 线为 GLM 时代标定，现役供应商未达标属模型文风方差；对账/数据支撑/合规维度 8~9 分健康）。

## F2 溯源可点击 ✅（代码+重渲染静态核验；浏览器点击实测见收尾）

**改动**
- `render/html_report.py`（仅渲染层，不进提示词/对账/字数）：
  - 溯源附录每行加锚点 `id="src-{fact_id}"`；
  - 新增 `_sources_html`：text/views/table/risk 章节末尾追加小字行 `<p class="sec-src">本章数据来源：[编号链接] 名称=值（来源，截至 …）</p>`，编号链接指向附录锚点，至多 12 条，无引用章节不显示；
  - notes 参数改为全量 dict（表格说明的 cited_fact_ids 可用）。
- `run_pipeline.py`：html 渲染传全量 notes（docx 保持 notes_bodies 现状，不做跳转）。
- CSS：`.sec-src` 11px 灰字、锚点暗金色。

**验证证据**：意图版成品 `国内高纯石英意图版_20260911_203227` 重渲染
- 附录锚点 188 个、章节来源行 8 行、来源链接 46 个、悬空链接 0；
- 来源行样例：`本章数据来源：rag.01.00 2025年我国4N级及以上高纯石英需求预测=38.31万t（RAG:全球高纯石英资源开发利用现状及供需分析_张海啟 p.5，截至 2025年）；…`
- 浏览器实测（8765 服务器 + 内嵌浏览器）：页面加载后统计 188 锚点/46 链接/8 来源行一致；点击来源链接 `consum.电光源.share`，页面从 0 滚动到 22367px，附录对应行精确停在视口顶部（截图留证）。

## 联跑附带修复（回归中发现的历史缺陷）

1. **对账数字截断**（pipeline/reconcile.py `_NUM_RE`）：小数后紧跟单位字母（"0.97g/t"、"4.3m"）会被负前瞻回溯截断成 0/4——首轮联跑的两个 `0.0` 未匹配数字即源于此。修复：小数分支仅禁跟数字、允许字母；整数分支维持原禁集（编号碎片防护不变）。11 组用例（含 hp_quartz 场景的 52.81%/38.31万t/SiO2≥99.995%）逐一验证，test_reconcile/test_bindings/test_charts 全绿。
2. **table 章节不嵌图**（render/html_report.py）：table 分支漏掉 `_charts_html`（docx 本就全章节嵌图），geology 直方图挂在表格节时 HTML 缺图——已补。

## 回归（tier_roles 注释后 = 默认单模型行为）

- `tier_for('extract'/'write')` 均 None，默认档 = model 段（V4-Flash @ siliconflow）；
- `company_review --data-only`：exit 0，37 条事实，交叉校验 PASS；
- `geology_demo_review --data-only`：exit 0，65 条事实；
- tests/test_bindings.py（①~⑧）、test_reconcile.py、test_charts.py 全绿。

## 汇总

**交付**：F3 模型分档 → F1 数据库动态规划 → F2 溯源可点击 → F4 图表扩展，四项全部完成并各有验收证据；附带修复两个历史缺陷（对账数字截断、table 章节不嵌图）。

**最终配置态**：settings.yaml 保留 `model_tiers`（fast=V4-Flash 继承 model 段；quality=DeepSeek 官方 deepseek-flash），`tier_roles` 已注释——默认行为与改动前完全一致；需要分档时取消注释，或生成页/CLI 按次指定 `--model-tier`。

**耗时**：约 2.5 小时（含两轮 geology 意图全流程联跑各约 6~22 分钟、一轮 company/geology 数据层回归、三轮浏览器实测）。

**改动清单**：
- 后端：datalayer/settings.py、datalayer/planner.py、datalayer/adapters/database.py、pipeline/llm.py、pipeline/reconcile.py、run_pipeline.py、server/routes_runs.py、render/html_report.py、render/charts.py、template_factory/schema.py
- 前端：static/js/views/generate.js、static/index.html（R12）
- 配置/模板：config/settings.yaml、config/report_types/geology_demo_review/report.yaml（内嵌 Au 品位直方图）
- 测试/文档：tests/test_charts.py（新增）、tests/test_bindings.py（⑥⑦⑧）、API.md、优化进度.md

**验证产物**（artifacts/，gitignored）：
- `GM-1_20260911_221132`（run2：F1 对账 PASS + F3 分档日志 + F4 直方图内嵌）、`GM-1_20260911_215921`（run1）
- `artifacts/_f1_run2.log`（分档统计行）、`_reg_company.log`、`_reg_geology.log`（默认行为回归）
- 意图版重渲染 `国内高纯石英意图版_20260911_203227/final.html`（F2：188 锚点/46 链接，浏览器点击滚动实测）

**遗留问题**：
1. judge≥36 未在任何现役供应商达成（run2 35 分，structure 维度受 deepseek-flash 小标题超长拖累）——既有结论：阈值按模型校准或等 GLM 余额恢复，本轮未动；
2. server 同进程并发场景（preview 与生成同时跑）的 tier 覆盖为进程级单值，已用 try/finally + run 启动时清理收敛，极端交叉仍可能串档（单机单用户可接受）；
3. 反爬兜底页（百度文库等）依旧抓取失败记 warning——既有已知项，未在本次范围。

## F4 图表种类扩展 ✅（代码+单测；geology 实跑见联跑）

**改动**
- `template_factory/schema.py`：`ChartTemplate.type` 增加 `"scatter" | "hist"`；新增 `bins` 字段（hist 分箱数，缺省 10）；docstring 注明 scatter/hist 仅支持 table: 源。
- `render/charts.py`：`_draw_scatter`（x 数值列 + y 多序列，x/y 解析失败的点剔除，无可绘点报错）、`_draw_hist`（y 单数值列，matplotlib 分箱计数，有效值 <3 拒绝）；中文字体/配色/图注机制复用；`_save` 统一出图。
- `tests/test_charts.py`（新增）：合成表格（rows_to_table 形状）直方/散点各渲染 PNG 成功 + 两类边界拒绝用例。
- `config/report_types/geology_demo_review/report.yaml`：assay_table 章节内嵌 `chart_au_hist` 直方图（Au 品位分布，bins=8）。

**验证证据（tests/test_charts.py 全绿）**
- ① 直方图 PNG 20KB（25 个品位值/bins=8）；② 散点 PNG 39KB（x=深度数值列，y 双序列）；③ hist 多数值列拒绝；④ scatter x 列无数值拒绝。

## 补记：GLM-5.3 全流程横评（2026-09-11 深夜）

- settings.yaml 切到硅基流动 `zai-org/GLM-5.3` + `reasoning_effort: low`（实测该参数被接受，思考字数 394→28）；
- 全流程 10 分钟（53 次 LLM 调用 / 398s），facts 118 条，4 图 3 表 + 11 行章节来源 + 118 个溯源锚点；
- **judge 37 分（各供应商最高）**，五维 7/7/7/8/8 全部 >4；verdict 仍 FAIL 的唯一原因：metallogeny 节"400～600℃ 成岩温度"为模型背景知识、语料 809 片段中不存在，对账层按"数字必须有出处"拦截（设计行为，非 bug）；
- 产物：`artifacts/国内高纯石英_20260911_232116/`（final.html / final.docx）。

## 质量治理任务一：范文数字豁免与通识数字出口（2026-09-12）

**改动**
- pipeline/reconcile.py：新增 `_reference_numbers(spec)`（judge_reference 全文 + 全部章节/槽位 fewshot，用与正文同一提取管线 `_numbers_in` 抽数）与 `_match_ref`（同 0.005 容差）；check() 中事实索引匹配不上的数字先过豁免集，命中即放行并计 `numbers_ref_exempt`；reconcile_report 顶层新增 `n_ref_exempt` 汇总字段（不污染事实索引/cited_fact_ids/溯源附录）。
- 通识数字出口：`_GENKNOW_RE`（全角中括号〔...〕）在 `_numbers_in` 预处理剥除——标记内数字不参与对账；validate 新规则 `style.genknow_cap`（全篇超 3 处 fail，check_rules 写 genknow_cap_off 关闭）；写作/修订提示词注入标注规则；渲染层 `_clean_genknow` 把〔x〕替换为 x（html 全 body + docx 四处正文出口）。
- pipeline/sections.py：SYSTEM 模板新增 `{timeliness}` 段（`_TIMELINESS_TMPL`：当前日期 + 数据年代分布 + 时效规则 + 通识标注规则），`_system(spec, fewshot, doc)` 五处调用全部传 doc；revise.py 的 `_system/_revise_issue` 同步（六处调用传 doc=doc）。
- report.yaml writing_rules 追加两条（范文通识数值可复述/其他通识数值标注制；过期预测历史口径）。

**验证证据（tests/test_reconcile.py ①~⑥ 全绿）**
- ① fewshot 含 400～600℃ 的最小 spec + 不含该数字的 facts + 正文复述 → unknown_numbers=[]、n_ref_exempt=2、index_size 不变；② 编造的 1234.5 依旧被拦（豁免不过宽）；③ 〔450～580℃〕不参与对账；④ 〔〕4 处被 validate 判 fail（metric=4）；⑤ stale_forecast 5 用例（见任务二）；⑥ _data_cutoff 取最大年份/回退未注明、〔x〕→x 清洗。

## 质量治理任务二：报告时效性治理（2026-09-12）

**改动**
- pipeline/validate.py：新增 `_FUTURE_RE`/`_HISTORY_RE`/`_stale_forecasts`（将来时措辞+目标年份<当前年命中；句内有"此前/曾/据…预测/历史上/当年"豁免；目标年==当前年不拦；当前年动态取 datetime）→ 规则 `stale_forecast`（fail 级，check_rules 写 stale_forecast_off 关闭），bodies 覆盖 title/views/forecast/risks/texts/notes。
- 写作层时效感知：_TIMELINESS_TMPL 注入当前日期与事实年代分布一行摘要（`_era_line`），提示词要求过期预测改历史口径。
- 渲染层：html_report `_data_cutoff(doc)`（as_of 最大四位年份，回退"未注明"）→ html 页首 meta 行追加"数据截至 {年份}年（语料文献口径）"；docx 首部同步。
- report.yaml writing_rules 追加历史口径措辞规则。

**验证证据**
- validate 新规则单测 5 用例全过（test_reconcile.py ⑤）："预计到2025年…将达"→fail；"预计到2030年"→过；"此前研究预测到2025年将达"→过；"据2020年统计"→过；目标年==当前年→过。
- 总验收联跑（见下节）。

## 质量治理总验收联跑（2026-09-12）

命令：`python -u run_pipeline.py --full --type hp_quartz_review --project 国内高纯石英 --reuse-data 国内高纯石英_20260911_232116`
（结果回填）
**结果（artifacts/国内高纯石英_20260912_173412，GLM-5.3，35 次 LLM 调用/402s）**
- (a) 对账 unknown_numbers=0（PASS 无对不上账数字）；本轮模型主动用通识标注机制
  把成岩温度写成〔400～600℃〕——走的任务一"出口"路径，n_ref_exempt=0 属正确
  行为（豁免与标注双通道并存，模型选了标注）；reconcile 状态 warn 仅含
  heading_drift（修订改写小标题，设计内信号）；
- (b) 将来时+过期年份命中 0（_stale_forecasts 复核）；"此前预测"历史口径句式出现；
- (c) final.html 页首出现"数据截至 2025年（语料文献口径）｜报告日期 2026-09-12"；
- (d) judge 35 分，扣分项无"无法对账的数字"（structure 5 分系小标题超长的
  模型文风问题，与对账无关）；
- 附带加固：联跑首遇硅基流动 503（System is too busy）打断——pipeline/llm.py
  把 5xx 服务端过载纳入指数退避重试（_is_5xx，退避 2/4s 重试两次）后重跑成功。
- 三件套测试全绿（test_reconcile ①~⑥ / test_bindings / test_charts）。


---

## 对话式报告工作台 M1：树工件与手动编辑（2026-09-14）

按根目录《实现方案.md》实施 MVP 三阶段的 M1（树工件与手动编辑）。

### 改动
- **schema 扩展**（template_factory/schema.py）：Section 增 `data_needs`/`origin`；
  SpecV2 增 `style_card`/`genre`；TableTemplate 增 `facts_rows` 渲染器与
  `source_prefix`。旧 report.yaml 不受影响（新增字段全带缺省值）。
- **trees/ 包**：store.py（config/trees/<id>/tree.yaml 含 tree: 元信息块含 subject；
  versions/v<N>.yaml 快照；ops_log.jsonl 统一操作日志 actor=system|manual|agent|rollback；
  指纹 sha256[:12]；回滚=恢复快照+版本+1；plans/ 数据计划存取）；lint.py（schema
  错误拦保存 + 软警告：缺 data_needs/图表缺表源/facts_rows 缺前缀/缺 heading）。
- **facts_rows 渲染器**（pipeline/sections.py）：按事实 id 前缀把标量事实成表
  （指标/数值/截至），树场景无绑定也能出用户点名的表。
- **run_pipeline.py**：新增 `--tree <id>`（跳过 type_exists；合成 sources 无静态绑定；
  subject 作 run_params.project 与 run_dir 命名）与 `--plan`（沿用树 plans/ 计划）；
  meta.json 增记 tree_id/tree_version/tree_fingerprint/style_card。
- **server**：routes_trees.py（列表/创建/详情/lint/保存/版本/回滚/日志/plans 列表/
  plan-start 后台任务）；routes_runs.py 增 POST /api/runs/from_tree；bus.sse_response
  抽公用（routes_runs events 改用）。
- **judge 无范文分支**（pipeline/judge.py，冒烟暴露后前置实现）：树运行无
  judge_reference 时，评分基准改为"各节 brief 覆盖度 + 行文规则 + 数据引用"，
  提示词注入评审基准块与各节写作要求；有范文路径行为不变。
- **前端**（static/js/views/trees.js 全新 + app.js 导航 + index.php 无关）：模板工作台
  ——树列表/新建/编辑器（节增删排序、逐节 brief/data_needs/charts/view_slots/字数区间、
  表格模板编辑、origin 徽标、lint 展示）/保存版本/版本回滚/操作日志/生成入口
  （意图+资料夹+计划下拉+SSE 进度+预览+docx）；深链 #/trees?tree=<id>；静态版本 R19。

### 修复的关键问题
1. judge.py 原 `spec 缺少 judge_reference` 直接 raise——树运行必炸，加无范文分支
   （评审基准重写，详见上）。
2. 前端 vue.prod 运行时编译器会把 v-for 卡片内作为结构元素的 `<template v-if>`
   连同后续兄弟节点丢弃（"+ 图"按钮/views 编辑区/表格节输入/生成结果块全不渲染）
   ——全部改为 `<div v-if>` 包装（R15~R18 修复）。
3. routes_runs.from_tree 与 routes_trees.plan_start 原为同步 def——FastAPI 线程池中
   无 running loop，`asyncio.get_running_loop()` 抛 RuntimeError——改 async def。

### 验证证据
- 单测：tests/test_tree_store.py（生命周期/校验拒绝/计划加载/lint/schema 兼容）、
  test_facts_rows.py（3 用例）、test_judge_noreference.py（无范文可跑 + 有范文路径
  不变）；pytest 全套 10 passed（含既有 test_reconcile/test_bindings/test_charts 不回归）。
- 冒烟①：scripts/make_smoke_trees.py 建两棵最小树 + 离线 rag 计划，
  `python run_pipeline.py --tree smoke_text --plan smoke --full` → judge 44 PASS；
  `--tree smoke_mix --plan smoke --full` → 全阶段跑通，facts_rows 表 6 行 + 图表
  chart_chart_smoke.png + 溯源附录 6 行均入 final.html。
- 冒烟②（网页端）：浏览器自动化走模板工作台——新建 manual_demo → 编辑器加图表
  （chart_market, facts:rag）与表格节（facts_key/facts_rows/rag）→ 保存 v2（0 错误
  4 提醒，指纹 47adc723a44d）→ 生成报告页签填资料文件夹（语料缓存 809 片段复用）
  → 生成（rag 3/web 2/db 1，97 条事实）→ judge 37 PASS → 页内预览 final.html +
  下载 docx。产物 artifacts/国内高纯石英_20260914_205055，meta.json 记
  tree=manual_demo v2 fp=47adc723a44d（树版本可追溯）。


---

## 对话式报告工作台 M2：对话生成树 + 文风卡 + 缺口回执（2026-09-14）

### 改动
- **三张内置文风卡** config/style_cards/（journal_paper 期刊论文体 /
  industry_research 券商研报体 / exec_brief 决策简报体）：规则文字 + 2 段自写
  百字节选（不抄第三方出版物）。加载器 pipeline/stylecards.py（list/load/apply）；
  注入点 sections._system 与 revise._system：rules 进 system prompt【文风】块，
  节选接在 fewshot 解析链兜底位（章节/槽位无范文时顶上）；无文风卡路径零变化。
- **tree_agent.py**：generate（需求对话→树，糊需求先反问至多 3 问，产出过
  SpecV2 校验；LLM 节对象统一归一化 _clean_section）；edit（每轮读当前树（D5），
  NL→受限操作集 set_meta/add_section/remove_section/update_section/move_section/
  add_chart/remove_chart，_apply_ops 纯函数应用后 save_tree(actor=agent)）。
  注意 _EDIT_SYSTEM 含 JSON 花括号示例，不能用 str.format，用 replace 占位符。
- **planner.py**：make_plan 增 needs 参数（树各节 data_needs 汇总），输出增
  needs_coverage 三态（covered/search/gap；_clamp_coverage 保逐条覆盖、非法
  status→gap；规则兜底全 gap 交用户裁决）；needs 存在时不再强制要求 intent/folder。
- **trees/store.py**：apply_decisions（缺口四选一落效：search→追加 web 查询；
  provide_folder→记 needs_folder；qualitative→所在节摘需求+brief 追加定性声明；
  drop→摘需求）+ coverage_gate（无未裁决 gap；全定性需二次确认）。
- **server**：POST /api/trees/generate、/{id}/chat、/{id}/plan/confirm、
  GET /{id}/plans/{name}、GET /style-cards/all；plan/start 传树 needs。
- **前端**（trees.js R20+）：对话卡常驻右侧顶部（未选中树=生成模式，选中=编辑
  模式，聊天记录页内呈现）；生成页签增计划面板（①出数据计划→三态列表+缺口
  四选一 radio+②确认裁决→自动选中该计划）。修复：新建树中文名 slug 空导致
  id="tree"（slug 兜底 tree_<ts>）；plan 下拉选中即载入该计划回执。

### 验证证据
- 单测：tests/test_tree_agent.py（生成过校验/反问/空树拒绝/编辑 ops 应用/D5
  actor=agent 同日志/no-op 不改版本）、test_gap_receipt.py（coverage 清洗/四选一
  各路径落效/gate 三态）——全套 pytest 22 passed。
- 网页端（萤石树）：对话输入完整需求 → 生成「萤石勘查开发综述结构」v1（6 节，
  含摘要/关键词惯用构件）→ 对话改「改名+加提纯技术进展节」v2 → 手动改节名 v3
  （编辑器）→ 对话用新节名加「环保约束分析」v5（agent 读到手动改动，D5 闭环）
  → 出数据计划（rag 3/web 2/db 1，coverage 三态全部判定）→ 造 gap 夹具经页面
  下拉载入 → 两个 ❌ 分别裁决 去搜/定性写 → 确认（徽标翻绿，web 查询追加、
  政策节摘需求+定性声明入树 v6）→ 生成沿用 gap_fixture.json（rag 5/web 7）
  → judge 37 PASS，final.html + docx 落盘（artifacts/国内萤石（氟化工原料）资源
  勘查与开发动态_20260914_212715）。

---

## 对话式报告工作台 M3：反馈回路（2026-09-14）

### 改动
- **pipeline/feedback.py**：parse（路由 agent：树摘要+带锚点全文+意见原文+台账
  → ops[target/kind/action/instruction]+ambiguities，target 只允许页面锚点集）；
  apply（三类分发：style→伪 judge_report 喂 revise.apply；data→追加 web/rag 查询
  重取数（同 id 覆盖、其余续号）+命中节带「旧引用+新事实」重写；structure→树操作
  透传 tree_agent._apply_ops+脏区追踪：新增/更新节重生成、删除节剔除、纯移动不重写，
  texts 按树序重排）；每轮 reconcile+validate 全量重跑+图表/HTML/DOCX 重渲染；
  轮次快照 rounds/<n>/（轮前 prev_* + diff.json 按节新旧对照 + ops/reconcile/
  validate）+ feedback_ledger.jsonl 台账；rollback 恢复轮前快照并重渲染；
  judge_deep 按需全量评审写 judge_report.json。
- **server**（routes_runs.py）：POST /{dir}/feedback/parse、/apply（start_job+SSE）、
  /feedback/rollback、/judge；GET /feedback/rounds、/feedback/diff、
  /feedback/{job}/events。
- **前端**（feedback.js 全新，#/run?dir=…；generate.js 历史行与生成完成卡加
  「反馈」/「反馈迭代」入口）：报告按节锚点视图 / 总意见框 / 解析→ops 回显勾选
  （执行按钮带计数）/「直接执行」开关默认关（先确认）/ SSE 进度 / 轮次列表 +
  左右两栏 diff 弹窗 + 回滚（页内 naive-ui 对话框二次确认，替代系统原生 confirm）
  / 深度评审按钮（judge 分数回显）。静态版本 R22。

### 修复的关键问题
1. feedback._run_structure_ops 动词词汇与 _apply_ops 不一致（add vs add_section）
   ——路由器直接输出树操作动词透传。
2. _ROUTER_SYSTEM/_EDIT_SYSTEM 内含 JSON 花括号示例，str.format KeyError——改
   replace 占位符。
3. 路由给中文节 id 时 _apply_ops 改写 id 导致重生成找不到节——feedback 按标题
   兜底定位；add_section 节对象先过 _clean_section 归一化（data_needs 字符串等
   形态不合规不再炸保存）。
4. _next_round 轮号碰撞（回滚条目占台账号不建目录）——取台账轮号与目录数的最大值+1。

### 验证证据
- 单测：tests/test_feedback.py（路由清洗/style 轮改写+未点名节逐字不变+diff 落盘/
  回滚恢复/结构轮 after 插入+树同步+D5/中文 id 标题兜底/深度评审写报告）——
  pytest 22 passed。
- 联跑（萤石运行，网页反馈工作台操作）：
  - 第 1 轮（style+data 两类意见）："市场供需节改写充实+补 2024 价格走势" →
    文风改写 1 处；market_supply_demand 带新数据重写（web 3 条追加取数），
    reconcile warn，rounds/1/diff.json 落盘，未点名节逐字不变。
  - 第 2 轮（structure 加节）→ 轮次落盘后回滚（页内对话框确认）→ 第 3 条台账
    rolled_back=True，稿面恢复第 1 轮后状态。
  - 第 4 轮（structure 扩写既有节）→ environmental_constraints 405→555 字，
    diff 落盘。
  - 第 5 轮（structure 删节+加节）→ keywords 删除（381→0）、
    logistics_constraints 新增（0→300 字），texts 按树序重排，reconcile warn。
  - 深度评审按钮 → judge 27 分落 judge_report.json（结构多次调整后评分回落，
    属预期，按需评审仅作参考）。


---

## 对话式报告工作台 MVP · 总验收（2026-09-14）

按《实现方案.md》§11 执行，逐项证据汇总于《交付报告.md》。

### A. 网页端七步全流程
萤石树全流程在浏览器自动化下实际走通（需求→对话生成树→对话改+手动改→缺口
三态+❌ 裁决→生成 SSE→反馈两类意见走确认→diff+docx），逐步操作与页面状态
记录见《交付报告.md》§二；产物
artifacts/国内萤石（氟化工原料）资源勘查与开发动态_20260914_212715。

### B. 情况矩阵 12 项
全部有证据，逐项映射见《交付报告.md》§三（自动测试 + 页面演示记录）。

### C. 质量门
- pytest 全套 22 passed（既有三件套不改仍绿 + 新增六件全过）。
- 旧类型回归：`--full --type hp_quartz_review --project 国内高纯石英
  --reuse-data 国内高纯石英_20260911_232116` → 全阶段跑通，judge 38（fail 原因
  为观点节字数超限的模型文风问题，非框架回归），对账 unknown=0、
  n_ref_exempt=1、final.html 含数据截至行与溯源附录、docx 落盘。
  产物 artifacts/国内高纯石英_20260914_222108。

### D. 交付报告与复验剧本
见《交付报告.md》（含 §五 用户复验剧本七步，供用户网页端照做复验）。
