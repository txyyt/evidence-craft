# EvidenceCraft 对话式报告工作台 · 实现方案

> 一句话目标：把"配置驱动的报告生成器"升级为"对话式工作台"——用户手里没有范文，
> 从一段需求描述出发，对话生成结构树，网页端确认数据计划后一键生成，
> 生成后用一段总意见驱动系统定向迭代，全程数字有出处、治理不旁路。
>
> 本期原则：**先打通总流程**，质量问题后续打磨（M4 及以后）。

---

## 1 背景

现有系统以"报告类型"为中心：`config/report_types/<id>/` 里的 report.yaml（Spec v2，
从范文提取）+ sources.yaml（数据绑定）。两个前提在真实用户面前不成立：

1. **用户没有范文**。每次报告的主题、结构都不同，结构树必须根据用户需求生成，
   而不是从范文提取；
2. **没有范文就没有 judge 对照锚点**，且用户需要自己给修改意见让系统迭代，
   而不是只接受系统内部评审的结果。

同时必须保留系统的立身之本：**数字必有出处（抽取即对账）、规则校验、溯源附录**。
灵活的是结构来源和修改入口，不是质量底线。

## 2 总体形态：用户旅程（全部在网页端完成）

```
①需求描述 ──→ ②树生成agent出树 ──→ ③树验收与修改（对话/手动）──→ ④数据计划+缺口回执
                                                                      │
⑦定稿入库 ←── ⑥反馈回合（总意见→路由→确认→定向执行→diff）←── ⑤生成（原流水线不动）
```

1. **需求描述**：用户一段话给出详细内容清单（"包括引言、市场供需、勘查现状、
   问题与建议；供需那节要有消费结构表，现状那节要有产能图"）。用户输入即 grounding，
   不依赖结构预设库；agent 从需求推断"形态"（期刊论文体/研报体/简报体），
   只影响惯用构件（论文体有摘要+关键词、研报体有核心观点+风险提示），不让用户多选一项。
   需求太糊时 agent 反问（最多 2~3 个问题），不硬编。
2. **树生成**：用户点名的章节照办，没说的由 agent 补全（编号、每节长度、title_style、
   每节 data_needs、writing_rules），每节标 `origin: user|agent`，验收时重点看 agent 补的。
3. **树验收与修改**，两种方式：
   - 对话改（"xxx 部分里的 xxx 改成 xxx"）→ 编辑 agent 解析意图 → 树操作 → **自动应用**，
     页面即时显示改了什么、一键撤销（便宜操作不确认）；
   - 手动改：树编辑器（增删节、上移/下移、编辑每节 brief/data_needs/charts）。
   - 两种方式写**同一份操作日志**、共用版本号；agent 每轮读树的当前值，
     手动改过的内容它看得见，两边不打架。每次改动自动跑树 linter。
4. **数据计划与缺口回执**（硬步骤）：树确认后，data_needs 汇总给 planner 出绑定计划
   （本地语料检索什么、联网搜什么、查哪些库），**用户点名的表/图逐项回执**：
   有来源 ✅ / 需联网搜 ⚠️ / 无来源 ❌。缺口让用户当场四选一：去搜 / 我提供资料 /
   本节定性写 / 砍掉。生成**之前**解决，不生成后返工。
5. **生成**：原流水线不动（取数 → 抽取即对账 → 大纲 → 分节 → 校验 → judge → 渲染），
   树替换 report.yaml 的位置。
6. **反馈回合**：用户写一段总意见 → 路由 agent 出结构化 ops → **回显确认（默认开，
   可关）** → 三类分发执行 → reconcile+validate 自动重跑 → 按节 diff → 下一轮。
   支持回滚到上一轮；judge 为按需"深度评审"按钮。
7. **定稿**：报告 + 树 + 反馈台账一起入库；同一棵树可派生复用。

## 3 已拍板的设计决策（讨论定稿，实现时不再摇摆）

| # | 决策 |
|---|---|
| D1 | 反馈分发 agent 是**修改入口**，不是评审替代：每轮意见执行后 reconcile+validate 自动重跑；judge 按需触发 |
| D2 | 树每节带 **data_needs**；缺口回执是生成前的硬步骤，不可跳过 |
| D3 | 文风卡 = **规则文字 + 2~3 段节选**（纯文字不发）；内置 3 张；注入优先级：**用户反馈 > 文风卡 > 默认写作规范** |
| D4 | 交互成本不对称：树对话改动**自动应用+可撤销**；报告重生成**先回显确认**（默认开，熟练可关） |
| D5 | **统一操作日志 + agent 每轮读现树**（对话改与手动改共用版本链） |
| D6 | "形态"（论文体/研报体/简报体）由 agent 从需求推断，不让用户选 |
| D7 | MVP = M1+M2+M3（打通总流程）；文风蒸馏工具、风格飞轮、增量 judge → M4 |
| D8 | 不动现有报告类型入口（兼容保留）；不动 settings.yaml 的 model 段 |

## 4 MVP 范围与非目标

**做**：树工件与存储、树生成/编辑 agent、数据计划与缺口回执、3 张内置文风卡、
`--tree` 生成路径、反馈回路（路由/确认/三类执行/diff/回滚）、两个前端工作台。

**不做（M4 及以后）**：文风蒸馏工具（用户样本→文风卡）、定稿报告回填 fewshot 的
风格飞轮、增量 judge（本期 judge 只提供按需全量评审）、树编辑器拖拽排序（用上移/下移按钮）、
树的分享/市场、多用户与权限、联网搜索付费 API 接入、planner 时效补查。

## 5 数据形态

### 5.1 结构树 tree.yaml

树**就是** Spec v2 结构加扩展字段——`template_factory/schema.py` 的 pydantic 模型
默认忽略未知键、新字段全部带默认值，旧 report.yaml 完全不受影响。扩展：

- `Section` 增加 `data_needs: list[str]`（语义数据需求，与 ViewSlot 已有字段同名同义）、
  `origin: "user"|"agent"`（缺省 agent）；
- `SpecV2` 顶层增加 `style_card: str | None`（文风卡 id）、`genre: str | None`（形态）；
- 文件内额外携带 `tree:` 元信息块（SpecV2 加载时忽略，树存储层解析）。

```yaml
tree:                       # 树存储层元信息（load_spec 忽略）
  id: hp_quartz_v2
  name: 高纯石英动态综述
  subject: 高纯石英        # 报告主体（agent 从需求推断）→ 生成时作 run_params.project
  status: draft            # draft | confirmed
  created_at: "2026-09-14T10:00:00"
  provenance: {kind: dialogue, conversation_id: "..."}   # 创建来源，派生时记父树

# ↓↓↓ 以下即 Spec v2 字段（sections[].style 就是 brief）↓↓↓
report_type: hp_quartz_v2
version: 2
description: ...
writer_role: 资深行业研究员
title_style: |
  ...
genre: journal            # 形态：journal|research|brief（agent 推断）
style_card: journal_paper # 文风卡 id（config/style_cards/）
writing_rules: [...]
check_rules: [...]
sections:
  - id: market_supply
    title: 市场供需
    heading: "1　市场供需"
    kind: text
    style: |              # brief：写什么、多长（由用户语言翻译而来）
      综述需求与消费结构……写 2~3 个自然段。
    data_needs: [市场供需数据, 消费结构占比]
    origin: user          # 用户点名；agent 补的节为 agent
    charts:
      - {id: chart_consum, title: 应用消费结构, type: pie, source: "table:consum_table"}
    check: {body_len: [300, 900]}
```

### 5.2 树存储与版本

```
config/trees/<tree_id>/
  tree.yaml            # 当前版（含 tree: 元信息块）
  versions/v<N>.yaml   # 每次保存（对话改/手动改/回滚）快照，N 单调递增
  ops_log.jsonl        # 统一操作日志：{ts, actor: agent|manual|rollback|system,
                       #   op: {...}, version_after, summary}
  plans/<ts>.json      # 数据计划（含缺口回执与用户裁决，状态 pending/confirmed）
config/style_cards/*.yaml
```

指纹沿用 `template_fingerprint` 模式（sha256 前 12 位），报告 meta.json 记
`tree_id / tree_version / tree_fingerprint`，保证可追溯。回滚 = 恢复某版本快照 +
记一条 rollback 操作。

### 5.3 文风卡

```yaml
id: journal_paper
name: 期刊论文体
desc: 学术综述形态：客观陈述、数据先行、段落充实
rules: |                # 注入 system prompt 的规则块
  概括陈述为主，不用口语和感叹；每段一个论点，段首给判断；
  数字带年份与出处口径；不使用"综上所述"式套话开头……
excerpts:               # 2~3 段百字级节选，让规则落地（自写示例，不抄第三方出版物）
  - {kind: text, text: "……"}
  - {kind: table_note, text: "……"}
source: built-in        # built-in（本期只做 built-in；distilled/user 留 M4）
```

内置三张（本期由实现者撰写）：`journal_paper` 期刊论文体、`industry_research` 券商研报体、
`exec_brief` 决策简报体。注入方式：`pipeline/sections.py` 的 SYSTEM 模板在写作规范前
插入文风卡 rules + 对应 kind 的节选（fewshot 解析链不变：节内 fewshot 优先，
文风节选作为全局兜底层——即树上无 fewshot 时文风节接替其位置）。

### 5.4 数据计划与缺口回执

复用 `datalayer/planner.py`（`make_plan` / `plan_to_bindings`），扩展两点：

1. 输入从"意图一段话"改为"树 + data_needs 汇总"（intent 参数传
   `description + 各节 data_needs 摘要`，`spec` 直接传树加载出的 SpecV2）；
2. plan 输出增加 `needs_coverage`（planner 的 LLM 输出 schema 扩展一字段）：

```json
{
  "mode": "llm",
  "rag": ["..."], "web": ["..."], "db": [], "tables_kept": [],
  "needs_coverage": [
    {"need": "消费结构占比", "section_id": "market_supply", "status": "covered",
     "queries": ["rag:..."]},
    {"need": "产能数据", "section_id": "status", "status": "search",
     "queries": ["web:..."]},
    {"need": "价格走势", "section_id": "market_supply", "status": "gap"}
  ],
  "warnings": []
}
```

缺口四选一的裁决记入 plan 并落效果：`web_search`（把追加查询并入 web 列表）、
`provide_folder`（走 `ensure_corpus` 现场建语料库，已有机制）、`qualitative`
（该 need 所在节 brief 追加"本节定性论述，不引用数字"）、`drop`（删对应图表或 need）。
**确认门槛**：不存在未裁决的 gap（每个 ❌ 都要有四选一结果）；若全树没有任何
数据来源（covered/search/provide_folder 皆无），须用户二次确认"全定性生成"
（各节 brief 已标注不引数字，对账层天然兜底），否则阻止——显式缺口，宁可拦不可编。
planner LLM 失败的规则兜底对树场景无静态绑定可沿用 → 兜底即"全部标 gap 交给用户裁决"。

**用户点名表/图的数据通路**（无静态绑定时的支持面，缺口回执按此判定）：
图已支持 `facts:<id前缀>` 源（bar/line/pie 直接吃标量事实）✅；表现有 renderer
只有 consensus_pe（股票）与 generic_rows（需 xlsx/sqlite 绑定产出
collections.tables），**不支持从标量事实组表**——M1 新增确定性 renderer
`facts_rows`（按 id 前缀把事实组成行列表格，LLM 不碰数字）；xlsx/sqlite 绑定源 ✅；
web 页面内嵌表格抽取不承诺（按 gap 四选一裁决）。

### 5.5 反馈 op 协议与台账

路由 agent 输入：树摘要 + 带锚点全文（`sec:<id>` / `view:<slot_id>` / `table:<tid>` /
`chart:<cid>` / `title` / `global`，段落编号）+ 意见原文 + 台账摘要。输出：

```json
{"ops": [
  {"target": "sec:market_supply", "kind": "style",     "action": "rewrite",
   "instruction": "消费占比改为与表一致，段落合并"},
  {"target": "sec:status",        "kind": "data",      "action": "refetch",
   "instruction": "补 2025 年产能数据"},
  {"target": "sec:policy",        "kind": "structure", "action": "add",
   "instruction": "新增一节'政策进展'，简述矿种列入与矿业政策"}
], "ambiguities": ["'结尾太仓促'未指明章节，已按 global 处理"]}
```

- `kind: style` → 构造伪 judge_report `{issues: [{target, problem, instruction}]}`
  喂给现有 `revise.apply`（title/forecast/table_note/risk/text 模板原样复用）；
- `kind: data` → planner 追加查询 → `run_data_layer(bindings_override=...)` →
  新事实并入 facts.json（同 id 以新内容覆盖=口径更新，其余 id 续号，引用不悬空）
  → 命中节带新事实重写；
- `kind: structure` → 转树操作（add/remove/reorder/re-brief）→ 树版本+1 → **脏区追踪**：
  只有受影响节重生成，未点名节逐字保留（add=新节、re-brief=该节、reorder=仅重渲染、
  views 槽位变化=该节观点重规划）。

每轮执行后：reconcile + validate 自动全量重跑；重新渲染 final.html/docx 与图表；
按节 diff（旧/新文本）随轮次落盘。台账 `feedback_ledger.jsonl`（每个 run_dir 一份）：
`{round, ts, text, ops, exec_summary, reconcile, validate, rolled_back}`。

## 6 后端实现

### 6.1 trees/ 包（新）

- `trees/store.py`：CRUD、版本快照、指纹、统一操作日志、回滚；读写 tree.yaml
  （`SpecV2.model_validate` 前剥离 `tree:` 块）。
- `trees/lint.py`：确定性检查——Spec v2 schema 合法性；软警告：节无 data_needs、
  用户点名 charts 无对应 plan 来源、views 节缺槽位、headding 编号不连续。lint 结果
  随保存返回前端展示。

### 6.2 树 agent（pipeline/tree_agent.py，新）

- `generate(messages) -> {kind: "tree", tree} | {kind: "questions", questions}`：
  首轮生成（需求太糊先反问）；产出必须过 SpecV2 校验 + lint 才落盘。
- `edit(tree_id, messages) -> {ops, tree_after}`：读**当前**树（D5），NL → 树操作，
  自动应用 + 版本 +1 + 记日志；返回前端用于展示"改了什么"。
- 同一台"NL → 结构化 ops"框架，与反馈路由 agent 共用提示词模式。
- LLM 失败/输出不合规：生成轮返回错误让用户重试；编辑轮不改树（树永远可读）。

### 6.3 生成管线接入（run_pipeline.py，改）

新增参数：`--tree <tree_id>`（spec 从树加载；**跳过 type_exists 校验**——现有
main() 一进来就校验报告类型存在，树路径必须绕开；type sources 用合成 dict
`{"name": 树名, "features": {}, "bindings": []}`；subject 取树元信息作
run_params.project，run_dir 以其命名）、`--plan <plans/xxx.json>`
（confirmed 计划 → `bindings_override=plan_to_bindings(plan)`）。

- M1 阶段先允许 `--tree` 与现有 `--intent/--folder`（planner 意图模式）组合，
  缺口回执在 M2 加上后再要求必须走 confirmed 计划；
- meta.json 增记 `tree_id / tree_version / tree_fingerprint / style_card`；
- 现有 `--type/--spec` 路径零改动。

### 6.4 反馈回路（pipeline/feedback.py，新）

- `parse(run_dir, text)`：路由 agent（一次 LLM 调用，同步返回 ops，不执行）；
- `apply(run_dir, ops, progress, cancel_event)`：三类分发执行 → reconcile+validate
  重跑 → 重渲染 → 轮次快照（`rounds/<n>/`：sections/outline/facts 副本 + diff.json）
  → 台账追加。挂 `server/bus.py start_job`（SSE 复用现有事件协议，stage 用
  `feedback`）；
- `rollback(run_dir, round)`：恢复上一轮快照 + 重渲染 + 台账标记；
- 按需深度评审：`judge.run` 全量跑一遍，结果写 `judge_report.json`（增量 judge
  留 M4）。**树运行没有 judge_reference**：judge 提示词增加无范文分支——评分依据
  改为"章节 brief 覆盖度 + writing_rules 遵守 + 数据引用规范"；M3 实现时先验证
  `judge.run` 在 reference 为 None 时能跑，不能则加 rubric 分支。

### 6.5 API 一览（server/，新增 routes_trees.py；routes_runs.py 增量）

| 端点 | 方式 | 说明 |
|---|---|---|
| `/api/trees` | GET | 树列表（版本/指纹/状态/时间） |
| `/api/trees/generate` | POST | 首轮生成（start_job SSE，长任务） |
| `/api/trees/{id}` | GET/PUT | 详情（含 lint）/ 手动保存（版本+日志） |
| `/api/trees/{id}/chat` | POST | 编辑对话（同步单次 LLM，自动应用） |
| `/api/trees/{id}/versions`、`/rollback` | GET/POST | 版本链 / 回滚 |
| `/api/trees/{id}/plan/start`、`/confirm` | POST | 出计划（start_job）/ 缺口裁决并确认 |
| `/api/style_cards` | GET | 文风卡列表（含节选预览） |
| `/api/runs/from_tree` | POST | `{tree_id, run_params}` → start_run |
| `/api/runs/{dir}/feedback/parse`、`/apply` | POST | 路由（同步）/ 执行（start_job SSE） |
| `/api/runs/{dir}/feedback/rounds`、`/rollback` | GET/POST | 轮次与 diff / 回滚 |
| `/api/runs/{dir}/judge` | POST | 按需深度评审（start_job） |

### 6.6 LLM 调用约定

全部走现有 `pipeline/llm.py chat_json`（503 重试加固已具备）与 tier 机制；
不新增供应商配置，不动 settings.yaml 的 model 段。树 agent / 反馈路由建议走
fast 档，改写/生成沿用现有 tier 分工。

## 7 前端实现（免构建约束：CDN Vue3 + 原生 JS 模块，不引入 npm）

新增/改动 `static/js/views/`：

- **trees.js · 模板工作台**：左侧对话面板（生成/编辑/反问流转），右侧树编辑器
  （章节列表：上移/下移/增删、逐节编辑 brief/data_needs/charts/origin 徽标，
  agent 补的节高亮）、文风卡下拉、lint 结果条、"改了什么"提示条 + 一键撤销（回滚版本）。
- **计划面板**（模板工作台第三栏/抽屉）：needs_coverage 三态列表（✅⚠️❌），
  缺口项四选一裁决控件，确认后出现"生成报告"按钮。
- **feedback.js · 反馈工作台**（挂在现有 run 详情页）：报告结构化视图（按节锚点）、
  总意见输入框、"解析意见"→ ops 回显（可勾选剔除）→"执行"（SSE 进度）、
  轮次列表 + 按节 diff（左旧右新两栏）、回滚按钮、深度评审按钮；
  "直接执行"开关（**默认关**=先确认）。
- `app.js` 注册路由、`api.js` 加端点、`?v=` 缓存版本号机制沿用（R12 起递增）。

## 8 治理延续（不可旁路的底线）

1. 树再自由，生成仍走"抽取即对账"：正文数字必须命中事实索引 / 范文豁免集（若有
   fewshot）/〔〕通识出口（至多 3 处）；
2. 每轮反馈执行后 reconcile + validate 自动重跑，结果随 diff 展示；
3. 用户意见与治理冲突时（要求写无出处数字）：系统明说"没有该数据，可去搜/你提供"，
   不沉默编造；qualitative 节由对账天然兜底（无事实即不得有数字）；
4. 溯源附录、数据截至声明（as_of）、stale_forecast 等已落地规则全部沿用。

## 9 实施顺序与阶段验收

| 阶段 | 内容 | 阶段验收 |
|---|---|---|
| M1 地基 | trees 存储/版本/日志/lint + 手动树编辑器 + `facts_rows` 表 renderer + `--tree`（配 `--intent/--folder`，页面提供意图/资料文件夹输入）跑通生成 + `/api/runs/from_tree` | ① 两棵最小树冒烟：纯 text 节树、含 views+table+risk 树各跑通 outline→sections→render（暴露并修复 outline/渲染对特定 kind 的隐含依赖）；② 手工建一棵含表/图的树 → lint 通过 → 网页选择树生成出 final.html/docx |
| M2 对话与文风 | 树生成/编辑 agent（含反问）+ 缺口回执四选一 + 三张内置文风卡 + 计划面板 | 纯对话从需求到生成全程网页完成，含一次对话改树 + 一次手动改树 + 一个缺口裁决 |
| M3 反馈回路 | 路由 agent + 三类执行 + 脏区重生成 + diff/轮次/回滚 + 深度评审 + 反馈工作台 | 三类意见各一轮全部生效，未点名节逐字不变，回滚可用 |

每阶段完成即自测（见 §10），三阶段全过后进入 §11 总验收。

**风险与对策**：前端工作量是后端两倍以上（树编辑器+diff 为大头），排期按此估；
树冷启动无 fewshot 文风平庸 → 文风卡顶住，属已接受的 MVP 代价；反馈轮延迟靠脏区
追踪压到分钟级；planner 失败兜底为"全 gap 交用户裁决"，系统永远可继续。

## 10 测试计划

**单元测试（mock LLM，不依赖真实接口）**：

- `tests/test_tree_store.py`：保存/加载/版本递增/指纹稳定/回滚；对话改与手动改
  写同一日志（D5 的机器证明）；
- `tests/test_tree_lint.py`：schema 违规报错；无 data_needs、图表无来源、views 缺槽位
  等软警告各触发；
- `tests/test_tree_agent.py`：mock 生成 → SpecV2 校验通过；糊需求返回 questions；
  mock 编辑 NL → 树 ops 正确应用且版本+1；
- `tests/test_gap_receipt.py`：needs_coverage 三态分类；四选一裁决各自落效
  （web 并入查询 / folder 建库标记 / qualitative 改 brief / drop 删图表）；
  全 gap 时 confirm 被阻止；
- `tests/test_feedback_router.py`：mock → 三类 ops 协议合法；歧义升 global；
- `tests/test_feedback_round.py`：mini run_dir——style 轮后文本变化且 reconcile 重跑
  unknown=0；structure 轮后未点名节逐字不变（脏区证据）；回滚恢复原文；
- `tests/test_facts_rows.py`：事实组表 renderer（列名/单位/空前缀不崩）；
- judge 无范文分支冒烟（mock：reference=None 可跑，rubric 走树依据）；
- 兼容回归：现有 `test_reconcile / test_bindings / test_charts` 等全部不改仍绿；
  旧 report.yaml 加载不受新字段影响。

**联跑场景（真实模型，当前配置 zai-org/GLM-5.3）**：M1/M2/M3 各阶段验收各跑一次；
总验收跑 §11 的全流程。

## 11 总验收要求（最终验收线，全部满足才算通过）

**A. 网页端全流程一遍过**（用户亲自或按剧本在页面操作，全程不碰命令行）：

1. 进入模板工作台，输入一段详细需求（明确含"xx 节要有 xx 表、xx 节要有 xx 图"）；
2. 对话生成树；验收树时完成**一次对话修改 + 一次手动修改**，页面可见"改了什么"
   且可撤销；lint 无红色错误；
3. 发起数据计划：缺口回执出现三态，用户对至少一个 ❌ 缺口做出裁决（四选一任一），
   确认计划；
4. 点击生成：SSE 进度可见，生成完成自动打开报告页（final.html）；
5. 进入反馈工作台，写一段总意见，**至少覆盖两类**（如一条文风类 + 一条数据类或
   结构类）：确认框默认出现（D4），核对 ops 后执行；SSE 进度可见；
6. 查看按节 diff；下载 final.docx 可正常打开；数据截至行、溯源附录完整；
7. 全程正文数字对账通过（unknown=0，或命中〔〕通识出口且 ≤3 处）。

**B. 情况矩阵全部有证据**（自动测试通过截图/产物即可）：

| 情况 | 证据来源 |
|---|---|
| 糊需求 → agent 反问 | test_tree_agent + 页面演示 |
| 手动改后对话改，agent 看得到手动改动 | test_tree_store + 页面演示 |
| 树版本回滚 | test_tree_store + 页面演示 |
| 缺口四选一各路径 | test_gap_receipt（四路径各一用例）+ 联跑至少覆盖一路径 |
| 反馈三类路由各自生效 | test_feedback_round 三用例 + 联跑 |
| 未点名节逐字不变（脏区） | test_feedback_round 断言 |
| 每轮后 reconcile+validate 重跑且通过 | test_feedback_round + 联跑产物 |
| "直接执行"开关：默认确认、关后跳过 | 页面演示 |
| 反馈轮回滚到上一轮 | test_feedback_round + 页面演示 |
| 深度评审按钮可用（judge 报告更新） | 页面演示 |
| 生成/反馈轮可取消（SSE cancel） | 页面演示 |
| 旧报告类型入口不受影响 | 现有测试全绿 + `--reuse-data` 快速回归一次 |

**C. 质量门**：pytest 全绿（现有 + 新增）；任一旧类型（company_review /
geology_demo_review / hp_quartz_review）`--reuse-data` 快速联跑不回归。

**D. 最终一关**：以上 A/B/C 全部满足后，**由用户在网页端实际完成一次完整报告生成
并确认可用**，验收才算通过。

## 12 工程约定

- 全程 UTF-8；如需 PowerShell 一律 pwsh7；前端免构建链不引入；
- 密钥只进 gitignored 的 `config/settings.yaml`，永不入库；
  `config/trees/`、`config/style_cards/` 为可入库内容（结构资产，不含密钥）；
- server 端口 8765 不变，`?v=` 静态资源版本号沿用并随前端改动递增；
- 不修改现有路由的行为，只新增端点；`run_pipeline.py` 现有参数语义不变；
- 每阶段完成后在 `优化进度.md` 回填一节（改动 + 验证证据 + 命令）。
