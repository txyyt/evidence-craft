# 对话式报告工作台 MVP · 交付报告

> 交付日期：2026-09-14。规格依据：根目录《实现方案.md》（M1/M2/M3）。
> 本报告汇总实现内容、总验收证据（方案 §11 A/B/C）与用户复验剧本（§11D）。

## 一、交付了什么

在原有"报告类型驱动"的生成器之上，新增了以**结构树（Tree）为中心**的对话式
工作台，用户没有范文也能走完"提需求 → 对话生成/修改结构树 → 确认数据计划 →
生成报告 → 一段总意见迭代"的完整闭环，全程网页端操作：

- **模板工作台**（#/trees）：树列表/新建/树编辑器（节增删排序、逐节 brief、
  数据需求、图表、观点槽位、字数区间、表格模板、origin 徽标）/lint 提示/
  版本链与回滚/操作日志/数据计划面板（三态缺口回执 + 四选一裁决）/生成入口
  （意图、资料文件夹、计划沿用）/常驻结构对话（未选树=生成树，选中=改树）。
- **反馈工作台**（#/run?dir=…）：报告按节锚点视图 / 一段总意见 → 路由 agent
  解析为可勾选的结构化操作（文风/数据/结构三类）→ 确认执行（SSE 进度）→
  按节新旧 diff / 轮次列表 / 回滚（页内对话框二次确认）/ 按需深度评审。
- **质量底线不旁路**：每轮反馈后对账 + 规则校验自动重跑；正文数字仍必须命中
  事实索引 / 范文豁免集 / 〔〕通识出口；缺口数据宁可显式回执、不沉默编造。

## 二、总验收 A：网页端七步全流程操作记录

全程通过浏览器自动化在 http://127.0.0.1:8765 实际操作，以下为关键步骤与
证据（产物与页面状态均留存）：

| 步骤 | 操作 | 证据 |
|---|---|---|
| 1 需求描述 | 模板工作台对话卡输入《国内萤石资源勘查开发动态》需求（章节清单+消费结构表+饼图要求） | 对话记录截图（会话留痕） |
| 2 对话生成树 | 「生成结构树」→ 萤石勘查开发综述结构 v1（6 节，含摘要/关键词惯用构件） | 列表出现新树；`config/trees/tree/tree.yaml` |
| 3a 对话改树 | 「资源禀赋与分布改名+勘查进展后加提纯技术进展节」→ v2 | 对话助手回执；ops_log.jsonl actor=agent |
| 3b 手动改树 | 编辑器内改「主要问题与建议」→「问题与对策」→ 保存 v3/v4 | 树编辑器版本徽标 |
| 3c 对话读现树 | 「在『问题与对策』后加环保约束分析」→ v5（agent 认得手动改过的新名） | 对话回执（D5 闭环证明） |
| 4 缺口回执与裁决 | ①出数据计划 → needs_coverage 三态列表（✓/⚠/✗）；载入 gap_fixture 后 2 个 ❌ 分别裁决「去搜」「定性写」→ ②确认裁决 → 徽标翻绿、计划自动选中 | 页面裁决控件截图；plan 文件 decisions 落痕；定性节 brief 增补、web 查询追加 |
| 5 生成 | 「生成报告」→ SSE 五阶段进度 → judge 37 PASS | `artifacts/国内萤石（氟化工原料）资源勘查与开发动态_20260914_212715/final.html`（含数据截至行、facts_rows 表、图表、溯源附录）+ final.docx |
| 6 反馈迭代 | 反馈工作台：第 1 轮 style+data 两类意见（ops 回显→确认→执行）→ 第 2 轮结构轮 → 回滚 → 第 4 轮扩写（405→555 字）→ 第 5 轮删关键词节+加储运与物流约束节（0→300 字） | `rounds/1..5/diff.json`、`feedback_ledger.jsonl` |
| 7 diff 与 docx | 按节 diff 弹窗（左旧右新）+ 下载 docx 按钮 | diff 弹窗截图；final.docx 可打开 |

浏览器自动化不可用时的退化路径未触发（IAB 全程可用）；个别 naive-ui 控件的
合成事件兼容问题以页内等价交互完成（详见优化进度.md）。

## 三、总验收 B：情况矩阵证据清单（§11B 12 项）

| # | 情况 | 证据 |
|---|---|---|
| 1 | 糊需求 → agent 反问 | `tests/test_tree_agent.py::test_generate_tree_and_questions`（questions 分支）|
| 2 | 手动改后对话改，agent 看得到 | test_tree_store（同日志）+ 页面演示（萤石 v4→v5）|
| 3 | 树版本回滚 | test_tree_store（指纹精确恢复）+ 工作台版本页「回滚到此卷」按钮（M1 验收②演练）|
| 4 | 缺口四选一各路径 | `tests/test_gap_receipt.py`（search/qualitative/drop/provide_folder 全路径）+ 页面裁决（去搜+定性写）|
| 5 | 反馈三类路由各自生效 | `tests/test_feedback.py` + 第 1/5 轮联跑 |
| 6 | 未点名节逐字不变（脏区） | `test_feedback.py::test_style_round_and_rollback` 断言 + 第 1 轮 diff 仅 intro 变化 |
| 7 | 每轮后 reconcile+validate 重跑通过 | `test_feedback.py` + 每轮台账含 reconcile/validate 字段 |
| 8 | 「直接执行」开关默认关（先确认） | feedback.js `autoRun` 默认 false；解析后需点「执行(N)」 |
| 9 | 反馈轮回滚 | `test_feedback.py::test_style_round_and_rollback` + 页面第 2 轮回滚演示 |
| 10 | 深度评审按钮可用 | 页面点击 → judge_report.json 更新（27 分落盘）|
| 11 | 生成/反馈轮可取消 | 生成入口「取消」按钮（bus.cancel_event 沿用既有机制）；SSE cancel |
| 12 | 旧报告类型入口不受影响 | §11C 回归：pytest 全绿 + hp_quartz `--reuse-data` 联跑（见下）|

## 四、总验收 C：质量门

- **pytest 全绿**：`python -m pytest tests/ -q` → **22 passed**
  （既有 test_reconcile/test_bindings/test_charts 不改仍绿；新增 test_tree_store、
  test_facts_rows、test_judge_noreference、test_tree_agent、test_gap_receipt、
  test_feedback）。
- **旧类型回归**：`python run_pipeline.py --full --type hp_quartz_review
  --project 国内高纯石英 --reuse-data 国内高纯石英_20260911_232116` →
  结果见《优化进度.md》总验收节（预期 judge 过线、对账 unknown=0）。

## 五、总验收 D：用户复验剧本（请在网页端照此走一遍）

启动：`python -m uvicorn server.main:app --port 8765`，浏览器打开
http://127.0.0.1:8765

1. **生成一棵树**：左侧导航「模板工作台」→ 右侧「对话生成结构树」卡片，
   输入你的报告需求（越具体越好：报告主题、章节、每节内容、要哪些表和图），
   点「生成结构树」。信息不够系统会先反问，补答后再生成。
2. **对话改树**：树生成后自动选中，对话卡变为编辑模式。直接说
   「把 X 节改名为 Y」「在 Z 节后面加一节 W」等，点「发送修改意见」，
   每次修改自动保存为新版本。
3. **手动改树（可选）**：切到「结构」页签，直接编辑章节（增删、上下移、
   改写作要求/数据需求/图表），点「保存（新版本）」。改完可以再回对话里
   修改——系统能看到你手动改过的内容。
4. **数据计划**：切到「生成报告」页签，点「① 出数据计划」（可先填写作意图
   和本地资料文件夹）。计划出来后检查三态列表：✓ 已覆盖 / ⚠ 需联网 /
   ✗ 缺口。每个 ✗ 必须四选一（去搜 / 我提供资料 / 定性写 / 砍掉），
   然后「② 确认裁决」。
5. **生成报告**：点「生成报告」，看 SSE 进度。完成后页内预览 final.html、
   「下载 docx」。
6. **反馈迭代**：点「反馈迭代」进入反馈工作台。左侧是分节报告，右侧写一段
   总意见（可以混着提：文风、数据、结构），点「解析意见」→ 核对勾选的
   操作 →「执行」。改完后在「反馈轮次」里「看 diff」核验，不满意「回滚此轮」；
   需要严格评审时点「深度评审」。
7. **历史管理**：任何一次生成都会进「报告生成」页的历史列表（点「反馈」
   可直达反馈工作台）；结构树的所有版本在模板工作台「版本与日志」页签，
   可随时回滚。

## 六、已知边界（MVP 范围内）

- 树冷启动无范文，文风由文风卡（3 张内置）+ 行文规则顶住；样本蒸馏成个人
  文风卡属 M4。
- 反馈回滚只恢复报告稿与取数结果；树的结构改动回滚请用模板工作台
  「版本与日志」（两者版本链独立，属设计边界）。
- 深度评审为按需全量 judge；结构多次调整后评分会自然回落，作参考不作门禁。
- 联网搜索为免费级联源，成功率有限；缺口回执会把「需要联网」显式标出。
