# M1 数据层
requests>=2.31
PyYAML>=6.0
pydantic>=2.6

# M2 流水线
jinja2>=3.1
openai>=1.30

# M4 渲染与前端（python>=3.10：代码使用 X | None 语法）
python-docx>=1.1
fastapi>=0.110
uvicorn>=0.29

# 图表渲染（render/charts.py，缺它生成报告时报错）
matplotlib>=3.8

# Excel 数据接入（本地资料文件夹绑定，datalayer）
openpyxl>=3.1

# 跑测试才需要（TestClient 依赖 httpx）
pytest>=8.0
httpx>=0.27

