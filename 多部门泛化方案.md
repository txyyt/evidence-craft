# 多部门泛化方案（v2 路线图）

> 背景：股票研报试跑（M1~M3）已验证核心论点——**规范报告需要的是工程纪律，不是自由 agent**。
> 现在回到真实目标：地学报告，且是多部门共用一个平台：各部门报告格式不同、数据来源不同。
>
> 新增两个入口需求：
> 1. **模板工厂**：用户把本部门的样例报告扔进去 → AI 分析结构 → 多轮对话/手动修改 → 定稿为模板
> 2. **数据源泛化**：本地文件 / RAG / 数据库 / 网络四类；网络源有登录墙问题（钢铁网、USGS 等）
>
> 与旧计划的关系：原 M4（前端+docx）、M5（泛化评估）未开工，被本方案吸收重排为新里程碑
> **M6 → M7 → M8 → M9**。核心原则不变：**数据用代码取，结构用 Schema 定，叙述交给 LLM，每个环节加校验。**

---

## 一、总判断：为什么这是泛化而不是重写

七阶段流水线（①规格 → ②数据 → ③大纲 → ④分节 → ⑤对账 → ⑥校验 → ⑦渲染）一行不推翻，
变化的只有两端的"配置面"：

| 环节 | 现状（股票特化） | 泛化后 |
|---|---|---|
| ① 报告规格 | 手写 `company_review.yaml` | AI 从样例提取草案 + 人工对话确认（模板工厂） |
| ② 数据采集 | 8 个东财 adapter 硬编码 | 数据源注册表：本地文件/数据库/RAG/网络四类 adapter，按部门配置绑定 |
| ③④⑤⑥⑦ | — | **机制不动**，仅把 prompt 里的股票措辞改为从 spec 注入 |

关键的前瞻性设计：**事实六元组 `{id, name, value, unit, source, as_of}` 对数据来源中立**。
`source` 写"eastmoney/push2"还是"化验台账.xlsx#Sheet1"还是"RAG检索:规范.docx p.23"还是
"钻孔数据库.tbl_hole"，下游对账/校验/渲染完全不关心。四类数据源在架构上是同一件事。

新增一个组织维度：**部门（Department）成为一等概念**。股票场景整体降格为第一个内置部门
（`stock_demo`），它的持续可跑就是回归标准。

```
目标架构（新增部分加粗）：

样例报告(1~3篇 docx/pdf)                        用户(部门数据管理员)
      │                                                │
      ▼                                                ▼
┌─ **模板工厂** ──────────────┐          ┌─ **收件箱 inbox/** ────────────┐
│ 解析→章节树→LLM结构提取→     │          │ 登录墙数据人工投喂：网页另存/    │
│ spec草案+置信度报告 → 人工   │          │ 导出Excel/PDF → 解析入库        │
│ 对话确认 → 定稿入库          │          └──────────────┬────────────────┘
└──────────┬─────────────────┘                          │
           ▼                                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ ① 报告规格  config/report_types/*.yaml（v2，与数据源无关）        │
│    × 部门配置  config/departments/{dept}/profile.yaml            │
│      （数据绑定：spec 的 data_needs → adapter+params）            │
├─────────────────────────────────────────────────────────────────┤
│ ② 数据层  **数据源注册表**                                        │
│    local_file / database / rag / web（现有东财8源收编为web首例）  │
│    → facts.json（六元组 + **reliability 可靠性分级**）            │
├─────────────────────────────────────────────────────────────────┤
│ ③④⑤⑥⑦ 流水线与质量防线：机制不变（大纲/分节/对账/校验/渲染）      │
└─────────────────────────────────────────────────────────────────┘
           │
           ▼
┌─ **前端工作台** ────────────────────────────────────────────────┐
│ /studio 模板工作台（上传→草案→对话修改→试跑→定稿）                 │
│ /run    生成监控（阶段进度/产物下钻/报告预览/docx下载）            │
└────────────────────────────────────────────────────────────────┘
```

---

## 二、核心概念定义

### 2.1 部门（Department）与报告规格（Report Spec）正交分离

- **report_types/*.yaml（spec v2）**：只描述结构——章节树、槽位、表格/图件模板、写作策略、
  规则。**与数据源无关**，同一模板可被不同部门复用。
- **departments/{dept}/profile.yaml**：描述资源——数据源绑定、术语表、校验规则覆盖、
  judge 对标范文、行业关键词。**与报告结构无关**。

生成命令从 `--stock 000803` 变为 `--department stock_demo --report-type company_review`
（stock_demo 内部把股票代码作为参数传给绑定）。

### 2.2 数据绑定（Binding）

spec 的每个槽位/表格声明**语义化的 data_needs**（"最新一期钻孔化验汇总"、"区域水文观测数据"），
部门 profile 把每个 data_need 绑到具体 adapter + params。**未绑定的 data_need 在生成时显式报
"数据缺口"，不猜**——延续防空引用幻觉的哲学：宁可显式失败，不让模型用常识填空。

配套约定：
- **data_needs 标签先自由生成、强制绑定**：模板工厂允许 LLM 自由发明语义标签（v2 不做
  平台级词表），但 spec 定稿时所有 need 必须在 profile 里有绑定，未绑定即显式报缺口；
  词表治理推迟到积累 3+ 模板后再做。
- **facts id 命名空间**：`{源前缀}.{业务键}`（如 `assay.ZK402.au_avg`，与现有
  `fin.2026Q2.revenue_yi` 风格一致），collections 的 key 同步规范化——模板的 fallback
  声明才能稳定引用。
- **as_of 口径不写死**：地学数据的时点有采样日/分析日/录入日之分，adapter 留
  `as_of_field` 参数，由部门 profile 指定取哪个字段。

### 2.3 事实可靠性分级

六元组增加可选字段 `reliability`（缺省 `authoritative`）：

| 等级 | 来源 | 下游处理 |
|---|---|---|
| `authoritative` | API/数据库/文件直读的结构化数据 | 现状不变 |
| `retrieved` | RAG 检索片段中由 LLM 抽取的数字 | 对账同等；**judge 对关键数字从严**；渲染溯源标注更醒目 |
| `manual` | 登录墙数据的人工投喂（inbox） | 同 authoritative，但 source 必须含投喂文件名与日期 |

理由：对账器只能验证"数字有没有出处"，验证不了"出处本身对不对"。RAG 片段有录入/OCR 错误和
版本混杂风险（地学储量数字常有"历史估算 vs JORC 复审"多版本），必须让风险可见。

### 2.4 部署形态（已拍板 2026-09-08）

**先单机后共享**：v2 按单机实现——每台机器/每个部门一份，数据天然隔离、无鉴权负担；
架构上留共享的口子（产物与配置全部文件化、无本地全局状态），确有多部门共用一台内网
服务器的需求时，再补部门数据隔离、并发生成排队与最小鉴权。

---

## 三、M6 — Spec v2 泛化 + 模板工厂

### 3.1 Spec v2 Schema 设计

现状问题：`company_review.yaml` 的槽位语义（catalyst 等）、风险镜像法、forecast 表渲染、
`industry_keywords` 全部焊死，且散落在 yaml 字段与 pipeline 代码 prompt 两处。

v2 字段结构（YAML 示例，地学场景）：

```yaml
report_type: mineral_exploration_summary
description: 矿产勘查阶段性总结报告：勘查工程进展 + 品位/资源量数据 + 下步计划
version: 2

title_style: |
  结论式标题：把本期最重要的勘查发现写进标题……（20~30字）

sections:
  - id: core_findings          # 结论型章节（对应现在的 core_views）
    title: 核心发现
    kind: views                # views | table | risk | text | figures
    n_views: 4
    view_slots:
      - id: drilling_progress
        brief: 钻探工程进展：完成钻孔数、进尺、见矿情况……
        data_needs: [drilling_summary]        # 语义需求，部门 profile 负责绑定
        fallback: top_collections.periods 3   # 空引用兜底策略（声明式，替代硬编码）
      - id: grade_analysis
        brief: 品位分析：主要见矿孔品位、厚度、资源量估算……
        data_needs: [assay_summary, resource_estimate]
        fewshot: |                # 槽位级范文：本槽位写法的 1~2 段示范（模板工厂从
          【范文】ZK402 见矿段：108.5~113.2m，视厚度 4.7m，   # 样例对应章节节选并给选段理由）
          Au 平均品位 3.42g/t，单样最高 8.15g/t……
    view_style: |
      每条 = 结论式小标题（8~15字）+ 一段论述（100~180字）……
  - id: resource_table
    title: 资源量估算表
    kind: table
    table: resource_estimate   # 引用 tables[] 里的模板，代码渲染，LLM 不碰数字
    style: 80~150字说明文字……
  - id: uncertainty
    title: 不确定性说明
    kind: risk
    strategy: mirror           # 写作策略枚举：mirror=镜像法 | enumerate=逐条列举
    style: 3~5条……
  - id: figures
    title: 图件
    kind: figures
    figure_slots:
      - id: section_profile
        title: 勘探线剖面图
        provider: user_upload  # user_upload（部门提供底图）| code_render（代码生成）
      - id: grade_histogram
        title: 品位分布直方图
        provider: code_render

tables:                        # 表格模板：列定义 + 渲染器
  - id: resource_estimate
    renderer: generic_rows     # 内置渲染器：consensus_pe（股票预测表）| generic_rows
    columns: [矿体编号, 资源量(万吨), 平均品位(g/t), 金属量(吨), 口径标准]

rules:                         # 通用化的校验规则参数
  forbidden_words: [我们认为, 显著提升, 重大突破]
  controlled_vocab:            # 受控词表（股票的 RATING_WORDS 泛化）
    - field: 口径标准
      words: [JORC, 333, 334, 历史估算]

# 注意：没有全局 fewshot 字段。范文挂在槽位/章节级（views 章节挂 view_slots[].fewshot，
# 其他 kind 挂 sections[].fewshot）：长报告各章节写法异质（综述腔/数据描述腔/参数方法腔），
# 全局单一 fewshot 无法覆盖；fewshot 只教段落级写法（句式/数据密度/语气/术语惯例），
# 每槽位 1~2 段几百字即有效剂量，由模板工厂从样例对应章节精准节选。

judge_reference: reference/xxx.md   # judge 对标范文挂模板级（跟报告类型走——company_review
                                    # 的范文评不了行业深度），部门 profile 可覆盖
```

### 3.2 流水线消费侧改造清单

| 文件 | 改动 |
|---|---|
| `pipeline/outline.py` | `load_spec()` 读 v2；SYSTEM prompt 中股票措辞改为从 spec 注入（title_style/view_style/fewshot 已经是数据驱动，主要是残余硬编码清理） |
| `pipeline/sections.py` | `_default_facts()` 硬编码的槽位兜底改为读 `slots[].fallback` 声明；全局 fewshot 改为**按槽位注入**（`slots[].fewshot`，无则用章节级）；`forecast_table()` 泛化为 `tables[].renderer` 分发 |
| `pipeline/validate.py` | `RATING_WORDS`、禁用词等改为从 spec `rules` 读取；PE 口径互查变成可插拔的"口径规则"列表（股票部门配置 PE 规则，地学部门配置口径标准规则） |
| `pipeline/judge.py` | `REFERENCE_PATH` 硬编码改为部门级配置；五维评分维度保留 |
| `config/report_types/company_review.yaml` | **直接迁移为 v2 语法**（不留 v1 兼容——只有一份 spec，干净优先），迁移后跑回归 |
| `run_pipeline.py` | CLI 增加 `--department`/`--report-type`，stock_demo 为缺省 |

### 3.3 模板工厂（template_factory/）

**定位：一次性接入工具，不是每次生成报告的环节。** 部门首次接入时做一次，定稿后模板是
稳定资产——如果每次生成都让 AI 重新"理解"格式，结构会漂移，报告就没法规范。

流程六步：

```
样例(1~3篇) → ①文档解析 → ②章节骨架提取 → ③槽位细化 → ④表格/图件模板提取
→ ⑤规则与风格提取 → ⑥多篇交叉合并(置信度) → spec_draft.yaml + extraction_report.md
```

1. **文档解析**（纯代码）：docx → python-docx 抽标题层级+段落+表格+图注；pdf → pypdf
   （表格密集时选装 pdfplumber）；产出统一"章节树"中间表示（JSON 落盘，可断点）。
2. **章节骨架提取**（LLM×1）：输入章节树，输出每章的功能分类（结论/数据展示/风险/图件）
   → `sections[]` 草案 + `kind` 判定。
3. **槽位细化**（LLM×N，逐槽位）：读该节正文样本，提取叙述模式（论证结构）、数据依赖
   （用到哪类数字/表格，归一为 data_needs 语义标签）、篇幅分布，并为该槽位节选
   **1~2 段最具代表性的范文**（附选段理由，供 extraction_report 人工复核/替换）
   → `view_slots[]` 草案（含槽位级 fewshot）。
4. **表格/图件模板提取**：表格 → 列 schema（表头+示例行）；图注 → 图件槽位清单及
   provider 判定（统计图→code_render，专业图件→user_upload）。
5. **规则与风格提取**：标题句式归纳、术语表、明显的格式禁忌 → `rules`/`title_style` 草案。
6. **交叉合并**：多篇样例逐字段比对——一致 → 高置信直接采纳；分歧 → 标记
   "待人工裁决"。产出 `extraction_report.md`：置信度清单 + 分歧点 + 建议人工确认项。

**硬约束：LLM 的所有输出必须落在 v2 Schema 上**（pydantic 校验，不合格即重试），
不允许自由文本 spec——后续流水线执行的是 Schema，不是散文。

### 3.4 对话式修改（机制在此定义，界面在 M8）

```
用户对话（"风险提示改成三段式"）
  → LLM 意图解析 → 生成 spec patch（结构化 diff，非全文重写）
  → 前端展示 diff 预览 → 用户确认应用
  → 可选：试跑一节（dry-run）看效果 → 定稿写回 config/
```

AI 修改也走 Schema 约束（patch 必须通过 pydantic 校验），与流水线的 revise 循环同构
（issues → instruction → 定向重写），不会改坏模板。

### 3.5 任务清单与验收

- [ ] v2 Schema pydantic 定义（`template_factory/schema.py`）
- [ ] company_review.yaml 迁移 v2 + 流水线消费侧改造（3.2 清单）
- [ ] **回归验收：000803 / 600519 / 600754 三股票重跑，judge 分数与对账结果不降**
- [ ] 模板工厂六步（`template_factory/extract.py`），CLI 入口：
      `python -m template_factory.extract --samples dir/ --out config/report_types/xxx.yaml`
- [ ] extraction_report.md 产出（置信度+分歧点）
- [ ] 流水线阶段进度回调钩子（progress callback 替代 print，M8 的 SSE 直接转发）
- [ ] 本里程碑涉及模块的 API 契约冻结进 `API.md`
- **验收**：拿 2~3 篇真实地学样例提取，人工确认/修改点 ≤ 10 处即可定稿；
  定稿 spec 能通过 Schema 校验并完成一次 **replay 样例回放**——解析样例时把样例正文中的
  数字连同上下文抽成临时 facts（`reliability: manual`、`as_of: 样例日期`），只验证模板的
  叙述模式通不通，不用于真实交付（与 M8 工作台的真数据 dry-run 是两回事）

---

## 四、M7 — 数据源插件化 + 部门配置

### 4.1 Adapter 统一接口

```python
# datalayer/adapters/base.py
class SourceAdapter(Protocol):
    kind: str                      # local_file | database | rag | web
    default_reliability: str       # authoritative | retrieved | manual

    def fetch(self, params: dict) -> AdapterResult:
        """AdapterResult = {facts: [...], collections: {...}, warnings: [...]}
        facts 元素 = 六元组 + reliability。失败抛 SourceError，不阻塞其他源。"""

# datalayer/registry.py：注册表 + 按 profile 绑定实例化
```

现有 `datalayer/sources/` 的 8 个东财 adapter **原样保留**，注册为 web 类的首批实现
（`eastmoney` adapter 的 params 就是 stock code）。`assemble.build()` 从股票专用入口泛化为
"按部门 profile 的绑定列表逐个调 adapter、汇总 facts_doc"——股票版 build 变成
stock_demo 部门的一个组装 profile。

### 4.2 四类 adapter 的工程要点

**local_file（最可靠，优先做）**
- 解析器：xlsx → openpyxl（声明式"表头行/数据区"映射配置）；docx/pdf → 沿用模板工厂的解析器
- 来源指纹：文件内容 hash + sheet/页码，写进 fact 的 source，改文件即失效缓存
- 收件箱复用：`inbox/{department}/{来源名}/` 目录即 local_file 的一个挂载点，
  人工投喂的登录墙数据从这进（meta 记投喂时间，source 显示
  "人工投喂：钢铁网价格表(2026-09-07).xlsx"）

**database**
- 连接配置进 `config/settings.yaml`（gitignore），凭据走环境变量，不进库；
  profile 只存连接名引用（`db_ref: assay_db`），不存连接串
- 查询模板进 profile（`query: "SELECT 孔号, 进尺, 品位 FROM tbl_assay WHERE 期次=:period"`）
- 首期只接 SQLAlchemy 支持的库；**部门实际库型待拍板**（Oracle/SQLServer/PostgreSQL/SQLite）

**rag（已拍板 2026-09-08：对方已有现成 RAG 服务，我们不自建库，只做对接 + 留接口）**
- 边界划分：**对方的服务负责存与检索**（库内容、embedding、向量库都是对方的）；
  **我们的 adapter 负责把检索片段变成可信 facts**——这层职责不变、仍是本类源的核心价值
- adapter 形态：外部检索服务客户端（`query → 片段列表[文档名/页码/正文]`），连接协议
  （HTTP/SDK）待对方明确，先用可配置的 http_json 形态留接口；mock 片段先跑通我方管线，
  真联调放 M9 试点前
- 抽数管线（我方保留）：片段 → LLM 抽数字事实（六元组 + `reliability: retrieved` +
  source 含文档名与页码）
- **抽取即对账**：抽出的每个数字必须能在检索片段原文中找到（把 reconcile 的理念前移到
  fact 生产环节），找不到即丢弃——防止"抽取幻觉"

**web（收编现有 + 登录墙分层策略）**
- 现有东财族全部挂到 web 类；新增源的接入顺序原则：
  **公开 API > 公开文件下载 > 免登录网页 > 登录网页**
- 登录墙四层策略（前两层为主）：
  1. **绕开 HTML 找开放出口**：USGS 地震 API / Mineral Commodity Summaries（公开 PDF）、
     世界钢铁协会、LME、国家统计局——逐源评估，建"开放出口清单"
  2. **人工投喂通道**（最现实）：部门有人有账号 → "人取数、机入库"，走 inbox，
     系统价值在人只负责"取"，解析/对账/成文全自动
  3. **受控采集**（playwright + 会话）：默认关闭的可选扩展，仅当"特定源确有必要 +
     部门明确授权 + 低频更新"三条件齐备；有合规/封号/改版维护三重代价
  4. 官方数据合作/采购：商务问题，非工程问题

### 4.3 部门 profile 示例

```yaml
# config/departments/stock_demo/profile.yaml
department: stock_demo
description: 股票研报（内置回归部门，平台能力的第一个验证场）
params_schema: {stock: 股票代码}

bindings:                        # spec 的 data_needs → adapter
  - need: quote_snapshot
    adapter: eastmoney_quote
  - need: financial_periods
    adapter: eastmoney_financial
  # ...

vocabulary:                      # 行业术语（现 spec 的 industry_keywords 迁移至此）
  industry_keywords: [UCO, 生物柴油, SAF, ...]

rule_overrides:                  # 校验规则覆盖（如 PE 口径互查只属于股票部门）
  - id: pe_ttm_suspect
    enabled: true

judge_reference: reference/company_review_guosen_000803.md   # 覆盖模板级缺省
```

### 4.4 任务清单与验收

- [x] adapter 接口 + 注册表；现有 8 源收编；assemble 泛化；stock_demo profile 建立（2026-09-08）
- [x] **回归验收：三股票最终回归 judge 38/42/45 全 PASS，高于 M6 基线**（2026-09-08）
- [x] local_file：xlsx 台账出 facts（声明式映射 + 内容指纹 + 缺失格跳过），对账通过
- [x] database：SQLite 查询模板过集成测试（真实库型接入待拍板后做）
- [x] rag：mock 片段跑通我方抽数管线（reliability=retrieved 标注、"抽取即对账"
      拦下注入假数字——测试用例）；对方服务协议明确后真联调
- [x] web 开放出口清单首项：USGS 地震 API 打通（公开 API 跨厂商模式验证）
- [x] **geology_demo 合成第二部门端到端**（方案外增补：防单例过拟合）——
      SQLite + xlsx + RAG mock 三源绑定，judge 39/50 PASS

---

## 五、M8 — 前端工作台·正式版（吸收原 M4）

定位升级：不是"两个页面"，而是一个正式的多部门工作台——平台壳 + 七个功能区。
此前讨论的全部功能（模板工作台 / 生成监控 / docx 终稿）全部保留，另配齐
模板库、部门管理、数据源、系统设置等管理功能区。

技术栈：FastAPI + SSE 后端不变；前端 Vue 3（CDN 引入，免构建链）+ Naive UI
组件库（表格/表单/上传/树/步骤条开箱即用）+ ECharts（judge 雷达），依赖文件
本地化到 static/vendor/，保持零 node 工具链。单机阶段不做登录鉴权（壳上预留
入口不实现），共享部署时再启用。

```
┌──────────────────────────────────────────────────────────────┐
│ EvidenceCraft 多部门报告平台       [部门上下文▾]   [⚙ 设置]  │
├────────────┬─────────────────────────────────────────────────┤
│ ① 首页     │                                                 │
│ ② 模板工作台│  内容区：七个功能区共用此壳；顶部全局切换部门    │
│ ③ 模板库   │  上下文，/studio 与 /run 自动跟随当前部门        │
│ ④ 生成监控 │                                                 │
│ ⑤ 部门管理 │                                                 │
│ ⑥ 数据源   │                                                 │
│ ⑦ 系统设置 │                                                 │
└────────────┴─────────────────────────────────────────────────┘
```

**① 首页**：部门卡片（已定稿模板数、最近一次运行 judge 分与状态）、快捷入口
（新建模板→工作台 / 发起生成→监控）、系统健康（LLM 连通、数据源最近告警）。

**② /studio 模板工作台**（核心，三栏：左样例章节树 / 中 spec 草案编辑器+diff /
右对话窗）
- 上传样例（支持 2~3 篇多选）→ SSE 推模板工厂六步进度 → 草案章节卡片 +
  extraction_report 置信度标记；低置信项左栏黄色高亮，与待裁决清单点选互跳
- 手动编辑（章节卡片表单化，可切 YAML 视图）与对话修改走同一条路：
  patch 过 Schema 校验 → diff 预览 → 确认才应用；手动编辑所见即所得
- 两级验证：回放验证（样例自身数字验证叙述模式，任何部门可跑）/
  试跑一节（真实数据 dry-run，需该部门已有绑定；新部门"样例先到、绑定后补"
  时回放兜底）
- 定稿写回 `config/report_types/`，每次保存前自动快照，git 作二道保险

**③ /templates 模板库**：report_types 全集（状态：草稿/已定稿）、版本时间线、
任意两版 diff、一键回滚、下载 yaml；点"继续编辑"回工作台。

**④ /run 生成监控**（三栏：左进度 / 中产物 / 右预览）+ 运行历史表
- 左：阶段进度 ①→⑦，实时显示当前在写哪一节（SSE 流式，纯转发 M6 进度回调）
- 中：中间产物查看器——点任意阶段看 facts/大纲/稿件 JSON；对账失败标红，
  点开看哪个数字对不上账、引用了哪个事实；judge 五维雷达 + 逐维评语 + issues
- 右：最终报告预览（HTML 内嵌）+ docx 下载；参数表单按 profile 的
  params_schema 动态生成；运行历史可筛选、回看任意一次产物
- docx 分级不变：M8 交规范简洁版（黑白、标准标题层级、规范题注，python-docx
  代码生成）；部门红头/图框等强版式走"docx 模板文件 + 占位替换"，M9 按试点
  部门真实模板做。图件槽位预览显示占位框，上传底图后合成进 docx；正文只允许
  用图件 id 引用（如 `fig:section_profile`），编号（"图1""图2"）由渲染层统一排，
  模型不得硬编码图号

**⑤ /departments 部门管理**：部门列表 + profile 编辑器——绑定表（need /
adapter 下拉 / 参数表单 / 启停 / 单绑定测试按钮）、vocabulary、features 开关、
crosschecks、judge_reference 选择、params_schema 表单化编辑；新增部门向导
（建目录 → 选 adapter → 最小绑定即可保存）。

**⑥ /sources 数据源**：adapter 注册表总览（key / kind / 用途）；外部连接配置
（数据库连接、RAG endpoint——写 settings.yaml，页面密钥掩码展示）；每项
"测试连接"；playwright 等默认关闭的受控采集能力在此放灰色开关（M9+）。

**⑦ /settings 系统设置**：LLM 配置（base_url / model / api_key 掩码 / 温度）、
流水线默认（revise 轮数上限、judge 门槛）、关于与版本。

server/ 薄层路由组（实现时逐个冻结进 `API.md`）：`/api/overview`（首页汇总）、
`/api/health`（LLM ping）、`/api/studio/*`（上传 / 提取进度 SSE / 草案 / patch /
dryrun / replay / finalize）、`/api/templates`（列表 / 版本 / diff / 回滚 /
下载）、`/api/runs`（启动 + SSE / 历史 / 产物读取）、`/api/departments`（CRUD +
单绑定测试）、`/api/sources`（adapter 列表 / 连接配置读写·掩码 / 测试连接）、
`/api/settings`（读写，密钥只写不回显）。

安全与既定原则不变：凭据只进 settings.yaml（gitignored），API 响应中密钥一律
掩码；profile 不存凭据只存 db_ref 引用；spec/profile 保存前自动快照。

- [x] 平台壳（侧边导航 / 部门上下文切换 / toast / SSE 总线）+ 系统设置 + 健康检查
- [x] /run 三栏 + 运行历史 + docx 渲染
- [x] /studio 工作台（上传 / 置信度标记 / 卡片编辑 / 对话 patch / dry-run / replay / 定稿）
- [x] 模板库（版本 / diff / 回滚）
- [x] 部门管理 + 数据源管理
- [x] 首页 Dashboard
- **验收（2026-09-08 ✅）**：浏览器完成"上传样例 → 看草案 → 对话改一处 → 试跑 → 定稿 →
  用新模板生成一份报告 → 下载 docx"全流程；000803 在 /run 页两次完整跑通
  （final.docx 可下载，judge 视觉验收 PASS）；另验管理功能全过：页面上改部门
  profile 并单绑定测试通过（SQLite 12 / xlsx 48 条）、设置页改模型热生效 + 连通
  测试通过、模板库完成 diff 与回滚。真实验收遗留：地学真实样例到达后在工作台
  复跑一次提取定稿。详细记录见《开发计划》第十节。

构建顺序（壳先行，核心功能其次，管理页再次，首页最后聚合）：壳+设置 →
/run+docx → /studio+模板库 → 部门/数据源 → 首页+验收。

---

## 六、M9 — 试点接入与回归（吸收原 M5）

- [ ] 首个试点部门全流程接入：2~3 篇真实样例 → 模板定稿 → 数据源绑定（按该部门实际的
      本地文件/数据库/RAG 组合）→ 端到端生成 1 份真实报告 → 部门人工评审（结构与数字）
- [ ] judge 范文按部门配置生效（茅台案例已证必要性：股票范文评地学报告必然失真）
- [ ] 回归集：股票场景三标的常态化回归（CI 式脚本，judge 分数与对账通过率基线对比）
- [ ] 《地学报告平移说明》（原 M5 遗留交付物）
- **验收**：试点部门评审通过（结构合规 + 数字 100% 对账 + 溯源可点击核对）；
  股票回归无退化

---

## 七、顺序、依赖与工作量

```
M6 spec v2 + 模板工厂 ──→ M7 数据源插件化 ──→ M8 前端工作台 ──→ M9 试点
   （先解耦模板，          （地学数据以本地/        （把 M6/M7 串成        （真实场景
     三股票回归兜底）        库为主，先打通            产品体验，含           验收 + 回归）
                              这两类）                docx）
```

- M6 先行的理由：模板工厂的 dry-run 依赖 spec 可执行；先证明"流水线与具体模板解耦"
  风险最小，且全程可用股票回归兜底。
- M7 数据源先于 M8 前端的理由：工作台的试跑/生成要有真实数据链路可调；前端是体验层，
  放最后不阻塞任何能力验证。
- 每阶段都有 CLI 可用形态（M6/M7 纯 CLI，M8 才上界面），符合"前端是观察窗口不是必需品"。

### 开发节奏：后端先行，前端集中 M8，不并行

理由：① 前端是纯薄层（跑流水线/推事件/给产物），能展示什么完全取决于后端已有什么；
② M6/M7 是接口剧变期（schema v2 / 数据源注册表 / CLI 参数），并行写前端返工率最高；
③ 单线开发节奏，"并行"实际是频繁切换上下文，稀释每个里程碑的质量。

后端先行期间的三个铺路动作（成本极低）：
1. **契约先行**：每完成一个模块就冻结其 API 签名（路由/请求/响应 JSON 形状），
   记入 `API.md`，M8 照契约写前端；
2. **进度事件钩子**：M6 改造流水线时把 print 式输出换成 progress callback，
   M8 的 SSE 层届时是纯转发；
3. **落盘产物即天然 API**：artifacts/ JSON 结构稳定，产物下钻直接读文件。

可选：M7 收尾时花半天先搭最简版 `/run`（静态文件 + SSE + 产物列表，无工作台），
提前在浏览器看报告生成——属提前兑现一小块 M8，非并行开发。

- 工作量当量（自研节奏，供排期参考）：M6 ≈ 2~3 天，M7 ≈ 2~3 天（RAG 不自建、
  只做对接后缩减），M8 ≈ 5~7 天（正式版工作台：壳 + 七个功能区 + docx），
  M9 ≈ 2 天 + 试点部门配合时间。

---

## 八、风险与对策

| 风险 | 对策 |
|---|---|
| 单篇样例的偶然特征被当成本质结构 | 要求 2~3 篇样例交叉提取；分歧点必标"待人工裁决" |
| RAG 抽数错误/文档版本混杂 | reliability 分级 + 抽取即对账 + judge 对 retrieved 关键数字从严 + 报告内醒目溯源 |
| 登录站爬取的合规与封号风险 | 默认只走"开放出口 + 人工投喂"；playwright 受控采集默认关闭、需三条件齐备 |
| 部门 spec 被对话修改改坏 | patch 走 pydantic 校验 + diff 预览 + 定稿版本留痕，可回滚 |
| 地学专业图件无法代码生成 | 图件槽位 provider=user_upload，预览占位、上传合成；统计图才走 code_render |
| 数据缺口导致模型编数 | data_needs 未绑定即显式报缺口；槽位 fallback 声明式兜底（M3 已验证的机制泛化） |
| 多部门凭据/连接管理 | 凭据只走环境变量/settings.yaml(gitignore)，profile 只存绑定关系 |
| 外部 RAG 服务协议/质量未知 | 接口抽象 + mock 片段先跑通我方抽数管线，联调放 M9，不阻塞主线 |

## 九、决策记录（2026-09-08 拍板）与仍待拍板

已拍板：
1. **部署形态：先单机后共享**（见 2.4）——v2 单机实现，留共享口子。
2. **样例形态：docx / 文字版 PDF**——OCR 不做，扫描版 PDF 列为已知边界。
3. **生成输入参数：多种形态并存、暂无定论**——v2 以"项目编号 + 期次"为主参数模型
   设计（最接近股票模式、数据绑定最规整），"人工圈定数据范围"与"台账按时间窗"作为
   参数形态扩展；profile 的 params_schema 本就是声明式的，容纳得下。
4. **RAG：对方已有现成服务，我们不自建**——只做对接客户端 + 抽数管线（见 4.2），
   原 embedding 选型决策作废。

仍待拍板：
1. **首个试点部门与样例**：2~3 篇真实样例（docx 优先，文字版 PDF 可）——M6 模板工厂
   验收的输入，M6 后半段（提取器）开工前需要。
2. **部门数据库类型**：Oracle / SQL Server / PostgreSQL / SQLite / 其他（决定 database
   adapter 首接目标与测试环境；SQLite 模拟库先行，不阻塞 M7）。
3. **受控采集（playwright）**：默认不做。若确有"必须自动更新 + 有授权账号"的源再开。
