# EvidenceCraft 修改方案 V4

> 审查日期：2026-09-15  
> 审查范围：前端人机交互与“生成结构树 / 生成报告 / 报告后续修订”核心工作流。  
> 调查方式：先通读 `README.md`、`API.md`，再启动 `python -m uvicorn server.main:app --port 8765`，以首次使用者视角实操 `#/new` 的三条起点分支、`#/reports`、历史报告详情五个页签、`#/templates`、`#/settings`。真实 LLM 调用包括一次结构树问答、一次数据计划、一次数据预检和一次完整报告生成；其余使用现有树与历史产物。  
> 本文只规定后续修改，不在本次审查中修改业务代码。历史已完成项不在本文重复立项。

## 〇、问题总账

| 编号 | 问题（用户视角描述） | 根因定位（文件） | 类别 | 严重度 |
|---|---|---|---|---|
| P1 | 主题名含 `/` 时，等待约 6 分钟并显示生成成功后却进入空白详情；新报告也不出现在报告库中，用户无法取得刚生成的成果。 | `run_pipeline.py` 的运行目录直接拼接 `subject`；`static/js/views/new.js` 的 `goDetail()` 把反斜杠替换为斜杠后只保留最后一段；`server/routes_runs.py` 的 `history()` 只扫描 `artifacts/` 直接子目录。 | 工作流 | P0 流程死胡同 |
| P2 | 打开不存在、已移动或 P1 产生的错误报告地址时，页面保留标题、页签和下载按钮，但正文为空，没有“报告不存在”、返回或重试入口。 | `static/js/views/detail.js` 的 `get()` 吞掉所有请求错误并返回 `null`，`load()` 没有 loading/error/ready 状态。 | 交互体验 | P0 流程死胡同 |
| P3 | 数据计划、反馈和报告生成只依附当前页面；离开向导后看不到任务，不能取消或回到任务，完成回调还可能把用户从别的页面强行跳走。 | `static/js/views/new.js` 在组件内持有 EventSource/轮询且未在卸载时解绑导航；`static/js/store.js` 只保存类型；`server/bus.py` 已保留任务状态，`server/routes_runs.py` 已有状态与取消端点，但前端没有全局任务模型。 | 工作流 | P1 高频不便 |
| P4 | 出计划、预检和生成等待 30 秒至 6 分钟时，主要反馈只是按钮转圈或原始日志；没有当前阶段、已用时间、可否离开、取消和恢复说明，等待期间输入仍可改。 | `static/js/views/new.js` 的 `startPlan()`、`previewPlan()`、`startRun()` 只维护局部 loading；模板没有统一长任务状态卡。 | 交互体验 | P1 高频不便 |
| P5 | 第 ② 步先看到“出数据计划”，写作意图和资料目录却排在后面；仓库已有计划也无法选择复用。用户容易先点按钮，之后才发现信息填早/填晚，或重复花时间生成计划。 | `static/js/views/new.js` 第 ② 步模板顺序；`loadPlans()` 的结果未形成选择器；`server/routes_trees.py::tree_plans()` 只返回文件名、大小、时间，缺少是否已确认、树版本匹配等决策信息。 | 工作流 | P1 高频不便 |
| P6 | 预检完成只显示“事实 N 条 / 来源计数”单行摘要；失败、0 事实、警告和交叉校验缺少清楚出路，而且 `canNext2` 不检查预检状态，用户可能带着坏数据进入 5–10 分钟生成。 | `static/js/views/new.js::previewPlan()` 丢弃已有返回字段；`canNext2` 只检查来源/计划确认，不检查预检运行中、失败或过期。 | 工作流 | P1 高频不便 |
| P7 | 最后确认页只显示树 id 和计划文件名。用户看不到树名/版本/章节数、预检事实与警告、预计耗时，也不能选择后端已经支持的模型档位，难以在昂贵长任务前发现选错。 | `static/js/views/new.js` 第 ③ 步模板和 `startRun()`；`server/routes_runs.py::TreeRunIn` 已有 `model_tier`，前端未展示也未发送。 | 交互体验 | P1 高频不便 |
| P8 | “向导”与“模板”都能用对话造树，但在模板页编辑完树后没有“用这棵树写报告”；还要回向导、重新选“现有树”并在大量重名项里找到它，路径割裂。 | `static/js/views/templates.js` 同时承担无树生成和有树修改，却没有生成报告的深链；`static/js/views/new.js` 不读取 tree 查询参数；`static/js/app.js` 未定义跨页上下文。 | 工作流 | P1 高频不便 |
| P9 | 报告出现“补数据后重新生成”或用户想换结构再写一版时，详情页只告知问题，不提供带回原树、计划和数据的操作；必须从向导第一步重走。 | `static/js/views/detail.js` 的顶部警告、治理页和元信息页没有回环操作；`run_pipeline.py` 的 `meta.json` 未记录可复用的计划文件；树模式启动未暴露复用已有数据选项。 | 工作流 | P1 高频不便 |
| P10 | 多轮反馈后，页头仍显示初始 `judge 23 FAIL`；“深度评审”结果只闪过 toast，页面没有更新；已经回滚的轮次仍可继续“回滚此轮”，实操产生 4 条重复回滚记录；diff 两栏也没有“修改前/后”文字标签。 | `static/js/views/detail.js::judgeDeep()` 写入未渲染的 `judgeInfo`；轮次模板无回滚状态判断；`pipeline/feedback.py::rollback()` 与 `rounds()` 没有重复回滚约束/显式关系字段。 | 工作流 | P1 高频不便 |
| P11 | 新用户在各页直接面对“结构树、lint、缺口裁决、预检、文风卡、judge、PASS/FAIL、data_support、renderer、facts_rows”等术语和内部 id，难以判断它们与自己写报告的关系。 | `static/js/fielddict.js` 词典陈旧且未形成共享解释组件；`static/js/views/new.js`、`reports.js`、`detail.js`、`templates.js`、`settings.js` 各自直接输出内部字段。 | 交互体验 | P1 高频不便 |
| P12 | 结构树列表有多条同名“动态简报/行业综述”，没有搜索、状态筛选和版本辨识；编辑器把章节类型、renderer、表 id、来源前缀等高级字段与常用字段平铺，部分输入无可访问 label。 | `static/js/views/templates.js` 的树列表、`KINDS`/`RENDERERS`、编辑模板和新建弹窗；`static/js/fielddict.js` 仍假定英文 section id，且没有基础/高级分层。 | 交互体验 | P1 高频不便 |
| P13 | 对话造树的输入为空时“生成结构树”仍可点击，点击后无提示；LLM 返回追问后焦点仍在按钮上，用户不容易意识到要回输入框逐项回答。 | `static/js/views/new.js::generate/chat` 的空值短路和按钮禁用条件；`static/js/views/templates.js::sendChat()` 也静默短路。 | 交互体验 | P2 低频打磨 |
| P14 | 设置页“可用适配器”整块为空，用户会以为系统没有数据接入能力；实际 `/api/sources/adapters` 已返回 14 项。 | `static/js/views/settings.js::load()` 取到了 `adapters`，但 `setup()` 的返回对象漏掉该响应式变量。 | 交互体验 | P1 高频不便 |
| P15 | 报告库有 64 条记录、其中 39 条显示 `FAIL`，却只有来源筛选；没有标题搜索、治理状态筛选或运行中任务，表格还直接展示 `judge`、`tree`、`PASS/FAIL`。 | `static/js/views/reports.js::load()`、`filtered`、`columns` 和统计区；未消费任务状态。 | 交互体验 | P1 高频不便 |
| P16 | 详情“文件”页把最终 HTML/DOCX 与 `facts.json`、`feedback_ledger.jsonl`、`rounds/*` 等内部审计文件混在一张长表里，普通用户找下载物困难。 | `static/js/views/detail.js` 文件页直接渲染 `server/routes_runs.py::list_files()` 的平面结果。 | 交互体验 | P2 低频打磨 |

### 实操证据索引

| 证据 | 页面与操作序列 | 用户预期 | 实际结果 / DOM 或产物证据 |
|---|---|---|---|
| E01 | `#/new` → 对话生成 → 保持输入为空 → 点“生成结构树” | 按钮禁用或提示必填 | `EC._newView` 对应按钮 `disabled === false`；完整鼠标事件后页面无 alert、message、校验文本，断言结果 `emptyClickProducedFeedback === false`。 |
| E02 | `#/new` → 现有树 → 展开选择器 | 可按名称和版本辨认 | 初始可见项中“动态简报”重复 4 条、“行业综述”重复 5 条；选项只给名称、id、节数，没有版本和 lint 状态。 |
| E03 | 选“国内高纯石英研究报告” → 第 ② 步 | 先填数据上下文，或复用已有计划 | DOM 中先出现启用的“① 出数据计划”；写作意图/资料文件夹位于按钮后；`plans/` 已有文件但页面没有计划选择器。 |
| E04 | 真实出计划，等待约 30 秒 | 看见阶段、耗时和离开后的恢复方式 | 等待期间只有按钮 spinner，输入仍可编辑；结果显示“0 个缺口待裁决”，同时多行仍标“⚠ 需联网/资料”，确认按钮仍需再点且确认后仍保持可操作。 |
| E05 | 真实预检，等待约 30 秒 | 知道资料是否足以安全生成 | 完成后仅一行“事实 8 条｜web…”，未展示返回中的 warning/crosscheck/sample；`canNext2` 对失败或 0 事实无约束。 |
| E06 | 第 ③ 步确认 | 在长任务前复核完整配置 | DOM 只显示“树：id｜计划：timestamp.json”；没有名称/版本/章节/预检/耗时/模型档位。 |
| E07 | 主题“中国高纯石英行业（砂/材料）市场研究” → 真实完整生成 | 成功后打开并在报告库找到报告 | 生成约 6 分 15 秒后自动进入空详情；磁盘实际生成在 `artifacts/中国高纯石英行业（砂/材料）市场研究_20260915_230104/` 两级路径，`#/reports` 仍为 64 条且不含该主题。 |
| E08 | 强刷 E07 自动打开的详情 URL | 明确错误并可返回/重试 | 页面只剩标题区、五个页签和下载链接，预览 iframe 为 `about:blank`，无错误文案；服务端记录 `FileNotFoundError`。 |
| E09 | 完整生成进行中 → 浏览其它页面 | 可在全局看到并控制任务 | 报告库没有运行中项，向导没有取消按钮；`POST /api/runs/{id}/cancel` 实际已存在。 |
| E10 | `#/templates` → 选择一棵树并编辑 | 保存后直接用它写报告 | 全页按钮枚举没有“用这棵树生成/写报告”；空态反而声称会出现“生成入口”。 |
| E11 | 打开历史详情 → 治理/元信息/反馈 | 看懂状态，并看到反馈后的当前结论 | 页面直接显示英文内部字段；实测 6 轮记录中第 3–6 轮均为“[回滚] 撤销第 2 轮”，每行仍有“回滚此轮”；页头仍显示初始 23 分 FAIL。 |
| E12 | 详情 → 点深度评审 | 评审结果成为当前可见状态 | 请求完成只出现 toast；`judgeInfo` 在脚本中赋值但模板无任何引用。 |
| E13 | `#/settings` → 查看可用适配器 | 看见系统支持的数据源 | UI 列表为空、控制台无错误；同一会话请求 `/api/sources/adapters` 返回 14 条，`setup()` 未 return `adapters`。 |
| E14 | `#/reports` | 快速找到需要继续处理的报告 | 共 64 条、39 条 FAIL；只有“来源”筛选，没有标题/状态筛选，列值为 `judge`、`tree`、`PASS/FAIL`。 |
| E15 | 历史详情 → 文件 | 先取得可交付文件 | 一张平面表同时列最终成品、事实、评审、反馈账本和所有轮次文件。 |

## 一、修改项详述

### A. 工作流类

### V4-01　保证生成产物可达，并给详情失败明确出路（P1、P2）

**现状**  
树的 `subject` 被直接用于目录名。Windows 会把 `/` 解释为路径分隔符，成功产物因此落到二级目录。前端再对路径做字符串切分，丢失父级；历史接口也只看一级目录。详情页同时吞掉 404，形成成功后的死胡同。

**改法**

1. 在 `run_pipeline.py` 增加 `_safe_artifact_name(subject, timestamp)`，仅用于磁盘目录名：将 Windows 禁止字符 `< > : " / \\ | ? *`、控制字符和路径分隔符统一替换为 `-`，合并连续替换符，去掉末尾空格/点，空结果回退 `report`，并限制 UTF-8 目录名长度；报告展示标题继续使用原始 `subject`。
2. `server/bus.py::summarize()` 和运行完成事件增加 `artifact_dir`（目录 basename）；保留现有 `run_dir` 字段，旧消费者不变。`static/js/views/new.js::goDetail()` 优先使用 `artifact_dir`，只执行一次 `encodeURIComponent()`，禁止自行拆路径；缺少新字段时用 `Path` 语义的 basename 兼容旧事件。
3. `server/routes_runs.py::_safe_dir()` 继续强制目标必须是 `artifacts/` 的直接子目录。`history()` 不递归吸收旧的错误嵌套目录，以免扩大可访问面；由第 1 条杜绝新错误。
4. `static/js/views/detail.js` 引入 `loadState: 'loading'|'ready'|'error'` 和 `loadError`。任一核心请求（至少 meta 或最终报告）404 时显示错误卡，提供“返回报告库”“重新加载”；下载按钮只在 ready 状态出现。非核心附属 JSON 缺失仍按旧产物兼容降级。

**树模式 API 只增不改**：仅在任务状态/结束事件添加 `artifact_dir`，保留 `run_dir` 和全部旧字段。

**涉及文件**  
`run_pipeline.py`、`server/bus.py`、`server/routes_runs.py`、`static/js/views/new.js`、`static/js/views/detail.js`、`static/index.html`、`tests/test_server.py`（或新增 `tests/test_v4_workflow.py`）。

**收益与风险**  
消除成功后拿不到报告的 P0 问题。风险是目录清洗后可能同名；时间戳仍参与名称，测试必须覆盖碰撞和超长主题。旧的错误嵌套产物不自动搬迁，避免审查修复顺带改历史文件。

**验收**

- 单测 mock LLM/管线：主题 `中国高纯石英行业（砂/材料）:市场?` 生成后，断言 `Path(artifact_dir).parent == artifacts_root`、目录名不含禁止字符、`GET /api/runs` 可找到、结束事件同时含 `artifact_dir` 与旧 `run_dir`。
- 路由测试：`GET /api/runs/<不存在>` 返回 404；页面将其转成 `[data-testid="detail-error"]`，且不存在 `[data-testid="detail-download"]`。
- 页面 DOM：

```js
console.assert(document.querySelector('[data-testid="detail-error"]'));
console.assert(document.querySelector('[data-testid="detail-back"]'));
console.assert(document.querySelector('[data-testid="detail-retry"]'));
```

### V4-02　把长任务提升为可离开、可恢复、可取消的全局任务（P3、P4）

**现状**  
计划和报告虽由后端任务总线执行，前端却把任务 id、EventSource 和完成跳转绑定在向导组件里。离开页面就失去任务上下文，报告页也看不到进行中任务；回调仍可能在后台触发路由跳转。

**改法**

1. `static/js/store.js` 增加 `activeTasks`，字段固定为 `{id, kind, treeId, treeName, artifactDir, status, stage, message, startedAt, updatedAt, returnRoute}`；仅把 id 和可公开元数据写入 `sessionStorage` 的 `ec.activeTasks.v1`，不存密钥、报告内容和原始数据。
2. `static/js/app.js` 在应用壳渲染 `[data-testid="active-task-banner"]`：展示任务类型、中文阶段、已用时间、“查看”“取消”。进入任意页面都根据 `GET /api/jobs/{id}` 或 `GET /api/runs/{id}` 重连；服务重启导致 404 时标记“任务记录已失效”并允许清除。
3. `static/js/views/new.js` 的 `startPlan()`、`startRun()` 只负责注册任务并更新全局 store。组件卸载时关闭本组件订阅，完成时更新 store 和报告库，不主动劫持当前路由；只有用户仍停留在该向导且没有主动离开，才自动跳详情。
4. 计划进度直接消费现有 `plan/start` SSE；报告进度消费现有 run SSE。`previewPlan()` 改用新增的 `POST /api/trees/{tree_id}/plan/preview/start` 创建后台 job，返回 `{id, events_url}`；保留原同步 `POST .../plan/preview`。新增通用 `GET /api/jobs/{job_id}` 和 `POST /api/jobs/{job_id}/cancel`，或在现有 job 路由上补同义能力，状态结构与 run 一致。
5. `static/js/views/reports.js` 顶部增加“运行中”区，消费同一 store；完成后自动刷新列表，但不强制跳页。所有任务状态卡显示中文阶段、已用时间、最后消息、离开页面仍会继续的说明；运行时锁定会改变请求语义的表单字段。

**树模式 API 只增不改**：新增 preview/start、通用 job 查询/取消；保留同步 preview、原 SSE 和 run 端点。

**涉及文件**  
`static/js/store.js`、`static/js/app.js`、`static/js/views/new.js`、`static/js/views/reports.js`、`server/bus.py`、`server/routes_trees.py`、`server/routes_overview.py`、`server/routes_runs.py`、`static/index.html`、`tests/test_server.py`、新增 `tests/test_v4_tasks.py`。

**收益与风险**  
长任务不再绑死单页，用户能安全离开、回来和取消。主要风险是 SSE 与轮询重复完成事件；store 更新必须按任务 id 幂等，完成跳转最多一次，组件卸载必须关闭订阅。

**验收**

- 单测 mock 一个可控慢 job：start → status running → cancel → cancelled；刷新状态接口的字段稳定，重复 cancel 不抛 500。
- 自动化把 `sessionStorage` 预置一个 running 任务后强刷 `#/reports`，断言任务条出现；模拟 status done 后任务条变为“已完成”且报告列表刷新一次。
- 在生成中从 `#/new` 切到 `#/settings`，触发 done，断言 `location.hash === '#/settings'`；点击任务条“查看”后才进入详情。

```js
console.assert(document.querySelector('[data-testid="active-task-banner"]'));
console.assert(document.querySelector('[data-testid="task-stage"]').textContent.trim());
console.assert(document.querySelector('[data-testid="task-cancel"]'));
```

### V4-03　重排数据步骤，提供已确认计划的聪明缺省（P5）

**现状**  
动作先于所需信息，计划生成后才出现用户原本可以预先填写的上下文；已有计划列表虽被请求，却没有 UI。0 缺口仍要求用户理解“缺口裁决”并手动确认。

**改法**

1. `static/js/views/new.js` 第 ② 步按顺序改为：数据来源与写作意图 → “使用已有计划 / 生成新计划” → 计划覆盖检查 → 预检。资料目录放在生成计划按钮之前，并清楚标为“可选：用于本地 PDF/Excel”。
2. `server/routes_trees.py::tree_plans()` 在每项增量返回 `confirmed`、`gap_count`、`qualitative_count`、`tree_version`、`tree_fingerprint`、`focus`、`folder`、`preview_cached`；读不到旧字段时返回 `null`，不能报错。生成计划时把当时树版本和稳定指纹写入计划 JSON。
3. 前端默认选中“最新、已确认、tree_fingerprint 与当前树一致”的计划；没有匹配项才默认生成新计划。旧版本计划可见但标“树已变化，需重新生成”，不能直接进入下一步。
4. 覆盖检查为 0 缺口时前端自动调用现有 confirm，并显示“所有章节已有数据策略，无需逐项裁决”；有缺口时才显示裁决表。“确认”成功后按钮变为不可重复的“已确认”。
5. 现有“只给意图/文件夹直接生成”的旁路收进“高级：跳过计划”折叠区，默认关闭，并说明不会得到逐节覆盖检查。打开旁路时仍执行现有来源门禁，不能绕过空来源与治理约束。

**树模式 API 只增不改**：计划列表与计划 JSON只新增字段；原文件名、大小、时间和端点保留。

**涉及文件**  
`server/routes_trees.py`、计划生成/持久化所在模块、`static/js/views/new.js`、`static/index.html`、`tests/test_trees.py`（或新增 `tests/test_v4_plans.py`）。

**收益与风险**  
减少重复计划和信息顺序错误；新用户只在确有缺口时学习裁决。风险是误复用旧计划，因此必须以内容指纹而非树名判断，任何不匹配都回到“重新生成”。

**验收**

- 单测准备同树匹配计划、旧版本计划、未确认计划，断言 API 字段和默认选择只命中第一种；旧计划文件缺少新字段时仍能列出。
- 0 缺口计划在 confirm 成功后显示 `[data-testid="plan-confirmed"]` 且按钮 disabled；有缺口仍显示每条裁决控件。
- DOM 顺序断言：

```js
const step = document.querySelector('[data-testid="data-step"]');
const fields = step.querySelector('[data-testid="data-context"]');
const action = step.querySelector('[data-testid="plan-action"]');
console.assert(fields.compareDocumentPosition(action) & Node.DOCUMENT_POSITION_FOLLOWING);
console.assert(step.querySelector('[data-testid="saved-plan-select"]'));
```

### V4-04　把预检变成生成前的可判定门，而非一行回执（P6）

**现状**  
后端预检已返回 `ok`、`n_facts`、`by_source`、`crosscheck`、`sample`、`warnings`、`error`、`cached`，页面只保留前两类信息；数据变动后也没有“预检已过期”概念。

**改法**

1. `static/js/views/new.js` 增加 `previewState`（idle/running/pass/fail/stale）和请求指纹。树、计划、意图、资料目录、裁决任一变化都把已完成预检标 stale。
2. 结果卡固定展示：事实总数、按来源计数、是否命中缓存、交叉校验摘要、警告列表、可折叠的 3 条样例；失败显示错误及“修改数据设置”“重新预检”。内部 source id 映射为中文，技术 id 放 tooltip。
3. `canNext2` 在 preview running/fail/stale 时为 false。正常树模式必须预检 pass 才能继续；若计划所有缺口都被用户明确裁决为定性写作且 confirm 门禁允许 0 事实，则显示二次确认“本报告将不陈述可核验数字”，确认记录进向导状态后方可继续。不能提供通用“仍然继续”绕过来源或计划确认。
4. 第 ③ 步继续展示同一预检摘要，避免用户切步后失去风险信息。

**涉及文件**  
`static/js/views/new.js`、`static/js/fielddict.js`、`static/index.html`、新增 `tests/test_v4_frontend_contract.py`（静态模板/函数契约）及现有树门禁单测。

**收益与风险**  
在最贵的步骤前暴露坏数据，减少无效生成。风险是让纯定性报告无法前进；上述二次确认只对“所有缺口已明确裁决为定性”开放，并复用后端既有门禁。

**验收**

- mock preview 分别返回 pass、`ok:false`、0 facts + 全定性三组；断言只有 pass 与经过二次确认的全定性可使“下一步”启用。
- 修改资料目录后断言原 pass 卡变成“需重新预检”，下一步 disabled。

```js
console.assert(document.querySelector('[data-testid="preview-facts"]'));
console.assert(document.querySelector('[data-testid="preview-crosscheck"]'));
console.assert(document.querySelector('[data-testid="preview-warnings"]'));
```

### V4-05　补齐长任务前的最终复核与模型档位（P7）

**现状**  
最终页的信息不足以识别选错树、旧计划或数据风险；后端 `TreeRunIn.model_tier` 已有能力却没有前端入口。

**改法**

1. `static/js/views/new.js` 第 ③ 步增加只读确认卡：树名/id/版本/章节数/lint 中文摘要、计划生成时间/是否确认、数据来源、预检事实数/警告、预计 5–10 分钟。
2. 增加模型档位单选：“自动（推荐）”“快速”“质量优先”，值必须来自后端允许列表，不能在前端另造枚举。`startRun()` 显式发送 `model_tier`；默认值沿用后端当前默认。
3. `server/routes_settings.py` 的 pipeline/about 响应增量返回 `model_tiers: [{value,label,description,is_default}]`，或新增只读 capability 端点；未提供时前端只显示“自动”，保证旧后端可用。
4. 任一上游树/计划/预检发生变化时清空最终确认；按钮运行中禁用并交给 V4-02 任务卡。

**树模式 API 只增不改**：仅新增能力描述；运行请求使用已存在的 `model_tier` 字段。

**涉及文件**  
`static/js/views/new.js`、`server/routes_settings.py`、`static/index.html`、`tests/test_server.py`、`tests/test_v4_frontend_contract.py`。

**收益与风险**  
减少长任务前的误选，并把已有性能/质量能力暴露为可理解选择。风险是前后端档位漂移，因此以后端 capability 为唯一来源。

**验收**

- API 单测断言默认档位恰有一个，value 均能被 `TreeRunIn` 接受。
- mock `/api/runs/from_tree`，在 UI 选“质量优先”后断言请求体含对应 `model_tier`，并含树/计划原字段。
- DOM 中 `[data-testid="final-review"]` 同时包含树名、版本、章节数、事实数和“5–10 分钟”。

### V4-06　统一结构树入口，保存后一步进入报告向导（P8）

**现状**  
向导和模板页各有一套“无树对话生成”，编辑页却没有写报告入口，用户必须跨页重新定位同一棵树。

**改法**

1. 导航与页标题把“模板”改为“结构与文风”，实体统一称“结构树”；路由 `#/templates` 保持不变。
2. `static/js/views/templates.js` 在已选树顶部放主按钮“用这棵树写报告”，跳 `#/new?tree=<encoded id>`；保留有选中树时的对话改树。无选中树时不再复制一套生成对话，改成说明和“对话生成新结构”链接 `#/new?start=chat`；“新建空白结构”继续作为高级入口。
3. `static/js/app.js` 的 hash 查询解析保留 `tree`、`start`；`static/js/views/new.js` 在 mount 时校验 tree id 存在，存在则直接选中并展示已实现的结构摘要，不存在则显示可恢复错误且回到三起点。`start=chat` 只展开对话起点，不自动发请求。
4. 模板页空态改成真实说明，删除“这里出现编辑器与生成入口”这类与实际不符的文案。

**涉及文件**  
`static/js/app.js`、`static/js/views/new.js`、`static/js/views/templates.js`、`static/index.html`、`tests/test_v4_frontend_contract.py`。

**收益与风险**  
把“造树”和“改树”各归一个主要入口，编辑后少走一次选择器。风险是旧用户习惯在模板页造新树；保留一键深链且不改原路由，功能仍可达。

**验收**

- 从模板页选择树 id `x`，点击 `[data-testid="use-tree"]`，断言 hash 为 `#/new?tree=x`，向导结构摘要显示同一 id 且停在第 ① 步可继续。
- 访问不存在 id，断言出现 `[data-testid="tree-link-error"]`，没有请求计划或生成。
- 模板页无选中树时只有一个“对话生成新结构”入口；选择树后仍有“继续调整”对话能力。

### V4-07　建立报告完成后的“补数据 / 改结构 / 复用数据再写”回环（P9）

**现状**  
详情页能诊断问题，却不能把诊断带回生成流程；元信息也不足以安全复用同次数据。

**改法**

1. `run_pipeline.py` 写 `meta.json` 时增量记录 `tree_id`、`tree_version`、`plan_file`、`plan_fingerprint`、`facts_count`、`data_generated_at`；旧产物缺字段时前端按不可复用显示。
2. `static/js/views/detail.js` 在顶部问题条、治理页和元信息页共用三种操作：
   - “补数据再生成”：`#/new?tree=...&plan=...&mode=data`，落到第 ② 步，保留树并要求重新预检；
   - “先改结构”：`#/templates?tree=...`，直接选中原树；
   - “按本次数据再写一版”：仅当 facts 文件、tree/plan 指纹仍可验证时显示，进入第 ③ 步复核，绝不自动开始。
3. `server/routes_runs.py::TreeRunIn` 新增可选 `reuse_data_from`，只接受 `_safe_dir()` 验证过的直接子目录；转换为管线已有 `--reuse-data` 参数。后端重新验证树、计划、facts 存在及来源门禁，不能由前端布尔值直接放行。
4. `static/js/views/new.js` 解析 plan/mode/reuse 查询参数后逐项向 API 校验；显示原数据日期、事实数、是否过期。任何校验失败都回到第 ② 步，并说明需要重新预检。

**树模式 API 只增不改**：`TreeRunIn` 只新增可选字段，`meta.json` 只新增键；经典模式不接入该回环。

**涉及文件**  
`run_pipeline.py`、`server/routes_runs.py`、`static/js/app.js`、`static/js/views/new.js`、`static/js/views/detail.js`、`static/js/views/templates.js`、`static/index.html`、`tests/test_server.py`、新增 `tests/test_v4_rerun.py`。

**收益与风险**  
把详情诊断变成下一步行动，显著缩短再改再生成路径。风险是复用陈旧数据，因此必须以后端文件、树和计划指纹三重校验；前端深链只负责预填。

**验收**

- 旧产物无新 meta 字段时能正常打开且不显示“按本次数据再写一版”。
- mock LLM 的 API 单测覆盖：合法直接子目录可复用；嵌套路径、`..`、树指纹不符、facts 缺失均 4xx；经典 `/api/runs/start` 契约不变。
- 点击三种 CTA 分别断言目标 hash 与预填状态；所有路径最终“开始生成”前仍需用户点击。

### V4-08　让反馈轮次呈现当前状态，并阻止重复回滚（P10）

**现状**  
反馈治理结果、深度评审和页头结论各自分离，用户无法判断当前版本是否改善。回滚操作可对同一目标重复执行，账本出现无意义记录。

**改法**

1. `static/js/views/detail.js` 使用单一 `currentJudge`：初始从 `judge_report.json` 加载，深度评审完成后用任务结果替换并渲染；页头明确标“当前评审”，初始值需要保留时放入“初始评审”折叠信息。
2. 反馈 apply/rollback 完成后重新加载 meta、rounds、当前治理结果和预览；最新轮次结果卡展示 reconcile/validate 的中文结论与“继续提意见 / 深度评审 / 重新生成”动作。深度评审仍由用户显式触发，避免自动增加 LLM 成本。
3. `pipeline/feedback.py::rounds()` 为每条记录增量返回 `kind`、`target_round`、`rolled_back`；`rollback()` 若目标本身是 rollback、目标已被有效回滚或轮次不存在，返回 409 和明确错误，不追加账本。并发双击需以锁或写前复查保证只追加一次。
4. 轮次 UI 仅在 `kind === 'apply' && !rolled_back` 时显示“回滚此轮”，执行中禁用。diff 两栏加可见标题“修改前”“修改后”，颜色只作辅助，操作名用中文，原始 op 放详情折叠。

**树模式 API 只增不改**：反馈轮次响应新增状态字段；原字段保留。经典报告若无 rounds 继续显示空态。

**涉及文件**  
`pipeline/feedback.py`、`server/routes_runs.py`、`static/js/views/detail.js`、`static/js/fielddict.js`、`static/index.html`、`tests/test_feedback.py`。

**收益与风险**  
用户能理解“现在这版”而不是初始快照，账本保持可审计。风险是旧账本已有重复回滚；读取时应确定性归并展示，不能改写历史文件。

**验收**

- `tests/test_feedback.py` 新增同一轮连续回滚两次：第一次成功且只新增一条，第二次 409，账本行数不变；回滚记录本身不可回滚。
- mock 深评任务返回新分数 37/PASS 后，DOM 页头出现 37 和“已达标”，旧 23 仅在初始折叠区。
- 每个 diff 容器都可查询 `[data-testid="diff-before"]` 与 `[data-testid="diff-after"]`；已回滚轮无 rollback 按钮。

### B. 交互体验类

### V4-09　建立统一的用户术语层和就地解释（P11）

**现状**  
同一概念在导航、页面和数据字段中使用不同名字，大量实现字段直接暴露，README 中的解释没有进入操作现场。

**改法**

1. 重写 `static/js/fielddict.js` 为共享 `EC.terms`，每项结构固定 `{label, shortHelp, technical}`，至少覆盖：结构树=报告提纲与每节写作规则、缺口裁决=决定缺数据章节如何处理、预检=生成前验证可用事实、文风卡=报告语气与格式规则、lint=结构检查、judge=报告评审、reconcile=数字对账、validate=规则校验，以及治理维度、章节类型、renderer、来源类型。
2. 新增无构建的 `TermHelp` 小组件或 `term-help` 指令：页面显示中文 label，问号/tooltip 展示 shortHelp 和技术键；技术键不再作为主文案。
3. `new.js`、`reports.js`、`detail.js`、`templates.js`、`settings.js` 全部通过词典显示术语。具体映射必须包括 `PASS→已达标`、`FAIL→需修改`、`0E/0W→0 个错误 / 0 个提醒`、`tree→结构树流程`、`length→篇幅完整性`、`data_support→数据支撑`、`professionalism→专业度`、`compliance→合规与引用`、`readability→可读性`、`risk_warning→风险提示`。
4. 设置页保留 `base_url`、模型 id 等技术键，但中文标题在前，原键放帮助文本；删除用户可见的 `M2`、`M8-R1` 里程碑术语。

**涉及文件**  
`static/js/fielddict.js`、`static/js/app.js`、`static/js/views/new.js`、`static/js/views/reports.js`、`static/js/views/detail.js`、`static/js/views/templates.js`、`static/js/views/settings.js`、`static/index.html`、`tests/test_v4_frontend_contract.py`。

**收益与风险**  
首次使用者不需先阅读工程文档。风险是翻译掩盖排障字段；tooltip 保留 technical，文件与 API 数据不改名。

**验收**

- 静态契约测试加载 `fielddict.js`，断言上述键均有非空中文 label/help，所有引用键都存在。
- 强刷四个顶层页面和详情治理页，主可见文本不出现孤立的 `PASS`、`FAIL`、`judge`、`0E`、`0W`、`M8-R1`；tooltip 中仍能查询技术键。

### V4-10　让结构树可找到、可读，并把高级字段渐进展开（P12）

**现状**  
重名树难以定位，编辑表单没有稳定 label；普通用户一进入就面对 renderer、source prefix 等实现细节，新建时还要手工造 id。

**改法**

1. `static/js/views/templates.js` 左栏增加名称/id 搜索、lint 状态筛选、更新时间排序；每行固定显示名称、短 id、版本、章节数和中文结构检查结果。重名时版本与 id 始终可见。
2. 编辑区分“常用设置 / 高级设置”。常用设置包含树名、主题、文风卡、章节标题、写作目标、是否必需；章节类型使用中文解释。高级区才放 renderer、table id、source prefix、min/max 字数等，默认折叠并记住本会话选择。
3. 所有输入建立真实的 `<label for>` 或 naive-ui 等价 label 关系；动态章节控件的 id 包含稳定 section id。保存/切换树前若有脏数据，弹出“保存 / 放弃 / 留在这里”，不能静默覆盖。
4. 新建弹窗只要求“名称、报告主题”，前端由中文名称生成候选 id，后端最终校验并在冲突时追加短序号；“自定义 id”放高级区。移除 `M2 起支持` 文案。
5. `KINDS`、`RENDERERS` 的值保持不变，仅通过 V4-09 词典显示。

**树模式 API 只增不改**：若自动 id 需后端支持，只新增“id 可省略/建议 id”能力；原显式 id 创建继续工作。

**涉及文件**  
`static/js/views/templates.js`、`static/js/fielddict.js`、`server/routes_trees.py`、`static/index.html`、`tests/test_trees.py`、`tests/test_v4_frontend_contract.py`。

**收益与风险**  
常见编辑不再被内部字段干扰，重名树仍能准确选择。风险是高级字段折叠导致遗漏；已有非默认值时折叠标题显示“已配置 N 项”，保存载荷必须与改造前深度相等。

**验收**

- DOM 为每个可编辑 input 断言可由 `label[for]` 或 `aria-labelledby` 定位；axe/等价脚本不得报告无名称输入。
- 构造两个同名树，搜索后两行均可见且版本/id 不同；lint 筛选结果正确。
- 展开/收起高级区前后序列化 save payload 深度相等；已有 renderer/source prefix 不丢失。

### V4-11　修复对话空提交，并把追问焦点送回回答区（P13）

**现状**  
空输入按钮可点但处理函数静默 return；LLM 追问是连续文本，返回后键盘焦点仍在提交按钮。

**改法**

1. `static/js/views/new.js` 与 `static/js/views/templates.js` 的对话提交按钮在 `trim()` 为空或请求中时 disabled；输入区下方常驻一句示例，不依赖点击后报错。
2. 处理函数仍保留防御校验，空值时显示“请先描述要写的报告或调整要求”，供程序调用/竞态兜底。
3. 返回内容若是问题列表，按换行/编号保守分段显示，不重写 LLM 原意；结果需要用户回答时，将 focus 移到输入框并把按钮文字改成“发送回答”。成功生成树时 focus 移到结果卡标题。
4. 加 `aria-live="polite"` 的状态区播报“正在生成 / 需要补充信息 / 已生成”。

**涉及文件**  
`static/js/views/new.js`、`static/js/views/templates.js`、`static/index.html`、`tests/test_v4_frontend_contract.py`。

**收益与风险**  
消除“点了没反应”，键盘用户也能顺着追问完成。风险是错误分割普通段落；只在明确编号/多行问号时分段，否则原样展示。

**验收**

- 输入 `''` 和纯空格时，两页按钮均 disabled；直接调用真实 handler 时能查询到校验文本。
- mock LLM 返回三条追问，断言 3 个列表项且 `document.activeElement` 为输入框；mock 成功树时焦点为结果卡标题。

### V4-12　恢复设置页适配器列表，并隔离各设置卡加载失败（P14）

**现状**  
请求成功但变量没有从 `setup()` 返回；另外当前聚合 `load()` 中任一请求失败容易让整页缺少反馈。

**改法**

1. `static/js/views/settings.js::setup()` 明确 return `adapters`、`adaptersLoading`、`adaptersError`、`reloadAdapters`，修复模板作用域。
2. `load()` 用独立 try/catch 或 `Promise.allSettled()` 加载模型、管线、适配器、about；每张卡维护自己的 loading/error，失败卡显示原因和“重试”，其他卡照常使用。
3. 适配器空数组显示“当前没有可用适配器”，请求失败显示“加载失败”，二者不能共用空态。名称使用中文说明，技术 id 通过 V4-09 tooltip 保留。

**涉及文件**  
`static/js/views/settings.js`、`static/js/fielddict.js`、`static/index.html`、`tests/test_v4_frontend_contract.py`。

**收益与风险**  
恢复实际已有的 14 个能力项，单个接口故障不再让整页难以判断。改动仅前端响应式暴露，风险低。

**验收**

- mock adapters 返回 14 条，断言 `[data-testid="adapter-list"]` 有 14 行。
- mock adapters 500 而 model 200，断言适配器卡有 `[data-testid="adapter-error"]`，模型表单仍可见；重试成功后错误消失。

### V4-13　让报告与文件围绕“找报告、拿成品、继续处理”组织（P15、P16）

**现状**  
报告库难以从大量失败记录中定位目标；详情文件页优先级平铺，交付物被内部文件淹没。

**改法**

1. `static/js/views/reports.js` 增加标题/目录关键词搜索、治理状态筛选（全部/需修改/已达标/未评审）、流程筛选（全部/结构树/经典），条件组合在前端完成，保留当前分页。筛选结果数与总数同时显示。
2. 把 `judge` 列名改“报告评审”，`PASS/FAIL` 改“已达标/需修改”，`tree` 改“结构树流程”；原始值留在数据属性/tooltip。目录名不是标题时优先显示 meta 中的 subject/name，目录短名作为次要信息。
3. 接入 V4-02 的运行中区；完成、失败、取消均有明确状态与入口。失败任务保留“查看错误/重新开始”，不能混成报告产物。
4. `static/js/views/detail.js` 文件页按规则分组：最上方“可交付文件”（`final.html`、`final.docx`、最终图表）和主下载按钮；“生成与审计记录”默认折叠，包含 facts、judge、ledger、rounds 等。分组只看受控文件名/相对路径，不改变 `list_files()` 返回和下载安全校验。

**涉及文件**  
`static/js/views/reports.js`、`static/js/views/detail.js`、`static/js/fielddict.js`、`static/index.html`、`tests/test_v4_frontend_contract.py`。

**收益与风险**  
高频目标从“读内部状态”变为“找到报告并继续处理”。风险是旧产物命名不一致；无法识别的文件一律归入审计记录而不是隐藏。

**验收**

- 用现有 64 条响应 fixture，搜索“石英”后每行标题或目录均命中；筛“需修改”后无 PASS；清空条件恢复 64 条。
- 文件 fixture 同时含 final、facts、rounds 和未知文件：final 只在 `[data-testid="deliverables"]`，其余均在 `[data-testid="audit-files"]`，未知文件仍可展开下载。
- 旧产物详情的预览、反馈、治理、文件、元信息五页签均可切换且无 console error。

## 二、总验收

### 2.1 逐项硬门与可查证据

编码 agent 必须逐行填写“证据位置”和结果；任一行为未满足不得标完成。截图只能辅助，不能替代 DOM/API/测试断言。

| 修改项 | 硬门 | 必交证据 |
|---|---|---|
| V4-01 | 含 `/\\:*?` 的主题成功生成到 `artifacts/` 直接子目录；报告库可见；错误详情有返回/重试 | `pytest` 用例名 + 生成目录断言 + `[data-testid="detail-error"]` DOM 输出 |
| V4-02 | plan/preview/run 三类任务均可跨页查看；run 和 job 均可取消；完成不劫持当前页 | API 状态迁移测试 + 从 new 切 settings 的 hash/DOM 自动化日志 |
| V4-03 | 数据上下文先于动作；匹配且已确认计划才自动复用；0 缺口不重复确认 | 计划 fixture API 输出 + DOM 顺序/默认选择断言 |
| V4-04 | 预检失败/过期不能下一步；全定性例外必须经过既有裁决和二次确认 | 三组 mock preview 测试 + `canNext2` 状态矩阵 |
| V4-05 | 最终卡信息完整，模型档位来自后端且请求体实际携带 | capability/API 单测 + from_tree 捕获请求体 |
| V4-06 | 模板选树后一键带树进入向导；无效深链可恢复；模板页不再重复无树生成器 | 两条 hash 导航自动化 + 页面按钮枚举 |
| V4-07 | 三个再处理入口均正确预填但不自动生成；复用数据后端严格校验 | 深链 DOM 状态 + 合法/非法 `reuse_data_from` 单测 |
| V4-08 | 当前评审可更新；同一轮只可回滚一次；diff 有文字标签 | feedback 账本行数单测 + 深评 DOM + diff DOM |
| V4-09 | 主界面无未解释的核心英文术语，技术键仍可查 | 词典完整性测试 + 四页强刷文本/tooltip 断言 |
| V4-10 | 重名树可区分；控件可访问；高级字段折叠前后不丢 | 重名 fixture、label 扫描、save payload 深比较 |
| V4-11 | 空对话不可提交；追问返回后焦点回输入框 | disabled、handler 防御、activeElement 三项断言 |
| V4-12 | 14 个适配器可见；单接口失败不影响别的设置卡 | 成功/失败两组 mock DOM 测试 |
| V4-13 | 报告组合筛选正确；最终成品与审计文件分组且文件不丢 | 64 条 fixture 筛选断言 + 文件集合守恒断言 |

### 2.2 自动化与存量回归

1. 必须执行 `python -m pytest tests/ -q`。当前审查基线为 98 项全绿；实施后所有 98 项必须继续通过，新增测试另计。所有 LLM、联网、文件采集和长任务测试均 mock，自动化测试禁止真实调用 LLM。
2. 新增测试至少覆盖以下状态矩阵：
   - 任务：queued/running/done/failed/cancelled/服务重启后丢失；
   - 计划：匹配/树已变化/未确认/0 缺口/有缺口；
   - 预检：running/pass/fail/stale/0 facts 全定性；
   - 详情：新产物/旧产物/不存在/附属文件缺失；
   - 反馈：apply/rollback/重复 rollback/deep judge。
3. 浏览器回归使用固定 fixture 或 mock API，逐页强刷并断言 `window` 无 `error`、`unhandledrejection`，浏览器 console 无 error：
   - `#/new`；
   - `#/reports`，并从中进入一个旧产物 `#/reports/<目录>` 验证五页签；
   - `#/templates`；
   - `#/settings`。
4. 旧产物兼容硬门：至少选择一个无 V4 新 meta 字段的现有目录，验证预览可加载、HTML/DOCX 下载地址可用、反馈页与治理页可打开、文件集合没有减少。
5. 树模式 API 的所有变更做向后兼容契约断言：旧请求体仍为 2xx，旧响应字段值和类型不变；新增字段允许旧前端忽略。经典模式 `/api/runs/start`、经典产物读取及其现有测试不得改变。
6. 前端自动化不得只调用内存函数断言状态；每项至少保留一个最终 DOM 断言。可用 `EC._newView`、`EC._detailView`、`EC._templatesView` 驱动真实 handler，但验收点必须落到用户可见 DOM。

### 2.3 人工回归清单

- `#/new`：三张起点卡均可进入和返回；示例复制、对话新建、现有树三条分支不回退；结构摘要、lint、计划、预检、最终复核和任务恢复连贯。
- `#/reports` 与旧产物详情：搜索/筛选/分页可组合；预览、反馈、治理、文件、元信息五页签均可用；错误详情能退出。
- `#/templates`：树搜索与重名辨识、常用/高级编辑、对话改同一棵树、文风卡新建/编辑/复制/删除均不回退；保存后可一键进向导。
- `#/settings`：模型、流水线、14 个适配器和 about 独立加载；任一卡失败时其他卡仍可操作。
- 全程观察：无 console error、无未处理 Promise、无完成事件导致的意外跳页；强刷后 active task 可恢复或明确显示已失效。

## 三、工程硬约束

以下约束原样进入实施任务，属于硬门：

1. **免构建链禁 node/npm/打包器；Vue3 + naive-ui 运行时模板编译，**继续使用当前 CDN 与 `static/js/*.js` 组织，不引入 TypeScript 编译、SFC 编译、Vite、Webpack、Rollup、Babel 或任何生成目录。
2. 修改任一静态 JS 后必须同步更新 `static/index.html` 对应 `?v=` 缓存版本；Vue 模板字符串内需要换行时使用 `\\n`，不得把非法字面换行写进普通 JavaScript 字符串。
3. 前端以实际 API 契约为准，不复制后端业务规则；来源门禁、计划确认、路径安全、树/计划指纹和复用数据校验必须由后端最终裁决。
4. **树模式 API 只增不改**：允许新增可选请求字段、新响应字段或新端点；不得删除、改名或改变既有字段类型/语义。经典模式保持冻结，除通用只读展示修复外不改其生成流程。
5. 评审、逐数对账、引用、缺口裁决等治理门禁不得因“跳过”“复用”“继续生成”按钮被绕过；任何聪明缺省都必须可见、可撤销，并在服务端重新验证。
6. API key 只在设置接口和现有受控配置中处理；前端 store、sessionStorage、日志、DOM、错误消息、测试 fixture 和报告 meta 均不得写入明文 key。现有掩码行为不得回退。
7. 所有新增/修改文本文件使用 UTF-8；Windows 开发命令使用 PowerShell 7。不得引入仅在 Bash 下可运行的测试或脚本。
8. 自动化测试中的 LLM 调用必须 mock（沿用项目现有 `pipeline.llm.chat_json` 等 patch 方式），不得在 pytest、浏览器回归或 CI 中访问真实模型、联网检索或真实付费接口。
9. 不用视觉间距、配色、动效或响应式调整替代本方案中的流程与反馈验收；新增样式只服务于状态、层级、可访问名称和错误出路。
10. 当前 `python -m pytest tests/ -q` 基线 98 项全绿；任何存量测试失败均视为方案实施未完成，不能以修改/删除旧断言规避兼容要求。
11. 调 `server/bus.start_run/start_job` 的端点必须 `async def`（同步 def 在线程池里没有 running loop 会 RuntimeError）。
12. vue.prod 运行时编译器会把 v-for 卡片内的结构性 `<template v-if>` 连同后续兄弟节点丢弃——一律用 `<div v-if>` 包装（具名插槽 `<template #xxx>` 不受影响）。
13. 浏览器自动化验收对 naive-ui 组件：合成 click 常无效，用 pointerdown+mousedown+pointerup+mouseup+click 全套派发或原生 click() 兜底；IAB webview 间歇 rAF 挂起导致弹层卡 opacity 0 时，对卡死弹层清除 `fade-in-scale-up-transition-*` 过渡类即可恢复，截图表面超时时改走 DOM 断言。

## 四、非目标（本批次不做）

- 视觉打磨：间距、配色、动效、响应式布局。
- 引入前端构建链或新依赖库（新组件一律用现有 CDN 全局组件组合）。
- 改动流水线评审/对账算法（judge、reconcile、validate 的评分与门禁逻辑不在范围；本批只动工作流、任务模型与界面）。
- 迁移或清理历史错误嵌套产物（V4-01 只杜绝新增，不回改旧目录）。
- 多用户、鉴权、国际化。
- 经典模式（`config/report_types/`）除只读展示修复外的一切改动。

## 五、实施顺序与依赖

六批依次执行，每批实现→自测→过该批所含项的验收后才进下一批：

| 批 | 项 | 说明 |
|---|---|---|
| ① 独立修复 | V4-01、V4-08、V4-11、V4-12 | 互不依赖，风险低，先落袋（含两个 P0 与三个实锤 bug） |
| ② 术语与树管理 | V4-09、V4-10 | 术语层是 12/13 显示映射与 10 表单文案的公共依赖 |
| ③ 全局任务中心 | V4-02 | 最大项：store/app.js 任务条/新增 job 端点；preview 后台化在此并入 |
| ④ 向导数据步骤 | V4-03、V4-04、V4-05 | 同属 new.js 第②③步，建立在 ② 批的任务模型与 ③ 批的 preview job 上 |
| ⑤ 跨页回环 | V4-06、V4-07 | 深链参数解析（tree/start/plan/mode）先落 06，07 复用 |
| ⑥ 报告库与文件页 | V4-13 | 消费 ③ 批的运行中区；完成后做总验收 |

依赖要点：09 → 10/12/13；02 → 03/04/05/13；06 → 07。方案与代码现实冲突时：以代码现实为准做最小调整，在附录 B 记偏差与理由；规格未覆盖的细节按"先打通"原则取最简实现，不得自行扩范围，两难时选保守路线并标注"待用户裁决"。

---

## 附录 A · 逐项完成核对表（随实施回填）

| 项 | 内容 | 状态 | 证据 |
|---|---|---|---|
| V4-01 | 产物目录名安全+详情错误出路 | ✅ 完成 | `tests/test_v4_batch1.py` 5 项（test_safe_artifact_name_strips_forbidden / test_end_event_carries_artifact_dir_and_run_dir / test_summarize_carries_artifact_dir / test_history_finds_sanitized_dir_and_api_404）；DOM：不存在目录详情页 `[data-testid="detail-error"]`"报告打不开"+ detail-back/detail-retry 出现、detail-download 不存在；点击返回按钮 hash→`#/reports` |
| V4-02 | 全局任务中心（可离开/恢复/取消） | ✅ 完成 | API：`tests/test_v4_tasks.py`（test_job_status_and_cancel_lifecycle 慢 job running→cancel→cancelled、重复 cancel 200 不 500、状态字段稳定 / test_job_unknown_404 / test_preview_start_background 后台预检+同步 preview 缓存命中 / test_plan_start_contract_unchanged）；DOM：预置 running 强刷 #/reports 任务条+运行中区出现（阶段/已用/取消/查看/离开提示），假 id 重连 404 →"任务记录已失效"+清除；模拟 done → 任务条"已完成"、报告列表自动刷新 1 次（performance 计数 +1）；#/#settings 下 done → hash 不变，点"查看"→ 进详情 |
| V4-03 | 数据步骤重排+已确认计划复用 | ✅ 完成 | API：`tests/test_v4_batch4.py`（test_tree_plans_enriched_fields：confirmed/gap_count/qualitative_count/tree_version/tree_fingerprint/folder/preview_cached 全命中、旧计划缺字段→null 不报错 / test_plan_start_snapshots_tree_fingerprint：出计划写入树版本+指纹+focus）；DOM：`[data-testid="data-context"]` 在 plan-action 前（compareDocumentPosition=true）、saved-plan-select 存在；旧计划（无指纹）不被自动复用（默认"生成新计划"，保守正确）；0 缺口计划选中即自动确认（plan-confirmed 标签+确认按钮消失） |
| V4-04 | 预检为生成前硬门 | ✅ 完成 | DOM：真实预检（缓存命中）→ pass（preview-facts"事实 80 条"/preview-crosscheck/preview-warnings 齐）→ canNext2=true；修改意图 → stale 标签 + canNext2=false；状态矩阵注入 fail/running→false、pass→true；预检前 idle 态 canNext2=false（必须 pass 才能进第③步）；全定性 0 事实二次确认 dialog 已实现（路径：0 事实+全裁决→确认后 pass），0 事实分支留待用户复验 |
| V4-05 | 最终复核卡+模型档位 | ✅ 完成 | API：`tests/test_v4_batch4.py::test_pipeline_capability_model_tiers`（默认档恰一、value 非空、无密钥泄露）；DOM：final-review 卡含树名/版本/9 节/预检事实 80 条/警告 5 条/"5–10 分钟"/计划已确认；档位单选"自动（推荐）/快速/质量优先"（后端允许列表），选"质量优先"→ from_tree 请求体实测 model_tier:"quality" 且含 tree_id/plan 原字段 |
| V4-06 | 树入口统一+带树深链进向导 | ✅ 完成 | DOM：导航与页题"结构与文风"；模板页选树点 `[data-testid="use-tree"]` → hash=`#/new?tree=<编码id>`、向导选中同一树、停第①步、结构摘要可见；无效树深链 → `[data-testid="tree-link-error"]`+回三起点+零计划/生成请求（plans 空、run.id 空）；`#/new?start=chat` → 对话起点展开不自动发请求；未选中树时模板页无第二套生成对话（chat-send=0），只有"对话生成新结构"入口+新建空白（高级）；空态改真实说明 |
| V4-07 | 报告完成后的回环（补数据/改结构/复用） | ✅ 完成 | API：`tests/test_v4_batch5.py`（reuse_data_from 合法直接子目录→200 且 argv 含 --reuse-data；嵌套/..→403；缺 facts→422；缺 plan→422；经典 /api/runs/start 契约不变）；DOM：旧产物（无 V4 meta 字段）CTA 区仅"补数据再生成/先改结构"无"按本次数据再写一版"；点 cta-redraft → #/new?tree=..&mode=data 落第②步；cta-restructure → 模板页选中原树；cta-reuse（注入 meta 字段后）→ 第③步 + reuse-info（复用目录/事实数/生成日期）+ "手动开始"提示 |
| V4-08 | 反馈轮次当前状态+回滚防重 | ✅ 完成 | `tests/test_v4_batch1.py` 4 项（test_rollback_twice_second_conflicts 账本行数不变 / test_rollback_api_returns_409 / test_rounds_fields_and_legacy_merge 旧账本归并不改写 / test_rounds_empty_dir_ok）；DOM（稀土产物 6 轮）：第 1 轮有"回滚此轮"、第 2 轮已回滚无按钮、第 3~6 条显示"↩ 已撤销第 2 轮"无按钮；diff 弹层 `[data-testid="diff-before"]`=修改前 / `[data-testid="diff-after"]`=修改后；页头注入深评结果后"当前评审 37 分 已达标"，旧 23 分仅在"初始评审"折叠区 |
| V4-09 | 统一术语层+就地解释 | ✅ 完成 | 静态契约：`tests/test_v4_batch2.py`（test_terms_dict_complete 28 键非空中文 label/shortHelp / test_pages_no_bare_jargon 五页无 PASS/FAIL/M2 起/M8-R1/0E/0W / test_termhelp_registered_and_used）；DOM：四页+详情治理页强刷 innerText 无孤立 PASS/FAIL/judge/0E/0W/M8-R1；TermHelp tooltip 实测含技术键（"结构完整　scores.structure / 章节齐全…"）；设置页配置键行保留（base_url 等） |
| V4-10 | 树可找到可读+高级字段渐进展开 | ✅ 完成 | API/契约：`tests/test_v4_batch2.py`（test_create_tree_without_id_and_conflict_suffix 纯中文→tree_ 前缀、显式冲突 409 / test_list_trees_contract_fields / test_frontend_templates_contract）；DOM：搜索"V410 重名"两行同显且 id（v410 / v410-2）与版本可辨，lint 筛选 ok→18/28 棵；冲突短序号实测（连建 3 棵同名 ASCII→id→id-2→id-3→id-4）；折叠前后 saveTree 载荷 JSON 串严格相等且 facts_rows/source_prefix 保留；脏数据切树弹"保存并切换/放弃修改/留在当前"三选一；17/17 可见输入有 aria-label（input-props 透传） |
| V4-11 | 对话空提交修复+焦点管理 | ✅ 完成 | DOM（#/new 对话起点）：chat-send 空/纯空格均 disabled；直调 sendChat 空值出 toast"请先描述要写的报告或调整要求"且不追加消息；mock 追问返回后 `[data-testid="chat-question-0/1/2"]` 3 条列表项、`document.activeElement`=#ec-chat-input textarea、按钮文案变"发送回答"、状态区"需要补充信息"；mock 成功树后结果卡出现且 `document.activeElement`=#ec-result-title；templates.js 同款 disabled/防御/aria-live 同步落地 |
| V4-12 | 设置页适配器修复+加载隔离 | ✅ 完成 | DOM（#/settings）：正常加载 `[data-testid="adapter-list"]` 14 行、无错误卡；mock adapters 500 后 `[data-testid="adapter-error"]`"适配器加载失败：HTTP 500"+ 模型表单仍可见；点错误卡内"重试"→ 错误消失、14 行回归 |
| V4-13 | 报告库筛选+文件页成品分组 | ✅ 完成 | DOM：搜索"石英"→ 显示 30/64 且逐行命中；组合"石英+需修改"→ 20 条全 fail 命中；"石英+已达标"→ 4 条无 fail；流程筛选 classic=49 全经典 / tree=15 全树模式；清空恢复 64/64；`[data-testid="filter-count"]` 实时计数；详情文件页：可交付文件 `[data-testid="deliverables"]` 3 项（final.html/final.docx/chart png）+ 主下载按钮置顶，审计区默认折叠、展开 `[data-testid="audit-files"]` 26 项（facts/评审/轮次），3+26=29 集合守恒 |

## 附录 B · 实施记录（随实施追加）

含：每批改动摘要、偏差与理由（方案↔代码现实冲突处）、待用户裁决项、真实联跑记录（时间/参数/产物目录）。

### 批①（V4-01/08/11/12）

**改动摘要**
- V4-01：`run_pipeline.py` 新增 `_safe_artifact_name()`（清洗 Windows 禁止字符/路径分隔符/控制字符→`-`、合并连续、去尾空点、UTF-8 ≤160 字节、空回退 `report`），run_dir 用清洗名+时间戳，展示标题不变；`server/bus.py` 的 end 事件与 `summarize()` 增量加 `artifact_dir`（basename，`run_dir`/`dir` 保留）；`new.js::goDetail` 优先 `artifact_dir`、单次 `encodeURIComponent`；`detail.js` 引入 `loadState/loadError`，meta 404 → 错误卡（detail-error/back/retry），下载按钮仅 ready 态（detail-download）。
- V4-08：`feedback.py` 新增 `RollbackConflict`+`assert_rollback_allowed()`（不存在/已被回滚/是回滚记录/无快照四类冲突），`rollback()` 写账本前复查；`rounds()` 增量返回 `kind`/`target_round`/`rolled_back`（由 rollback 记录推导，旧重复回滚照实展示不改写）；`routes_runs.py::feedback_rollback` 预检冲突→409；`detail.js` 单一当前评审（深评结果直接更新页头 judge，深评前分数入 `initialJudge` 折叠区），轮次行按 kind/rolled_back 控制回滚按钮，diff 两栏加"修改前/修改后"文字标签。
- V4-11：new.js/templates.js 对话提交按钮 `chatBusy||!trim()` 禁用，handler 空值 toast"请先描述要写的报告或调整要求"，追问按条目分段（`chat-question-N`），追问后焦点回输入框+按钮转"发送回答"，成功生成树焦点到结果卡标题（#ec-result-title，tabindex=-1），两页各加 `aria-live="polite"` 状态区（正在生成/需要补充信息/已生成）。
- V4-12：settings.js 五卡拆分独立加载（loadModel/Pipeline/Conns/Adapters/About + Promise.allSettled），各卡独立 error+重试；`setup()` 补 return `adapters` 等全套；适配器卡改为原生表格三态（loading/失败/真空态分离），挂 `EC._settingsView` 观测缝。

**偏差与理由**
1. 方案说"rollback() 返回 409"：HTTP 409 是传输层语义，`pipeline/feedback.py` 不依赖 FastAPI——改为 `assert_rollback_allowed()` 在端点层预检（冲突→HTTPException 409），`rollback()` 内部写前复查抛 `RollbackConflict`（兜并发双击，任务态 error、不追加账本）。语义与验收（第二次 409、账本行数不变）完全一致。
2. 浏览器实测发现 Vue3 运行时模板中**内联表达式裸用 `location` 点击无效**（模板作用域解析不到全局对象，render 闭包不受影响）：V4-01 的 detail-back 及元信息页既有"打开"按钮均中招（后者为潜伏存量 bug，顺带修复）。导航动作统一挪进 setup 函数（`goReportsList`/`openReport`）。
3. `summarize()` 的 `artifact_dir` 与既有 `dir` 同值（均 basename）——方案要求新字段供新前端统一消费，旧字段保留，重复成本为零。

**待用户裁决项**：无。

**真实联跑记录**：本批未消费真实生成额度（浏览器验收全部用现有产物 + 页面内 mock）。

### 批②（V4-09/10）

**改动摘要**
- V4-09：`fielddict.js` 重写——`EC.terms`（28 个术语，固定 {label, shortHelp, technical}）+ `EC.verdictLabel`/`EC.gateLabel`/`EC.lintText` 映射 + `EC.TermHelp` 问号 tooltip 组件（app.js 全局注册）；detail.js 五维评分/对账/校验/树健康/op 类型全部中文（技术键进 title/tooltip）；reports.js 列名 judge→报告评审、PASS/FAIL→已达标/需修改（原始值留 data 属性）；new.js lint 徽标中文；settings.js 中文标题在前、技术键行保留、里程碑术语删除。
- V4-10：templates.js 左栏搜索（名称/id）/lint 筛选/排序（更新时间/名称），行内名称+短 id+版本+节数+中文检查+更新时间；编辑器常用/高级渐进展开（会话记忆 sessionStorage，高级标题显示"已配置 N 项"）；切树前脏数据三选一（guardUnsaved：保存并切换/放弃修改/留在当前）；新建弹窗只必填名称+主题、id 可省略（`routes_trees.py::TreeNewIn.id` 可选，后端从名称派生 ASCII snake_case、纯中文回退 `tree_<日期>`、冲突加 -2/-3 序号；显式 id 创建不变）；KINDS/RENDERERS 显示中文在前值不变；M2 文案移除；17 个输入全部经 `:input-props` 透传 aria-label（naive-ui n-input 不透传外层 aria-label——实测踩坑后改用官方 input-props）。

**偏差与理由**
1. naive-ui n-input 的 aria-label 属性落在组件根 div 而非内部 input，第一版替换扫描 named=0；改用 naive-ui 官方 `:input-props="{ 'aria-label': … }"` 透传后 17/17 命中。中间一次批量替换误伤动态绑定行（`::input-props` 语法错误），已全部修复并以浏览器强刷 + 无 console error 验证。
2. 方案验收"主可见文本不出现 FAIL"：详情治理页 validate 条目的**数据原文**含"系统校验FAIL"字样（评审问题内容本身），属报告内容而非 UI 术语，不改写数据；UI 标签/徽标/列名已全部中文化。
3. `TreeNewIn.id` 从必填改为可选属于"只增不改"允许的范围（旧请求体显式 id 仍 2xx，语义不变）。

**待用户裁决项**：无。

**真实联跑记录**：本批未消费真实生成额度。验收用测试树（v410 系列与 id 系列、共 7 棵）已全部删除，用户真实树 25 棵未动。

### 批③（V4-02）

**改动摘要**
- 后端：`bus.py` 的 job worker 支持协作取消（`progress.cancel_event` 注入 + fn 抛 PipelineCancelled 或检查后正常返回 → status=cancelled）；end 事件增量带 `result`（done 时）；`routes_overview.py::GET /api/jobs/{id}` 增量字段 stage/message/run_dir/kind（旧字段全保留）；`routes_runs.py` 新增 `POST /api/jobs/{id}/cancel`（幂等，非 running 返回 {ok:false}）；`routes_trees.py` 抽出 `_preview_core` 并新增 `POST /{tree_id}/plan/preview/start` 后台化（同步 preview 保留）。
- 前端：`store.js` 重写为全局任务中心（register/update（不可变更新）/remove/list/subscribe/restore + sessionStorage `ec.activeTasks.v1` + kind/stage 中文映射）；`app.js` 壳渲染 `[data-testid="active-task-banner"]`（类型/中文阶段/已用时间/最后消息/离开提示/取消/查看/清除，重连 404 → "任务记录已失效"）；`new.js` 的 startRun/startPlan 注册任务并在 end 时更新 store，自动跳详情仅限"用户仍在向导第③步"，组件卸载关订阅+停轮询；`previewPlan` 改走 preview/start（SSE end.result 直接进结果卡）；`reports.js` 顶部"进行中的任务"运行中区（完成自动刷新一次列表不跳页）；生成运行中锁定第②③步上一步/下一步按钮。

**偏差与理由**
1. `GET /api/jobs/{id}` 已存在于 `routes_overview.py`（V3 遗留，且注册在前会先命中）——按"只增不改"在其上增量加字段（旧字段全保留），`routes_runs.py` 只新增 cancel 端点，避免重复路由。
2. 浏览器实测发现 store 的 `update` 若用 Object.assign 原地 mutate 元素，Shell 任务条不重渲染（register/remove 重建数组则正常）——改为不可变更新（map 重建数组与元素），语义等价且 IAB/生产一致。
3. 预检后台 job 中的 HTTPException 被 bus 转为任务 error 态（end 事件携带错误消息），前端按预检失败展示——无需在 job 内部维持 HTTP 语义。

**待用户裁决项**：无。

**真实联跑记录**：本批未消费真实生成额度（任务状态机用页面内 store 注入与 mock job 验证；真实生成留总验收）。

### 批④（V4-03/04/05）

**改动摘要**
- V4-03：第②步重排为 数据背景（意图+资料夹）→ 计划动作（使用已有/生成新）→ 覆盖检查 → 预检；`tree_plans()` 增量 8 字段（读不到→null）；抽出 `plan_fingerprint()` 公共函数（`_preview_core` 复用）；`plan/start` 出计划时写入 tree_version/tree_fingerprint/focus/folder；前端聪明缺省（最新+已确认+指纹一致才自动复用，否则默认生成新计划）；选中计划即拉内容加载覆盖区，0 缺口自动确认；确认后按钮消失转"已确认"标签；旧旁路收进"高级：跳过计划"折叠（默认关，保留来源门禁）。
- V4-04：`previewState` 状态机（idle/running/pass/fail/stale）+ 请求指纹（树版本/指纹/计划/意图/资料/裁决任一变→stale）；结果卡（事实数/来源计数/交叉校验/缓存命中/警告列表/可折叠样例）；`canNext2` 硬门化——必须预检 pass（或全定性 0 事实经二次确认后 pass）才能进第③步；fail 显示错误+修改数据设置+重新预检；第③步确认卡继续展示预检事实数/警告数。
- V4-05：`GET /api/settings/pipeline` 增量 `model_tiers`（自动/快速/质量优先，只出档名不泄配置）；第③步最终复核卡（树名/id/版本/节数/结构检查/计划确认态/意图/来源/预检事实与警告/预计 5–10 分钟）；档位单选来自后端列表（拉不到只显示"自动"）；`startRun` 显式发送 `model_tier`。

**偏差与理由**
1. 测试首版断言 `focus == make_plan 的 focus`——实现语义为"focus 记录出计划时的写作意图"（更符合复用判断），按实现修正断言。
2. `saved-plan-select` 仅在"使用已有计划"动作下渲染（planAction=new 时区域隐藏）——方案 DOM 断言默认选择器存在的前提是存在可复用计划；实测树旧计划无指纹不被自动复用（保守正确），切"使用已有计划"后选择器可见。

**待用户裁决项**：无。

**真实联跑记录**：本批未消费真实生成额度。

### 批⑤（V4-06/07）

**改动摘要**
- V4-06：导航/页题"模板"→"结构与文风"（路由不变）；templates.js 已选树编辑器头部加主按钮"用这棵树写报告"（→ `#/new?tree=<id>`）；未选中树时移除第二套生成对话，改说明卡+"对话生成新结构"（→ `#/new?start=chat`）+"新建空白结构（高级）"；空态文案改真实说明；new.js mount 解析 tree/start 深链（tree 存在→选中+起点锁定；不存在→`tree-link-error` 可恢复回三起点且零请求；start=chat 只展开对话）。
- V4-07：`trees/store.py` 新增 `plan_fingerprint()`（routes_trees re-export 共用）；`run_pipeline.py` meta.json 增量 plan_file/plan_fingerprint/facts_count/data_generated_at（指纹容错不阻塞生成）；`TreeRunIn` 增可选 `reuse_data_from`，from_tree 后端三重校验（_safe_dir 直接子目录→facts.json 存在→plan 必随行），argv 转 `--reuse-data`；detail.js 顶部/治理页/元信息页三处回环 CTA（reuseSameData 仅当 meta 新字段齐备）；new.js 解析 plan/mode/reuse 深链并逐项 API 校验（失败回第②步），startRun 发送 reuse_data_from，第③步显示复用数据信息。

**偏差与理由**
1. templates.js 未选中树时移除了生成对话输入框（方案 V4-06.2 明确要求），V4-11 的"两页按钮 disabled"验收在新形态下覆盖为：new 页生成对话 + templates 页选中树后的编辑对话（输入框仍存在，禁用逻辑同一实现）。
2. 一次 python 批量替换锚点重叠导致 `treeLinkError` 等进了观测缝但漏出 return（模板读不到、错误卡不渲染）——浏览器验收抓出，已修正并复验。

**待用户裁决项**：无。

**真实联跑记录**：本批未消费真实生成额度。
### 批⑥（V4-13）

**改动摘要**
- reports.js：搜索（标题/目录/来源关键词）+ 评审结论筛选（全部/需修改/已达标/未评审）+ 流程筛选（全部/结构树流程/经典）三组条件前端组合；筛选计数（"显示 N / 共 M 条"）常驻表头；列名/结论值批②已中文化；运行中区（批③）含进行中任务与完成自动刷新。
- detail.js：文件页分组——"可交付文件"（final.html / final.docx / chart_*.png）置顶含主下载按钮，"生成与审计记录"默认折叠（自管展开按钮 + v-if），分组只按受控文件名判断，`list_files()` 返回与下载校验未动。

**偏差与理由**
1. n-collapse 的展开内容在 IAB 下渲染不可靠（点击后 DOM 不出现），改用与 V4-10 高级区相同的自管展开（按钮 + v-if），行为一致且验收可控。
2. "失败任务保留查看错误/重新开始"：banner 失败态展示错误消息 + "查看"（跳 returnRoute 回向导重新配置）；未做一键"重新开始"按钮（重新开始语义=回向导改参重启，避免无脑重放失败任务烧额度）。

**待用户裁决项**：无。

**真实联跑记录**：本批未消费真实生成额度。


### 总验收记录（2026-09-16）

- `python -m pytest tests/ -q`：**121 passed**（存量 98 全部保留仍绿 + 新增 23：批① 8、批② 6、批③ 4、批④ 3、批⑤ 2——分属 tests/test_v4_batch1/2/4/5/tasks.py）。所有 LLM/联网调用 mock。
- 浏览器回归（§2.2.3）：#/new、#/reports、#/templates、#/settings 强刷后 `window.__lastErr` 全 null、console.error 收集 0 条、无未处理 rejection。
- 旧产物兼容硬门（§2.2.4）：稀土产物（无 V4 新 meta 字段）预览 iframe 加载、反馈/治理/文件/元信息五页签切换零错误、`/api/runs/report?...&format=docx` **200**、final.html 静态地址 **200**、文件集合 29 项无减少（3 可交付 + 26 审计）。
- 树模式 API 向后兼容（§2.2.5）：单测断言——plans 增量字段旧计划→null、plan/start 响应 `{id, events_url}` 不变、同步 preview 保留且缓存命中、GET /api/jobs 旧字段全保留、经典 /api/runs/start 契约不变（argv 无树模式参数）。
- 真实 LLM 联跑（2 次，预算 ≤4）：
  1. **V4-01 非法字符主题**（02:17~02:22，task `a2d8941d2ed7`）：project=`V4验收:石英砂/砂市?场*测试` → 产物 `artifacts/V4验收-石英砂-砂市-场-测试_20260916_022139/`（直接子目录 ✓、禁止字符零残留 ✓）、报告库可见（artifact_dir 字段 ✓）、meta 增量键齐（plan_file/facts_count=80/data_generated_at/plan_fingerprint=f14921d871aa）。
  2. **向导完整回归**（02:23~02:33，task `3fd2e4bc392f`）：现有树 → 已确认计划自动确认 → 预检 pass（缓存）→ 第③步最终卡 → 自动档生成 → 完成自动跳详情 `#/reports/中国高纯石英行业（砂-材料）市场研究_20260916_022847`（subject 半角 `/` 清洗为 `-` ✓）、任务条"生成报告已完成"、store 记录 done+artifactDir。
- 静态版本号最终态：app=R31、store=R14、api/icons=R13、fielddict=R13、new=R40、detail=R39、templates=R42、reports=R30、settings=R16。
- git：全部改动留在工作区未 commit/push（等用户验收后自行提交）。
