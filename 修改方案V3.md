# EvidenceCraft 工作台 V3 · 体验修复与流程加固方案

> 日期：2026-09-15 定稿。**V2 三阶段改造已交付并通过总验收**（《修改方案.md》
> 附录 A/B 有全部证据），本文件是 V2 交付后**用户实际试用反馈**的修复批次，
> 与 V2 规格互不覆盖：V2 已完成项不动，本文件只做新增量。
>
> 反馈来源：用户对向导/模板页/报告详情的真实操作（2026-09-15 上午）+ 我对
> 反馈问题的逐一复核定位。所有根因都已用代码走读或产物实锤确认，非猜测。

---

## 〇、问题总账（全部已核实）

| # | 问题（用户视角） | 根因定位 | 严重度 |
|---|---|---|---|
| P1 | 新建报告页三个入口堆叠在一页，看着像先后步骤而不是三选一 | new.js 第①步把三条起点全部平铺（左列示例卡×3+现有树下拉，右列对话卡），无选择态 | 体验 |
| P2 | 对话生成树后没有任何提示，想改结构还得去模板页；而且接着发消息会**再生成一棵新树**而不是改刚才那棵 | 向导对话卡只会调 /api/trees/generate（永远生成新树），不像模板页那样按"有无选中树"切换生成/编辑模式；生成成功后无结果反馈 | 体验+流程坑 |
| P3 | 没有文风卡的编辑入口；"在模板页修改文风卡片没有用" | 三件事叠加：①V2 范围只做了"选择+预览"，卡内容编辑是缺口；②换卡保存链路本身是通的（已 API 实测往返正确），但**只对下一次生成生效**，已生成的报告不会变文风——界面没告知这层语义；③真 bug：stylecards.load_card 带 `lru_cache`，**手改 config/style_cards/*.yaml 后不重启服务不生效** | 功能缺失+bug |
| P4 | 向导第②步填了本地文件夹，出计划/预检却完全没用上 | new.js 出数据计划的请求只传了 intent、漏传 folder（生成路径传了）——同一步里的两个请求参数不一致 | bug（已修一行待收尾） |
| P5 | 生成完自动跳转的报告详情页，反馈迭代/治理体检等页签全是空的；从报告库手动点进去却正常 | 流水线 end 事件的 `run_dir` 是**绝对路径**（D:\evidence-craft\artifacts\xxx），new.js 的 goDetail 直接拿它拼 URL；详情页用它调产物接口，后端目录白名单校验看到反斜杠一律 403 → **所有产物加载失败**（meta=null → 页签全空）。手动进入用的是纯目录名，所以正常。断线兜底路径取的是目录名，所以当时联调没暴露 | bug |
| P6 | 报告里"价格走势分析"只有 26 字残句，改两轮也没改完 | **三道防线叠加失守**（实锤产物：artifacts/中国高纯石英砂行业的供需格局…_20260915_103159）：①生成时该节 brief 要求"定量复盘分档价格 500~650 字"但事实库里只有 9 条对不上的旧价格事实，模型硬写失败搁笔成残句；F4 数据稀薄注入本应拦截，但其关键词判据（数据/数字/图表/口径）没匹配上这节 brief 的措辞（"定量复盘""配折线图"），未触发；②judge 第一轮已精准点名该节，但 issue 措辞含"缺少"二字，被 F3 关键词分类器误归**结构类**——结构类不喂重写，确定性处理器又只会删重复节/插表两种，处置结果为"跳过"（台账 structure_notes 里两轮都白纸黑字记着"已跳过：价格走势"）；③validate 标红了 length.价格走势: 正文 26 字（要求 150~1600），系统本会合成"扩写至区间内"的可执行指令，但 judge 合成 issues 时按 target 去重，被 LLM 那条（错误分类的）占位吞掉 | 流程 bug |
| P7 | （复核新发现）预检缓存指纹漏算 Excel 绑定 | F9 预检指纹只哈希 rag/web/db/tables_kept/corpus/mode，不含 file_bindings——换 Excel 不换查询时吃旧缓存 | bug（隐蔽） |
| P8 | （复核新发现）同一节被 judge 点多条意见时，逐条串行重写互相覆盖 | revise.apply 按 issues 逐条处理，同 target 第二条重写只带自己的指令、基于第一条产出再改——前面的修改可能被冲掉（压缩 vs 扩写指令并存时必然打架） | 质量隐患 |
| P9 | （实测产物）正文残留"（rag.02.00）"式引用编号 | `_strip_cites` 清洗正则未覆盖部分括号形态，judge 每轮都要花一条意见去修 | 小缺陷 |

**共同教训**：P6 不是模型单方面的问题——同样的残句下次还会被冻结，必须修流程。

---

## 一、修改项详述

### A. 向导第①步重构：三选一改为「先选起点，再走分支」（P1）

**现状**：三条起点平铺一页，视觉上是流水线四段（示例卡×3 + 现有树下拉 + 对话卡）。

**改法**（推荐卡片选择式；如评审时想改页签式再换，结构不变）：

1. 进入第①步先显示**起点选择视图**：三张并排起点卡——
   - 「从示例开始」：副文案"用内置模板改一改，最快"
   - 「对话生成」：副文案"描述你的报告，AI 出结构"
   - 「选择现有树」：副文案"复用之前建好的结构"
   单击选中（高亮描边），底部一个主按钮「用这个开始」。
2. 点「用这个开始」进入对应**分支视图**，顶部常驻「← 换个起点」返回：
   - **示例分支**：示例树列表（名称/一句话描述/节数/文风卡 tag），每行
     「复制为草稿」按钮（复用既有 /api/trees/templates/copy）。
   - **对话分支**：对话卡（placeholder 维持四行引导模板：主题/章节清单/
     表图要求/口吻篇幅），行为见 B 项。
   - **现有树分支**：filterable 树下拉（显示 名称（id，N 节）），选中即出
     结构摘要。
3. 任一分支选定树后，第①步底部出现**结构摘要条**（替换现在那行灰字）：
   `已选：〈树名〉 vN ｜ M 节 ｜ lint 状态徽标 ｜ [查看结构] [更换]`，
   「下一步：数据」按钮点亮。[查看结构] 展开只读章节清单（标题+类型+
   brief 首行），不提供内嵌编辑器（见非目标）。
4. 换起点再进来时清空已选树（`selId=null`），避免"示例的树 + 对话的语境"
   串状态。

**涉及文件**：`static/js/views/new.js`（模板与 setup 重排，逻辑基本复用）。

**验收**：
- 页面演示：进入 #/new 第①步只见三张起点卡与「用这个开始」，无堆叠；
- 三条分支各走一次：示例复制→摘要条出现；对话生成→摘要条出现；下拉选树→
  摘要条出现；每条分支「← 换个起点」能返回且不残留上一分支选中态；
- 摘要条显示树名/版本/节数/lint 徽标，与后端 /api/trees/{id} 返回一致
  （DOM 断言）。

### B. 生成后的衔接：结果卡 + 对话切换编辑模式（P2）

**现状**：生成成功后唯一变化是底部灰字换成树名、下一步按钮变亮；对话卡
继续发消息会调用生成接口**再造一棵树**。

**改法**：

1. **结果卡**：对话生成成功（或示例复制成功）后，在分支视图顶部插入结果卡：
   - 标题行：「结构树已生成：〈树名〉（N 节）」/「已从示例复制为草稿：…」
   - 只读章节清单：每行 `heading/标题 · 类型 · brief 首行 40 字`
   - lint 摘要（0 错误 N 提醒 徽标；有 error 时红显并提示先处理）
   - 按钮：`[继续调整（对话）]`（聚焦对话输入框）`[就这样，去配数据 →]`
     （等价于点下一步）
2. **对话卡双模式**：与模板页同规则——`selId` 为空＝生成模式
   （POST /api/trees/generate）；`selId` 非空＝编辑模式
   （POST /api/trees/{id}/chat，带 base_version 乐观锁）。标题与按钮文案
   随模式切换（"生成结构树"/"发送修改意见"）。向导内无手动编辑器，
   dirty 恒为 false，无需 B3 三选一保护。编辑成功后结果卡与摘要条同步
   刷新（版本 +1 可见）。
3. 编辑模式下对话卡顶部给一行灰字：「正在调整〈树名〉；发"再加一节讲
   XX"这类意见即可，要另起一棵新树请点 ← 换个起点」——把"改这棵"和
   "造新的"两条路 explicitly 分开，消除 P2 的坑。

**涉及文件**：`static/js/views/new.js`。

**验收**：
- 页面演示（对话分支全流程）：描述需求 → 出树 → 结果卡出现（含章节清单
  与 lint 徽标）→ 点「继续调整」→ 发送"把第 X 节拆成两节" → **同一棵树
  版本 +1**（结果卡与摘要条刷新，不产生新树）→ 点「就这样，去配数据」
  进入第②步；
- 网络断言：编辑消息命中的是 /api/trees/{id}/chat 而非 /api/trees/generate；
- 换起点后再对话，命中的是 generate（生成模式恢复）。

### C. 文风卡：补编辑能力 + 修缓存 + 语义说明（P3）

**现状**：卡内容只读（YAML 手改不生效——lru_cache）；树编辑器只有选择
下拉；无任何"什么时候生效"的说明。

**改法**：

1. **去掉 `pipeline/stylecards.py` 的 `lru_cache`**（`_card_path`/`load_card`）。
   卡文件是几百字节的小 YAML、调用频率低，缓存没有收益只有 staleness
   害处——去掉后手改 YAML 与界面保存都立即生效（`list_cards` 本来就没
   缓存）。
2. **模板页加「文风卡」管理区块**（放在页面底部独立卡片，不进树编辑器
   ——卡片是全局资产，与单棵树平级）：
   - 卡片列表：名称 / 描述 / 规则首行 / 节选数 / 来源 tag（内置/自定义）；
   - 操作：**新建**（填 id（英数下划线）+名称）、**编辑**（名称、描述、
     规则多行、节选列表增删——每条节选一个多行文本 + 可选出处）、
     **复制内置卡为自定义**（三张内置卡 journal_paper/industry_research/
     exec_brief 只读保护：编辑内置卡时自动转为"复制为自定义"流程）、
     **删除**（仅自定义卡，内置卡按钮禁用）。
3. **后端端点**（树模式只增不改）：
   - `POST /api/trees/style-cards`（新建：{id, name, desc, rules, excerpts[]}，
     id 合法性校验+防覆盖）；
   - `PUT /api/trees/style-cards/{id}`（保存；内置三卡返回 403）；
   - `DELETE /api/trees/style-cards/{id}`（仅自定义；删除前检查是否有树
     引用，被引用时返回 409 附引用树清单）；
   - 写入即落盘 `config/style_cards/<id>.yaml`（UTF-8）。
4. **语义说明**：树编辑器文风卡下拉下方加灰字：「文风卡在**下一次生成**
   时生效；已生成报告的文风调整走反馈迭代（反馈意见 > 文风卡 > 默认规范）」。

**涉及文件**：`pipeline/stylecards.py`、`server/routes_trees.py`、
`static/js/views/templates.js`、新建或复用卡片编辑弹窗。

**验收**：
- 单测：新建/保存/删除端点（含内置卡 403、被引用删除 409、YAML 往返
  字段完整）；缓存移除后修改文件再 load_card 结果为新值（monkeypatch
  或临时文件夹）；
- 页面演示：模板页新建一张卡（填规则+两条节选）→ 树编辑器下拉立即出现
  → 选中保存 → 树 yaml 落盘正确 → 删除时（有树引用）弹 409 提示；
- 页面演示：复制"行业综述"为自定义 → 改规则一行 → 保存 → 无需重启，
  预览立即显示新规则（等价于验证 lru_cache 已除）；
- 下拉旁语义灰字可见（DOM 断言）。

### D. 文件夹接线收尾（P4）

**现状**：new.js 出计划已补传 folder（一行修复已在工作区，本项收尾），
但用户看不到"文件夹被用上了"的证据。

**改法**：

1. 向导第②步出计划成功后，回执区顶部显示资料摘要行：
   「资料：PDF 语料 N 片段（新建/缓存）｜ Excel M 张（表 id 列表前 3 个）」
   ——数据来自 plan.corpus 与 plan.files（F11 已落盘，纯前端展示）；
2. 无 PDF 仅 Excel 时显示「PDF 无｜ Excel M 张」；两者皆无时不显示该行；
3. 预检结果中 `by_source` 已按来源分布展示（local_file:xxx 即 xlsx），
   保持现状即可，不做新改动。

**涉及文件**：`static/js/views/new.js`（出计划回调处）。

**验收**：
- 页面演示（夹具 data/geology_demo）：第②步填文件夹 → 出计划 → 回执区
  出现「Excel 1 张（assay_ledger）」→ 预检显示事实且 by_source 含
  local_file 来源；
- 网络断言：/plan/start 请求体含 folder 字段（P4 根修的回归验证）。

### E1. F3 分类器收敛 + 处理不了的结构 issue 退回重写通道（P6-②）

**现状**：`judge.py` 尾部按关键词（重复/缺失/缺表/未附表/缺少/章节/多余/
顺序）打 kind；"某节内容缺少/残句"这类**写作问题**被误归结构类 →
`revise.apply` 过滤结构类 → `handle_structure_issues` 只会删重复节/插表
两种，其余记"跳过"丢弃 → 问题永久冻结。

**改法**：

1. **分类规则重写**（judge.py）：
   - 先判**写作信号词**：残句/截断/未完成/仅.{0,4}字/扩写/压缩/字数/
     内容缺少/论述不足/深度不足/未展开 —— 命中任一 → 不归结构类
     （按现有规则继续判 data/style）；
   - 结构类收紧为**结构动作**：重复节/多余节/节顺序/章节缺失/缺表/
     未附表/未附图/结构缺失（动作词+结构对象组合才算）；
   - 其余默认 style。保持 judge 输出协议不变（仍三值 kind）。
2. **退回通道**（revise.py + run_pipeline.py）：`handle_structure_issues`
   返回值从 `handled: list[str]` 改为 `(handled, unhandled_issues)`——
   没能确定性处理的结构 issue 原样返回；run_pipeline 把 unhandled 的
   kind 改写为 "style" 并入本轮 revise.apply 的 issues（附一条 note：
   "结构类问题未能确定性处理，已转写作通道修复"）。结构类从此只有两种
   出口：被确定性处理，或被重写，**不存在丢弃**。

**衔接说明**："章节缺失"类结构 issue（树上有该节、稿件里整节没有）不在
退回通道里硬闯——revise.apply 按 section_id 找不到正文条目会是 no-op；
该形态由 F1 的确定性"补生成缺失节"处理。仍可能 no-op 的只剩 judge 幻觉
出不存在的节 id 这类脏输入，转写作通道后静默跳过即可接受（handled 记
note）。

**涉及文件**：`pipeline/judge.py`、`pipeline/revise.py`、`run_pipeline.py`。

**验收**：
- 单测（用实锤原文做用例）："正文仅一句残句（26字），缺少暴涨与回落
  复盘、分档价格、驱动因素分析与折线图，未满足500~650字要求" →
  kind **≠ structure**；"存在重复节「储运」" → structure；
  "正文存在 3 个无法对账的数字" → data（回归）；
- 单测：一个"既不像重复节也无表模板可插"的结构 issue（如本例原文强标
  structure）经修订循环组合逻辑后**进入了 revise.apply 的重写列表**
  （mock chat_json 断言被调用且指令含原 problem；可将循环内"结构处理+
  转发"组合段抽为可测函数，不必整跑 main）；
- 联跑：见总验收第 3 条。

### E2. judge 合成指令不再被去重吞掉（P6-③）

**现状**：judge.run 两处兜底（对账未匹配合成、validate FAIL 合成）都用
`have = {target}` 去重——LLM 先占了同 target 的坑（哪怕分类错误、不可
执行），系统合成的可执行指令就被吞。

**改法**：去重改为**合并**——合成时若 target 已有 issue，把合成
instruction **追加**进那条 issue 的 instruction（换行拼接，去重语义
保留、信息不丢）；没有才新建条目。追加时给合成段加前缀
"【系统校验】"以便区分来源。这样同 target 的指令始终一条、重写一次、
指令完整。

**涉及文件**：`pipeline/judge.py`（两处 have 判定）。

**验收**：
- 单测：LLM 输出含 target=价格走势 的 issue，validate 有
  length.价格走势 fail → 最终 issues 中该 target **恰一条**，且
  instruction 同时含 LLM 原文与"扩写至区间内"（或等价系统指令）；
- 单测：无冲突时合成条目行为与旧版一致（新增条目、verdict=fail）。

### E3. F4 数据稀薄判据修正（P6-① 预防）

**现状**：`thin_section_if_needed` 靠 brief 关键词（数据/数字/图表/口径）
判断"要数据"——"定量复盘""配折线图"这类措辞漏网，价格走势节未注入
定性声明。

**改法**：

1. **判据重写**（注意：不只是删关键词——现有跳过逻辑也要改）：
   - 触发条件：**节 data_needs 非空 且 `facts_for_section < 3` 且
     outline 计划引用事实数 <3**（两个信号**都**稀缺才注入）；
   - 现有代码的跳过判断是 `min(n_avail, max(old_cited,0) or n_avail) >= 3`
     ——任一信号弱就注入。data_needs 是无空格中文短语时 token 匹配
     几乎必然算出 n_avail=0，会连"outline 已引用 5 条事实、写得好好的"
     节也误注入，反而压制其用数。必须改成 `n_avail < 3 and old_cited < 3`
     才注入（即任一信号 ≥3 即跳过）；
   - brief 关键词判断（数据/数字/图表/口径）整个删除。
2. **已知局限（写进方案，不隐藏）**：数量判据识别不了"事实不相关"——
   价格走势节引用了 9 条旧口径价格事实，若 outline 给它分了 ≥3 条，
   依然不会注入（引用数够了但口径全不对）。事实相关性判断超出本批
   范围，这类情况的兜底是 E4 残句防线 + E1/E2 修订出口。

**涉及文件**：`pipeline/sections.py`（thin_section_if_needed）、
`tests/test_phase2_loop.py`（改 test_f4 用例）。

**验收**：
- 单测（用实锤节定义做用例）：style="以2022—2026年为主线，定量复盘…
  配折线图。500~650字"、data_needs=["高纯石英砂分档历史价格",…]、
  事实库无匹配 → **注入**（旧实现不注入，此用例就是回归实锤）；
- 单测（回归）：无 data_needs 的节不注入；事实充足（≥3 命中）不注入；
- 联跑：见总验收第 3 条（新运行 thin_warnings 应包含价格走势类节）。

### E4. 残句防线：超短节生成即重试（P6-① 拦截）

**现状**：模型搁笔产出 26 字残句直接进入流水线，全靠下游 judge 发现。

**改法**：`gen_text_section` 与 `gen_view` 返回后做廉价检查——
正文长度 < `max(60, body_len 下限 × 0.5)`（无区间时取 60）判定为疑似
残句，**立即重试一次**，重试时在 guidance 追加提示：
「上一稿仅 N 字、疑似未写完被丢弃。请一次完整输出 X~Y 字的成稿，不要
中途停笔」。重试仍短则接受产出并记入 `thin_warnings`
（前缀"残句重试后仍短"），交给下游（修复后的 E1/E2 能兜住）。
检查与重试都在 `sections.py` 内完成，run_pipeline 不感知。

**涉及文件**：`pipeline/sections.py`。

**验收**：
- 单测：mock chat_json 第一次返回 26 字、第二次返回合格正文 → 最终
  产出为第二次结果，且第二次请求的 user 文本含"疑似未写完"提示；
- 单测：两次都短 → 返回第二次结果且 thin 警示记录（返回值或调用侧
  可观测）；
- 单测（回归）：正常长度正文零重试（chat_json 恰调用一次）。

### E5. 自动跳转修复：goDetail 归一化目录名（P5）

**现状**：end 事件 `run_dir` 为绝对路径，goDetail 直接拼接 → 详情页所有
产物接口 403 → 页签全空。

**改法**：new.js `goDetail(dirName)` 入口统一归一化：
`String(dirName).replace(/\\/g, '/').split('/').pop()`（取最后一段）；
pollRecover 里已有的取值保持不变。同时 detail.js 的 dir 解析处加同样的
防御（`/reports/` 参数段已是单段，风险低，双保险即可）。

**涉及文件**：`static/js/views/new.js`（一行）、`static/js/views/detail.js`
（防御性一行）。

**验收**：
- 页面演示：向导正常连着 SSE 生成一篇（不经断线路径）→ 完成自动跳转
  → 详情页五页签全部有数据（DOM 断言：meta 树名、judge 分、轮次、文件
  清单非空），URL 中目录名不含 `%3A`/`%5C`；
- 回归：断线兜底路径跳转仍正常（复跑一次断线演示）。

### E6. 预检缓存指纹漏算 Excel 绑定（复核新发现的 bug）

**现状**：F9 预检的内容指纹只哈希 rag/web/db/tables_kept/corpus/mode
六个字段，**不含 file_bindings/files**——用户换了资料夹里的 Excel 但
计划查询没变时，指纹一致，预检直接吃旧缓存（旧 Excel 的事实数/表格）。

**改法**：`server/routes_trees.py` 预检端点的 `fp_src` 字段列表补上
`file_bindings`（files 摘要随 file_bindings 变化，无需单列）。

**涉及文件**：`server/routes_trees.py`（一行）。

**验收**：
- 单测：两份计划仅 file_bindings 不同（同名不同 path）→ 指纹不同 →
  第二次预检真跑数据层（run_data_layer 被调两次）；完全相同的计划 →
  缓存命中（只调一次）（在现有 test_plan_preview.py 上加用例）。

### F1. 缺节确定性生成（E1 盲区补全）

**现状**：judge 报"章节缺失"（树上有该节、稿件整节没有）→ 转重写通道后
revise.apply 按 section_id 在 texts 里找不到目标，重写为 no-op，该节
永远缺。

**改法**：`handle_structure_issues` 增加第三种确定性处理——issue 判为
"章节缺失"（结构动作词命中"章节缺失/缺少.*节/整节缺失"）且
`spec.section_by_id(target)` 存在、texts 无该 section_id 时：按树定义
就地生成（复用反馈结构调整的生成路径：`sections.gen_text_section(doc,
sec, {"guidance": issue.instruction}, spec)`），插入位置按 spec 章节序。
只改本运行内存不动树，handled 记"缺失节〈X〉已按树定义补生成"。

**涉及文件**：`pipeline/revise.py`。

**验收**：单测——树两节、稿件只有第一节，喂"第二节整节缺失"结构 issue
→ 一轮后 texts 含第二节正文、树文件版本未变（mock gen_text_section）。

### F2. 图表渲染失败上界面（体验坑）

**现状**：render_charts 单图失败（多为数据不足/列不存在）记 warning 后
跳过——信息只在生成日志一闪而过；用户看到的是"brief 说配图但报告没图"，
反馈"把图加上"后修订通道永远救不了（图由代码按数据渲染），白白烧轮次
（实测第二轮 judge 确实点了"配柱状图但仅文字描述"）。

**改法**：
1. `run_pipeline.py` 渲染阶段把 render_charts 返回的失败信息收集为
   `chart_failures`（图 id/标题/原因），写入 meta.json 新字段——注意
   meta.json 在大纲阶段已落盘，此处读入→补字段→写回（树/经典两模式
   都写，行为只增不改）；
2. 详情页治理体检页签加"图表状态"小块：spec 中声明的每张图
   （渲染成功 ✓ / 失败原因 / 未声明—）——数据源=meta.chart_failures +
   spec.charts；
3. 详情页顶部 FAIL 警告条与完成摘要处，若存在 chart_failures 追加一行
   "N 张图因数据不足未渲染（详见治理体检）"，引导用户去补数据/改图源
   而不是反复反馈。

**涉及文件**：`render/charts.py`（返回失败清单，现返回值已含 jobs，
补 failures）、`run_pipeline.py`（落 meta）、
`static/js/views/detail.js`（两处展示）。

**验收**：单测——render_charts 对一张数据缺失的图返回 failures 条目
（原因含"数据"或列名）；页面演示——用现有含图但数据缺的运行（如
高纯石英砂 20260915_103159，其 chart_price_2526 因 facts:web 无匹配
数据未渲染）打开治理体检，能看到该图失败原因行。

### F3. revise 按 target 聚合多条意见（防覆盖）

**现状**：revise.apply 逐条遍历 issues，同 target 第二条重写只带自己的
指令、基于第一条的产出再改——前面的修改可能被冲掉。

**改法**：`revise.apply` 入口先把 issues 按 target 分组，同 target 多条
合并为一条（problem 拼接去重；instruction 按序拼接）后进入既有重写
流程——每节每轮只重写一次、指令完整。title/views 槽位/表格说明/风险
章的同 target 聚合同理。

**涉及文件**：`pipeline/revise.py`（apply 入口约 10 行）。

**验收**：单测——同 target 两条 issue（"压缩到 300 字"+"补充驱动因素"）
→ mock chat_json 恰被调用一次，且 user 文本同时含两条指令；不同 target
互不影响（调用两次）。

### F4. 模板页树列表 lint 徽标（小打磨）

**改法**：模板页树列表行（id · vN · M 节那行）追加 lint 状态徽标
（0E/0W 绿、有 W 黄、有 E 红）。数据取后端：`trees/store.py` 的
`list_trees` 对每棵树跑 lint 附 `{errors, warnings}` 计数（树数量
个位数，成本可忽略），前端只渲染。

**涉及文件**：`trees/store.py`（list_trees）、`static/js/views/templates.js`。

**验收**：页面演示——列表行出现徽标；人为造一棵带警告的树（brief 提
"表"的 text 节）后徽标变黄。

### F5. 引用编号清洗正则补全（小缺陷）

**现状**：`_strip_cites` 未覆盖部分全角括号/无括号形态，正文残留
"（rag.02.00）（rag.04.00）"（实测产物 judge 点名）。

**改法**：`pipeline/sections.py` 的 `_strip_cites` 正则扩展：全角括号
（…）、半角 (…)、方括号［…］/[…] 包裹的 `[a-z]{2,4}[.\-]\d+[.\-]\d+`
形态统一剥除；剥除后相邻多余的空格/标点收尾清理。对已生成产物不回改。

**涉及文件**：`pipeline/sections.py`（一处正则）。

**验收**：单测——含"（rag.02.00）""(web.01.02)""［rag.03.00］"及行内
裸"rag.02.00"的正文 → 清洗后四种形态全部消失且句读完整；无编号正文
零变化。

---

## 二、总验收（全部通过才算本批次完成）

1. **逐项核对表（硬门）**：对照 A/B/C/D/E1~E6、F1~F5 十五项生成核对表（附录形式
   回填进本文件），每项附可查证据（单测名/页面演示记录/产物路径/命令
   输出）；无证据项＝未完成，禁止事后补造。
2. **pytest 全绿**：现有 71 项不改仍绿；新增 ≥17 项（E1×2、E2×2、E3×2、E4×3、C×3、E6×1、F1×1、F2×1、F3×1、F5×1 量级），预计 ≥88。
3. **P6 复现联跑**：用同一棵树「国内高纯石英产业研究报告」按原路径
   （同意图、同计划模式）重新生成一次，断言：
   - 生成后"价格走势"节正文 ≥ 其字数区间下限的 0.5 倍（E4 拦截）或
     thin_warnings 含该节（E3 注入）；
   - 若 judge 仍点名该节，修订第 1 轮后该节字数显著变化（E1/E2 生效，
     不再两轮 26 字不动）——修订轨迹 revision_history.json 各轮 lens
     作证；
   - 产物存档 `artifacts/_baseline/p6_regression/`（轨迹 JSON 副本）。
4. **P5 复现演示**：向导生成 → 自动跳转详情五页签有数据（浏览器 DOM
   断言留证）。
5. **页面演示清单**：A 三分支各一次、B 对话闭环一次（同树版本 +1）、
   C 建/编/删卡各一次（含 409 引用提示）、D 文件夹回执一次、E5 一次、
   F2 治理体检图表状态一次、F4 列表 lint 徽标一次；E6 仅单测。
6. **回归**：旧类型 `--reuse-data` 联跑不破坏；四页（#/new、#/reports、
   #/templates、#/settings）强刷无 console 报错。
7. **收尾**：本文件附录回填实施记录（改动+证据+命令）；API.md 补录
   style-cards 写端点；静态版本号统一递增。

---

## 三、工程硬约束（沿用 V2）

- 免构建链（禁 node/npm/打包器）；中文一律 UTF-8；
- 不改 config/settings.yaml 的 model 段与任何 key；密钥不入库；
- 后端冻结边界不变（--type/--spec、routes_types、/api/runs/start、
  /api/runs/preview 语义不动）；树模式 API 只增不改；feedback 协议不变；
- LLM 调用全走 pipeline/llm.py，缺省行为不变；单测一律 mock LLM；
- 治理不旁路：A4 门禁、每轮对账/校验重跑等 V2 硬线全部维持；
- 全程不 git commit/push，改动留在工作区；
- 已知环境怪癖沿用 V2 附录 B 记录（IAB rAF 挂起时对卡死弹层清过渡类；
  EC._*View 观测缝继续用于演示）。

## 四、非目标（本批次不做）

- 向导内嵌迷你结构编辑器（结构调整走对话通道已闭环；编辑器仍在模板页）；
- judge 评审质量/提示词改造（本批只修指令路由，不动评分逻辑）；
- 报告库/详情页视觉打磨（空态、间距、响应式）；
- 文风卡与反馈迭代的联动（反馈时自动选卡）；
- M4 及以后范围（文风蒸馏、风格飞轮等，维持 V2 非目标）。

## 五、实施顺序与工作量

**第一批 E+F（E1~E6、F1~F5 共十一个 bug/流程加固，纯后端为主，约 40%）→
第二批 D（收尾，5%）→ 第三批 C（文风卡，25%）→ 第四批 A+B（向导重构，
30%，两者同属 new.js 第①步，合并实施合并验收）**。每批做完自测过批验收
再进下一批，全部通过后做总验收。预计总量一个工作日内。

实施纪律沿用 V2：分项验收失败按条目归因隔离修复，不放宽验收线；模型
接口故障等既有重试兜底；页面演示以 DOM 断言/截图留证。

---

## 附录 A · 逐项完成核对表（随实施回填）

| 项 | 内容 | 状态 | 证据 |
|---|---|---|---|
| A | 向导三选一起点卡+分支 | ✅ 完成 | 页面演示（DOM 断言）：三张起点卡 `[data-testid="start-*"]` + 「用这个开始」，旧堆叠入口消失；示例分支复制为草稿→摘要条出现；对话分支生成→摘要条出现；现有树分支下拉选树→摘要条出现；「← 换个起点」返回后 result-card/summary-bar 清空、确认按钮回置灰（不残留）；摘要条显示 树名/vN/节数/0E0W 徽标 + [查看结构] 展开 + [更换] |
| B | 结果卡+对话双模式 | ✅ 完成 | 页面演示：生成→结果卡（树名/节数/章节清单/lint 徽标/两按钮）；网络断言 `window.__net`：编辑消息命中 `/api/trees/{id}/chat`（非 generate），同树 v1→**v2**；编辑灰字 hint 出现；「就这样，去配数据」进入第②步；结果卡标题三分支文案（已生成/已复制/已按意见更新） |
| C | 文风卡管理+缓存+语义 | ✅ 完成 | 单测 tests/test_v3_batch3.py 9 项（create_roundtrip/409/422、put 立即生效含手改 YAML 验证、内置 403、删除 409 附清单、id 校验）；页面演示：建卡 demo_style_v3（2 节选）→ 下拉出现 → 选中保存 smoke_text v4→v5（tree.yaml:55 `style_card: demo_style_v3` 落盘）→ UI 删除弹 409 toast「正被 1 棵树引用：smoke_text」→ 解除引用后删除成功；复制内置卡自动转自定义（exec_brief→exec_brief_custom）；下拉旁语义灰字 DOM 断言 |
| D | 文件夹接线收尾 | ✅ 完成 | 网络断言：/plan/start 请求体 `folder:"data/geology_demo"`；回执区资料行「资料：PDF 无｜Excel 1 张（assay_ledger）」与方案文本一致；预检 78 条事实 by_source 含 `local_file:assay_led`（plans/_preview_616b7c0bb55e718a.json） |
| E1 | F3 分类收敛+退回通道 | ✅ 完成 | 单测：test_e1_write_issue_not_structure（P6 实锤原文→style）、test_e1_structural_actions_are_structure、test_e1_data_reconcile_regression、test_e1_unhandled_structure_routed_to_rewrite（route_structure_issues 组合函数）；judge.classify_issue_kind 抽为可测函数 |
| E2 | judge 合并去重 | ✅ 完成 | 单测：test_e2_merge_appends_to_same_target（同 target 恰一条、instruction 同时含 LLM 原文与【系统校验】段、幂等）、test_e2_merge_new_target_when_no_conflict（无冲突=旧行为）；judge._merge_synthetic_issue 两处兜底共用 |
| E3 | F4 判据修正 | ✅ 完成 | 单测 test_phase2_loop.test_f4_thin_section_injection 重写：P6 实锤节定义（"定量复盘…配折线图。500~650字"+data_needs 无匹配）→ 注入；无 data_needs 不注入；≥3 命中不注入；**n_avail=0 但大纲已引 5 条 → 不注入**（旧 min() 写法误注入的回归）；已知局限（事实相关性）记本附录 B |
| E4 | 残句防线 | ✅ 完成 | 单测：test_e4_truncated_retry（首稿 17 字→重试提示含"疑似未写完"→二稿采用）、test_e4_still_short_records_thin、test_e4_normal_length_no_retry（恰 1 次调用）；sections._trunc_retry/_body_floor；run_pipeline 收 thin 落盘 |
| E5 | 跳转归一化 | ✅ 完成 | 页面演示（P5 联跑）：向导 SSE 生成 134651 → 自动跳转 `#/reports/国内稀土出口管制与产业动态_20260915_134651`，URL 无 `%3A/%5C`；五页签 DOM 断言全有数据（meta.tree_id/judge 36 分/6 节正文/文件 14 项/图表状态）；detail.js dir 归一化双保险 |
| E6 | 预检指纹补 file_bindings | ✅ 完成 | 单测 test_plan_preview.test_preview_fingerprint_includes_file_bindings（换 Excel→指纹变→run_data_layer 被调 2 次；同计划→缓存命中 1 次）；routes_trees.py fp_src 补第 7 字段；API.md 同步 |
| F1 | 缺节确定性生成 | ✅ 完成 | 单测 test_f1_missing_section_generated_inplace（树 2 节稿件 1 节→喂"第二节整节缺失"→texts 含第 2 节、树版本恒 v1）；revise.handle_structure_issues 缺节分支（_MISS_SEC_RE+按 spec 序插入） |
| F2 | 图表渲染失败上界面 | ✅ 完成 | 单测 test_f2_render_charts_reports_failures；联跑产物 …_131922/meta.json `chart_failures` 2 条（新代码自动落盘）+ feedback 轮同步刷新；页面演示：治理体检「图表状态」✓2 张成功 + ✗×2 带真实原因行、顶部警告条"N 张图因数据不足未渲染" |
| F3 | revise 按 target 聚合 | ✅ 完成 | 单测：test_f3_merge_issues_by_target、test_f3_apply_same_target_single_rewrite（恰 1 次 user 含两指令）、test_f3_apply_distinct_targets_two_calls；revise.merge_issues_by_target + apply 入口聚合 |
| F4 | 模板页列表 lint 徽标 | ✅ 完成 | 单测 test_f4_list_trees_carries_lint（0E≥1W 断言）；页面演示：树列表行徽标 0E/0W（绿）与 0E/2W（黄，industry_review）；store.list_trees 附 lint 计数 |
| F5 | 引用编号清洗正则补全 | ✅ 完成 | 单测：test_f5_strip_cites_all_forms（全角（）/半角()/［］/裸编号四形态消失、句读完整）、test_f5_clean_text_unchanged（无编号零变化）；sections._STRIP_CITE_RE |

**pytest：98 项全绿**（V2 存量 71 + 新增 27：test_v3_batch1.py×17、test_v3_batch3.py×9、
test_plan_preview.py×1；E3 用例在 test_phase2_loop.test_f4 重写）。
**联跑**：P6 复现（…_131922，断言见下）、P5 复现（…_134651）、--reuse-data 回归
（000803_20260915_131957，exit 0）全部通过。

## 附录 B · 实施记录（随实施追加）

### P6 复现联跑断言（总验收第 3 条）
- 路径：`python run_pipeline.py --full --tree 国内高纯石英产业研究报告 --plan 20260915_102804.json --intent <同原图>` → 产物 `artifacts/中国高纯石英砂行业的…_20260915_131922`
- 旧实锤（…_103159）：价格走势两轮恒 **26 字**，structure_notes 两轮记"已跳过：价格走势"
- 本次：round1 lens 价格走势 **648 字**（≥下限 500×0.5=250，E3/E4 防线达标）；
  judge 点名后第 1 轮修订 648→**202** 显著变化（E1/E2 出口生效，不再冻结）；
  structure_notes **空**；thin_warnings 空（首轮即达标，注入未需触发）
- 轨迹存档 `artifacts/_baseline/p6_regression/`（revision_history/judge_report/meta/sections/README.json）
- 备注：settings.yaml `revise_rounds: 2`（既有配置），两轮后退出为配置行为

### 改动文件清单
后端：pipeline/judge.py、pipeline/revise.py、pipeline/sections.py、pipeline/feedback.py、
pipeline/stylecards.py、run_pipeline.py、render/charts.py、server/routes_trees.py、trees/store.py；
前端：static/js/views/new.js、detail.js、templates.js、static/index.html（?v=R32 统一）；
文档：API.md（style-cards 三写端点、预检指纹字段补 file_bindings）；
测试：tests/test_v3_batch1.py（新 17）、tests/test_v3_batch3.py（新 9）、
tests/test_plan_preview.py（+1）、tests/test_phase2_loop.py、tests/test_feedback.py

### 偏差与理由（以代码现实为准的最小调整）
1. **judge.py 修既有潜伏 bug**：`pipeline_temperature` 在 V2 F5 引入时未导入（judge.run 一调用即 NameError，存量测试经 mock 掩盖）——补一行导入。
2. **F2 详情页"图表状态"数据源**：方案写"meta.chart_failures + spec.charts"，但详情页无 spec 数据源且不加新端点——实现为 `meta.chart_failures`（失败✗+原因）+ 产物 `chart_*.png` 计数（成功✓）；验收核心"能看到失败原因行"达成。
3. **F2 演示产物换 131922**：方案原定 103159 为旧代码产物（meta 无 chart_failures 字段），改用本批新代码真实联跑产物 131922（2 张失败图原因真实：`无匹配数据事实：rag*`）。
4. **A 项示例分支「继续调整（对话）」**：示例分支无对话输入框可"聚焦"，实现为切换到对话分支（复用 B 编辑模式链路），行为等价。
5. **存量测试适配（断言不变）**：V2 test_f3_duplicate/missing_table 两处对 handle_structure_issues 新元组返回值解包（E1 签名变更）；test_feedback.py 三个测试改 monkeypatch fixture——修复直接赋值模块属性不恢复导致的**跨测试污染**（正是它掩盖了 E4 测试初跑失败）。
6. **E1 组合段抽为 `run_pipeline.route_structure_issues`**（方案验收允许"抽为可测函数"）；unhandled 转写作通道时以对象 id 去重防重复转发。
7. **模板串 `\n` 坑（V2 已知）复发**：F4/C 新增模板里 `.split('\n')` 导致 vue.prod 编译「Invalid or unexpected token」、整页渲染中断——改 `\\n`（与 V2 `join('\\n')` 同解）。排查方法留档：`Vue.compile(template)` 逐行删行定位。
8. **观测缝扩充**：EC._newView 增 startSel/startPoint/showOutline/resultCard/lastCardAction/preview/confirmStart/changeStart/replaceSelection/switchToChatBranch；EC._templatesView 增 styleCards/loadStyleCards/scForm/scShow/openNewCard/openEditCard/saveCard/deleteCard/addExcerpt；EC._detailView 增 chartStatus。
9. **E3 已知局限（方案原文照录）**：数量判据识别不了"事实不相关"——若 outline 给价格走势分了 ≥3 条口径全错的旧事实，仍不会注入；兜底为 E4 残句防线 + E1/E2 修订出口（本次 P6 联跑中该节首轮即 648 字达标）。
10. **演示辅助**：对话生成树「国内稀土出口管制月度动态简报」与文风卡 demo_style_v3（已删）、测试草稿树为演示产物，保留在树清单中可复验；无生产数据被修改或删除。
